// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.

import MiniSearch from 'minisearch';
import * as fs from 'node:fs/promises';
import * as path from 'node:path';
import { getLogger } from './logger';

type PerLanguageData = {
  method?: string;
  example?: string;
};

type MethodEntry = {
  name: string;
  endpoint: string;
  httpMethod: string;
  summary: string;
  description: string;
  stainlessPath: string;
  qualified: string;
  params?: string[];
  response?: string;
  markdown?: string;
  perLanguage?: Record<string, PerLanguageData>;
};

type ProseChunk = {
  content: string;
  tag: string;
  sectionContext?: string;
  source?: string;
};

type MiniSearchDocument = {
  id: string;
  kind: 'http_method' | 'prose';
  name?: string;
  endpoint?: string;
  summary?: string;
  description?: string;
  qualified?: string;
  stainlessPath?: string;
  content?: string;
  sectionContext?: string;
  _original: Record<string, unknown>;
};

type SearchResult = {
  results: (string | Record<string, unknown>)[];
};

const EMBEDDED_METHODS: MethodEntry[] = [
  {
    name: 'create',
    endpoint: '/v1/messages',
    httpMethod: 'post',
    summary: 'Create a Message',
    description:
      'Send a structured list of input messages with text and/or image content, and the model will generate the next message in the conversation.\n\nThe Messages API can be used for either single queries or stateless multi-turn conversations.\n\nLearn more about the Messages API in our [user guide](/en/docs/initial-setup)',
    stainlessPath: '(resource) messages > (method) create',
    qualified: 'client.messages.create',
    params: [
      'max_tokens: number;',
      "messages: { content: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; } | { source: { data: string; media_type: 'image/jpeg' | 'image/png' | 'image/gif' | 'image/webp'; type: 'base64'; } | { type: 'url'; url: string; }; type: 'image'; cache_control?: { type: 'ephemeral'; }; } | { id: string; input: object; name: string; type: 'tool_use'; cache_control?: { type: 'ephemeral'; }; } | { tool_use_id: string; type: 'tool_result'; cache_control?: { type: 'ephemeral'; }; content?: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; } | { source: { data: string; media_type: 'image/jpeg' | 'image/png' | 'image/gif' | 'image/webp'; type: 'base64'; } | { type: 'url'; url: string; }; type: 'image'; cache_control?: { type: 'ephemeral'; }; }[]; is_error?: boolean; } | { source: { data: string; media_type: 'application/pdf'; type: 'base64'; } | { data: string; media_type: 'text/plain'; type: 'text'; } | { content: string | { text: string; type: 'text'; cache_control?: object; citations?: object | object | object[]; } | { source: object | object; type: 'image'; cache_control?: object; }[]; type: 'content'; } | { type: 'url'; url: string; }; type: 'document'; cache_control?: { type: 'ephemeral'; }; citations?: { enabled?: boolean; }; context?: string; title?: string; } | { signature: string; thinking: string; type: 'thinking'; } | { data: string; type: 'redacted_thinking'; }[]; role: 'user' | 'assistant'; }[];",
      'model: string;',
      'metadata?: { user_id?: string; };',
      'stop_sequences?: string[];',
      'stream?: boolean;',
      "system?: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; }[];",
      'temperature?: number;',
      "thinking?: { budget_tokens: number; type: 'enabled'; } | { type: 'disabled'; };",
      "tool_choice?: { type: 'auto'; disable_parallel_tool_use?: boolean; } | { type: 'any'; disable_parallel_tool_use?: boolean; } | { name: string; type: 'tool'; disable_parallel_tool_use?: boolean; } | { type: 'none'; };",
      "tools?: { input_schema: { type: 'object'; properties?: object; }; name: string; cache_control?: { type: 'ephemeral'; }; description?: string; } | { name: 'bash'; type: 'bash_20250124'; cache_control?: { type: 'ephemeral'; }; } | { name: 'str_replace_editor'; type: 'text_editor_20250124'; cache_control?: { type: 'ephemeral'; }; }[];",
      'top_k?: number;',
      'top_p?: number;',
      'anthropic-beta?: string[];',
      'anthropic-version?: string;',
      'x-api-key?: string;',
    ],
    response:
      "{ id: string; content: { citations: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; text: string; type: 'text'; } | { id: string; input: object; name: string; type: 'tool_use'; } | { signature: string; thinking: string; type: 'thinking'; } | { data: string; type: 'redacted_thinking'; }[]; model: string; role: 'assistant'; stop_reason: 'end_turn' | 'max_tokens' | 'stop_sequence' | 'tool_use'; stop_sequence: string; type: 'message'; usage: { cache_creation_input_tokens: number; cache_read_input_tokens: number; input_tokens: number; output_tokens: number; }; }",
    markdown:
      "## create\n\n`client.messages.create(max_tokens: number, messages: { content: string | { text: string; type: 'text'; cache_control?: object; citations?: object | object | object[]; } | { source: object | object; type: 'image'; cache_control?: object; } | { id: string; input: object; name: string; type: 'tool_use'; cache_control?: object; } | { tool_use_id: string; type: 'tool_result'; cache_control?: object; content?: string | object | object[]; is_error?: boolean; } | { source: object | object | object | object; type: 'document'; cache_control?: object; citations?: object; context?: string; title?: string; } | { signature: string; thinking: string; type: 'thinking'; } | { data: string; type: 'redacted_thinking'; }[]; role: 'user' | 'assistant'; }[], model: string, metadata?: { user_id?: string; }, stop_sequences?: string[], stream?: boolean, system?: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; }[], temperature?: number, thinking?: { budget_tokens: number; type: 'enabled'; } | { type: 'disabled'; }, tool_choice?: { type: 'auto'; disable_parallel_tool_use?: boolean; } | { type: 'any'; disable_parallel_tool_use?: boolean; } | { name: string; type: 'tool'; disable_parallel_tool_use?: boolean; } | { type: 'none'; }, tools?: { input_schema: { type: 'object'; properties?: object; }; name: string; cache_control?: { type: 'ephemeral'; }; description?: string; } | { name: 'bash'; type: 'bash_20250124'; cache_control?: { type: 'ephemeral'; }; } | { name: 'str_replace_editor'; type: 'text_editor_20250124'; cache_control?: { type: 'ephemeral'; }; }[], top_k?: number, top_p?: number, anthropic-beta?: string[], anthropic-version?: string, x-api-key?: string): { id: string; content: object | object | object | object[]; model: string; role: 'assistant'; stop_reason: 'end_turn' | 'max_tokens' | 'stop_sequence' | 'tool_use'; stop_sequence: string; type: 'message'; usage: object; }`\n\n**post** `/v1/messages`\n\nSend a structured list of input messages with text and/or image content, and the model will generate the next message in the conversation.\n\nThe Messages API can be used for either single queries or stateless multi-turn conversations.\n\nLearn more about the Messages API in our [user guide](/en/docs/initial-setup)\n\n### Parameters\n\n- `max_tokens: number`\n  The maximum number of tokens to generate before stopping.\n\nNote that our models may stop _before_ reaching this maximum. This parameter only specifies the absolute maximum number of tokens to generate.\n\nDifferent models have different maximum values for this parameter.  See [models](https://docs.anthropic.com/en/docs/models-overview) for details.\n\n- `messages: { content: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; } | { source: { data: string; media_type: 'image/jpeg' | 'image/png' | 'image/gif' | 'image/webp'; type: 'base64'; } | { type: 'url'; url: string; }; type: 'image'; cache_control?: { type: 'ephemeral'; }; } | { id: string; input: object; name: string; type: 'tool_use'; cache_control?: { type: 'ephemeral'; }; } | { tool_use_id: string; type: 'tool_result'; cache_control?: { type: 'ephemeral'; }; content?: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; } | { source: { data: string; media_type: 'image/jpeg' | 'image/png' | 'image/gif' | 'image/webp'; type: 'base64'; } | { type: 'url'; url: string; }; type: 'image'; cache_control?: { type: 'ephemeral'; }; }[]; is_error?: boolean; } | { source: { data: string; media_type: 'application/pdf'; type: 'base64'; } | { data: string; media_type: 'text/plain'; type: 'text'; } | { content: string | { text: string; type: 'text'; cache_control?: object; citations?: object | object | object[]; } | { source: object | object; type: 'image'; cache_control?: object; }[]; type: 'content'; } | { type: 'url'; url: string; }; type: 'document'; cache_control?: { type: 'ephemeral'; }; citations?: { enabled?: boolean; }; context?: string; title?: string; } | { signature: string; thinking: string; type: 'thinking'; } | { data: string; type: 'redacted_thinking'; }[]; role: 'user' | 'assistant'; }[]`\n  Input messages.\n\nOur models are trained to operate on alternating `user` and `assistant` conversational turns. When creating a new `Message`, you specify the prior conversational turns with the `messages` parameter, and the model then generates the next `Message` in the conversation. Consecutive `user` or `assistant` turns in your request will be combined into a single turn.\n\nEach input message must be an object with a `role` and `content`. You can specify a single `user`-role message, or you can include multiple `user` and `assistant` messages.\n\nIf the final message uses the `assistant` role, the response content will continue immediately from the content in that message. This can be used to constrain part of the model's response.\n\nExample with a single `user` message:\n\n```json\n[{\"role\": \"user\", \"content\": \"Hello, Claude\"}]\n```\n\nExample with multiple conversational turns:\n\n```json\n[\n  {\"role\": \"user\", \"content\": \"Hello there.\"},\n  {\"role\": \"assistant\", \"content\": \"Hi, I'm Claude. How can I help you?\"},\n  {\"role\": \"user\", \"content\": \"Can you explain LLMs in plain English?\"},\n]\n```\n\nExample with a partially-filled response from Claude:\n\n```json\n[\n  {\"role\": \"user\", \"content\": \"What's the Greek name for Sun? (A) Sol (B) Helios (C) Sun\"},\n  {\"role\": \"assistant\", \"content\": \"The best answer is (\"},\n]\n```\n\nEach input message `content` may be either a single `string` or an array of content blocks, where each block has a specific `type`. Using a `string` for `content` is shorthand for an array of one content block of type `\"text\"`. The following input messages are equivalent:\n\n```json\n{\"role\": \"user\", \"content\": \"Hello, Claude\"}\n```\n\n```json\n{\"role\": \"user\", \"content\": [{\"type\": \"text\", \"text\": \"Hello, Claude\"}]}\n```\n\nStarting with Claude 3 models, you can also send image content blocks:\n\n```json\n{\"role\": \"user\", \"content\": [\n  {\n    \"type\": \"image\",\n    \"source\": {\n      \"type\": \"base64\",\n      \"media_type\": \"image/jpeg\",\n      \"data\": \"/9j/4AAQSkZJRg...\",\n    }\n  },\n  {\"type\": \"text\", \"text\": \"What is in this image?\"}\n]}\n```\n\nWe currently support the `base64` source type for images, and the `image/jpeg`, `image/png`, `image/gif`, and `image/webp` media types.\n\nSee [examples](https://docs.anthropic.com/en/api/messages-examples#vision) for more input examples.\n\nNote that if you want to include a [system prompt](https://docs.anthropic.com/en/docs/system-prompts), you can use the top-level `system` parameter — there is no `\"system\"` role for input messages in the Messages API.\n\n- `model: string`\n  The model that will complete your prompt.\n\nSee [models](https://docs.anthropic.com/en/docs/models-overview) for additional details and options.\n\n- `metadata?: { user_id?: string; }`\n  An object describing metadata about the request.\n  - `user_id?: string`\n    An external identifier for the user who is associated with the request.\n\nThis should be a uuid, hash value, or other opaque identifier. Anthropic may use this id to help detect abuse. Do not include any identifying information such as name, email address, or phone number.\n\n- `stop_sequences?: string[]`\n  Custom text sequences that will cause the model to stop generating.\n\nOur models will normally stop when they have naturally completed their turn, which will result in a response `stop_reason` of `\"end_turn\"`.\n\nIf you want the model to stop generating when it encounters custom strings of text, you can use the `stop_sequences` parameter. If the model encounters one of the custom sequences, the response `stop_reason` value will be `\"stop_sequence\"` and the response `stop_sequence` value will contain the matched stop sequence.\n\n- `stream?: boolean`\n  Whether to incrementally stream the response using server-sent events.\n\nSee [streaming](https://docs.anthropic.com/en/api/messages-streaming) for details.\n\n- `system?: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; }[]`\n  System prompt.\n\nA system prompt is a way of providing context and instructions to Claude, such as specifying a particular goal or role. See our [guide to system prompts](https://docs.anthropic.com/en/docs/system-prompts).\n\n- `temperature?: number`\n  Amount of randomness injected into the response.\n\nDefaults to `1.0`. Ranges from `0.0` to `1.0`. Use `temperature` closer to `0.0` for analytical / multiple choice, and closer to `1.0` for creative and generative tasks.\n\nNote that even with `temperature` of `0.0`, the results will not be fully deterministic.\n\n- `thinking?: { budget_tokens: number; type: 'enabled'; } | { type: 'disabled'; }`\n  Configuration for enabling Claude's extended thinking. \n\nWhen enabled, responses include `thinking` content blocks showing Claude's thinking process before the final answer. Requires a minimum budget of 1,024 tokens and counts towards your `max_tokens` limit.\n\nSee [extended thinking](https://docs.anthropic.com/en/docs/build-with-claude/extended-thinking) for details.\n\n- `tool_choice?: { type: 'auto'; disable_parallel_tool_use?: boolean; } | { type: 'any'; disable_parallel_tool_use?: boolean; } | { name: string; type: 'tool'; disable_parallel_tool_use?: boolean; } | { type: 'none'; }`\n  How the model should use the provided tools. The model can use a specific tool, any available tool, decide by itself, or not use tools at all.\n\n- `tools?: { input_schema: { type: 'object'; properties?: object; }; name: string; cache_control?: { type: 'ephemeral'; }; description?: string; } | { name: 'bash'; type: 'bash_20250124'; cache_control?: { type: 'ephemeral'; }; } | { name: 'str_replace_editor'; type: 'text_editor_20250124'; cache_control?: { type: 'ephemeral'; }; }[]`\n  Definitions of tools that the model may use.\n\nIf you include `tools` in your API request, the model may return `tool_use` content blocks that represent the model's use of those tools. You can then run those tools using the tool input generated by the model and then optionally return results back to the model using `tool_result` content blocks.\n\nEach tool definition includes:\n\n* `name`: Name of the tool.\n* `description`: Optional, but strongly-recommended description of the tool.\n* `input_schema`: [JSON schema](https://json-schema.org/draft/2020-12) for the tool `input` shape that the model will produce in `tool_use` output content blocks.\n\nFor example, if you defined `tools` as:\n\n```json\n[\n  {\n    \"name\": \"get_stock_price\",\n    \"description\": \"Get the current stock price for a given ticker symbol.\",\n    \"input_schema\": {\n      \"type\": \"object\",\n      \"properties\": {\n        \"ticker\": {\n          \"type\": \"string\",\n          \"description\": \"The stock ticker symbol, e.g. AAPL for Apple Inc.\"\n        }\n      },\n      \"required\": [\"ticker\"]\n    }\n  }\n]\n```\n\nAnd then asked the model \"What's the S&P 500 at today?\", the model might produce `tool_use` content blocks in the response like this:\n\n```json\n[\n  {\n    \"type\": \"tool_use\",\n    \"id\": \"toolu_01D7FLrfh4GYq7yT1ULFeyMV\",\n    \"name\": \"get_stock_price\",\n    \"input\": { \"ticker\": \"^GSPC\" }\n  }\n]\n```\n\nYou might then run your `get_stock_price` tool with `{\"ticker\": \"^GSPC\"}` as an input, and return the following back to the model in a subsequent `user` message:\n\n```json\n[\n  {\n    \"type\": \"tool_result\",\n    \"tool_use_id\": \"toolu_01D7FLrfh4GYq7yT1ULFeyMV\",\n    \"content\": \"259.75 USD\"\n  }\n]\n```\n\nTools can be used for workflows that include running client-side tools and functions, or more generally whenever you want the model to produce a particular JSON structure of output.\n\nSee our [guide](https://docs.anthropic.com/en/docs/tool-use) for more details.\n\n- `top_k?: number`\n  Only sample from the top K options for each subsequent token.\n\nUsed to remove \"long tail\" low probability responses. [Learn more technical details here](https://towardsdatascience.com/how-to-sample-from-language-models-682bceb97277).\n\nRecommended for advanced use cases only. You usually only need to use `temperature`.\n\n- `top_p?: number`\n  Use nucleus sampling.\n\nIn nucleus sampling, we compute the cumulative distribution over all the options for each subsequent token in decreasing probability order and cut it off once it reaches a particular probability specified by `top_p`. You should either alter `temperature` or `top_p`, but not both.\n\nRecommended for advanced use cases only. You usually only need to use `temperature`.\n\n- `anthropic-beta?: string[]`\n  Optional header to specify the beta version(s) you want to use.\n\nTo use multiple betas, use a comma separated list like `beta1,beta2` or specify the header multiple times for each beta.\n\n- `anthropic-version?: string`\n  The version of the Anthropic API you want to use.\n\nRead more about versioning and our version history [here](https://docs.anthropic.com/en/api/versioning).\n\n- `x-api-key?: string`\n  Your unique API key for authentication.\n\nThis key is required in the header of all API requests, to authenticate your account and access Anthropic's services. Get your API key through the [Console](https://console.anthropic.com/settings/keys). Each key is scoped to a Workspace.\n\n### Returns\n\n- `{ id: string; content: { citations: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; text: string; type: 'text'; } | { id: string; input: object; name: string; type: 'tool_use'; } | { signature: string; thinking: string; type: 'thinking'; } | { data: string; type: 'redacted_thinking'; }[]; model: string; role: 'assistant'; stop_reason: 'end_turn' | 'max_tokens' | 'stop_sequence' | 'tool_use'; stop_sequence: string; type: 'message'; usage: { cache_creation_input_tokens: number; cache_read_input_tokens: number; input_tokens: number; output_tokens: number; }; }`\n\n  - `id: string`\n  - `content: { citations: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; text: string; type: 'text'; } | { id: string; input: object; name: string; type: 'tool_use'; } | { signature: string; thinking: string; type: 'thinking'; } | { data: string; type: 'redacted_thinking'; }[]`\n  - `model: string`\n  - `role: 'assistant'`\n  - `stop_reason: 'end_turn' | 'max_tokens' | 'stop_sequence' | 'tool_use'`\n  - `stop_sequence: string`\n  - `type: 'message'`\n  - `usage: { cache_creation_input_tokens: number; cache_read_input_tokens: number; input_tokens: number; output_tokens: number; }`\n\n### Example\n\n```typescript\nimport Sam from 'sam';\n\nconst client = new Sam();\n\nconst message = await client.messages.create({\n  max_tokens: 1024,\n  messages: [{ content: 'Hello, world', role: 'user' }],\n  model: 'claude-3-7-sonnet-20250219',\n});\n\nconsole.log(message);\n```",
    perLanguage: {
      go: {
        method: 'client.Messages.New',
        example:
          'package main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/DefinitelyATestOrg/sam-go"\n\t"github.com/DefinitelyATestOrg/sam-go/option"\n\t"github.com/DefinitelyATestOrg/sam-go/shared"\n)\n\nfunc main() {\n\tclient := sam.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\tmessage, err := client.Messages.New(context.TODO(), sam.MessageNewParams{\n\t\tMaxTokens: sam.F(int64(1024)),\n\t\tMessages: sam.F([]sam.MessageNewParamsMessage{{\n\t\t\tContent: sam.F[sam.MessageNewParamsMessagesContentUnion](shared.UnionString("Hello, world")),\n\t\t\tRole:    sam.F(sam.MessageNewParamsMessagesRoleUser),\n\t\t}}),\n\t\tModel: sam.F("claude-3-7-sonnet-20250219"),\n\t})\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", message.ID)\n}\n',
      },
      http: {
        example:
          'curl /api/v3/v1/messages \\\n    -H \'Content-Type: application/json\' \\\n    -d \'{\n          "max_tokens": 1024,\n          "messages": [\n            {\n              "content": "Hello, world",\n              "role": "user"\n            }\n          ],\n          "model": "claude-3-7-sonnet-20250219"\n        }\'',
      },
      java: {
        method: 'messages().create',
        example:
          'package me.elborai.api.example;\n\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\nimport me.elborai.api.models.messages.MessageCreateParams;\nimport me.elborai.api.models.messages.MessageCreateResponse;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        SamClient client = SamOkHttpClient.fromEnv();\n\n        MessageCreateParams params = MessageCreateParams.builder()\n            .maxTokens(1024L)\n            .addMessage(MessageCreateParams.Message.builder()\n                .content("Hello, world")\n                .role(MessageCreateParams.Message.Role.USER)\n                .build())\n            .model("claude-3-7-sonnet-20250219")\n            .build();\n        MessageCreateResponse message = client.messages().create(params);\n    }\n}',
      },
      kotlin: {
        method: 'messages().create',
        example:
          'package me.elborai.api.example\n\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\nimport me.elborai.api.models.messages.MessageCreateParams\nimport me.elborai.api.models.messages.MessageCreateResponse\n\nfun main() {\n    val client: SamClient = SamOkHttpClient.fromEnv()\n\n    val params: MessageCreateParams = MessageCreateParams.builder()\n        .maxTokens(1024L)\n        .addMessage(MessageCreateParams.Message.builder()\n            .content("Hello, world")\n            .role(MessageCreateParams.Message.Role.USER)\n            .build())\n        .model("claude-3-7-sonnet-20250219")\n        .build()\n    val message: MessageCreateResponse = client.messages().create(params)\n}',
      },
      python: {
        method: 'messages.create',
        example:
          'from sam import Sam\n\nclient = Sam()\nmessage = client.messages.create(\n    max_tokens=1024,\n    messages=[{\n        "content": "Hello, world",\n        "role": "user",\n    }],\n    model="claude-3-7-sonnet-20250219",\n)\nprint(message.id)',
      },
      ruby: {
        method: 'messages.create',
        example:
          'require "sam"\n\nsam = Sam::Client.new(api_key: "My API Key")\n\nmessage = sam.messages.create(\n  max_tokens: 1024,\n  messages: [{content: "Hello, world", role: :user}],\n  model: "claude-3-7-sonnet-20250219"\n)\n\nputs(message)',
      },
      typescript: {
        method: 'client.messages.create',
        example:
          "import Sam from 'sam';\n\nconst client = new Sam();\n\nconst message = await client.messages.create({\n  max_tokens: 1024,\n  messages: [{ content: 'Hello, world', role: 'user' }],\n  model: 'claude-3-7-sonnet-20250219',\n});\n\nconsole.log(message.id);",
      },
    },
  },
  {
    name: 'count_tokens',
    endpoint: '/v1/messages/count_tokens',
    httpMethod: 'post',
    summary: 'Count tokens in a Message',
    description:
      'Count the number of tokens in a Message.\n\nThe Token Count API can be used to count the number of tokens in a Message, including tools, images, and documents, without creating it.\n\nLearn more about token counting in our [user guide](/en/docs/build-with-claude/token-counting)',
    stainlessPath: '(resource) messages > (method) count_tokens',
    qualified: 'client.messages.countTokens',
    params: [
      "messages: { content: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; } | { source: { data: string; media_type: 'image/jpeg' | 'image/png' | 'image/gif' | 'image/webp'; type: 'base64'; } | { type: 'url'; url: string; }; type: 'image'; cache_control?: { type: 'ephemeral'; }; } | { id: string; input: object; name: string; type: 'tool_use'; cache_control?: { type: 'ephemeral'; }; } | { tool_use_id: string; type: 'tool_result'; cache_control?: { type: 'ephemeral'; }; content?: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; } | { source: { data: string; media_type: 'image/jpeg' | 'image/png' | 'image/gif' | 'image/webp'; type: 'base64'; } | { type: 'url'; url: string; }; type: 'image'; cache_control?: { type: 'ephemeral'; }; }[]; is_error?: boolean; } | { source: { data: string; media_type: 'application/pdf'; type: 'base64'; } | { data: string; media_type: 'text/plain'; type: 'text'; } | { content: string | { text: string; type: 'text'; cache_control?: object; citations?: object | object | object[]; } | { source: object | object; type: 'image'; cache_control?: object; }[]; type: 'content'; } | { type: 'url'; url: string; }; type: 'document'; cache_control?: { type: 'ephemeral'; }; citations?: { enabled?: boolean; }; context?: string; title?: string; } | { signature: string; thinking: string; type: 'thinking'; } | { data: string; type: 'redacted_thinking'; }[]; role: 'user' | 'assistant'; }[];",
      'model: string;',
      "system?: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; }[];",
      "thinking?: { budget_tokens: number; type: 'enabled'; } | { type: 'disabled'; };",
      "tool_choice?: { type: 'auto'; disable_parallel_tool_use?: boolean; } | { type: 'any'; disable_parallel_tool_use?: boolean; } | { name: string; type: 'tool'; disable_parallel_tool_use?: boolean; } | { type: 'none'; };",
      "tools?: { input_schema: { type: 'object'; properties?: object; }; name: string; cache_control?: { type: 'ephemeral'; }; description?: string; } | { name: 'bash'; type: 'bash_20250124'; cache_control?: { type: 'ephemeral'; }; } | { name: 'str_replace_editor'; type: 'text_editor_20250124'; cache_control?: { type: 'ephemeral'; }; }[];",
      'anthropic-beta?: string[];',
      'anthropic-version?: string;',
      'x-api-key?: string;',
    ],
    response: '{ input_tokens: number; }',
    markdown:
      "## count_tokens\n\n`client.messages.countTokens(messages: { content: string | { text: string; type: 'text'; cache_control?: object; citations?: object | object | object[]; } | { source: object | object; type: 'image'; cache_control?: object; } | { id: string; input: object; name: string; type: 'tool_use'; cache_control?: object; } | { tool_use_id: string; type: 'tool_result'; cache_control?: object; content?: string | object | object[]; is_error?: boolean; } | { source: object | object | object | object; type: 'document'; cache_control?: object; citations?: object; context?: string; title?: string; } | { signature: string; thinking: string; type: 'thinking'; } | { data: string; type: 'redacted_thinking'; }[]; role: 'user' | 'assistant'; }[], model: string, system?: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; }[], thinking?: { budget_tokens: number; type: 'enabled'; } | { type: 'disabled'; }, tool_choice?: { type: 'auto'; disable_parallel_tool_use?: boolean; } | { type: 'any'; disable_parallel_tool_use?: boolean; } | { name: string; type: 'tool'; disable_parallel_tool_use?: boolean; } | { type: 'none'; }, tools?: { input_schema: { type: 'object'; properties?: object; }; name: string; cache_control?: { type: 'ephemeral'; }; description?: string; } | { name: 'bash'; type: 'bash_20250124'; cache_control?: { type: 'ephemeral'; }; } | { name: 'str_replace_editor'; type: 'text_editor_20250124'; cache_control?: { type: 'ephemeral'; }; }[], anthropic-beta?: string[], anthropic-version?: string, x-api-key?: string): { input_tokens: number; }`\n\n**post** `/v1/messages/count_tokens`\n\nCount the number of tokens in a Message.\n\nThe Token Count API can be used to count the number of tokens in a Message, including tools, images, and documents, without creating it.\n\nLearn more about token counting in our [user guide](/en/docs/build-with-claude/token-counting)\n\n### Parameters\n\n- `messages: { content: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; } | { source: { data: string; media_type: 'image/jpeg' | 'image/png' | 'image/gif' | 'image/webp'; type: 'base64'; } | { type: 'url'; url: string; }; type: 'image'; cache_control?: { type: 'ephemeral'; }; } | { id: string; input: object; name: string; type: 'tool_use'; cache_control?: { type: 'ephemeral'; }; } | { tool_use_id: string; type: 'tool_result'; cache_control?: { type: 'ephemeral'; }; content?: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; } | { source: { data: string; media_type: 'image/jpeg' | 'image/png' | 'image/gif' | 'image/webp'; type: 'base64'; } | { type: 'url'; url: string; }; type: 'image'; cache_control?: { type: 'ephemeral'; }; }[]; is_error?: boolean; } | { source: { data: string; media_type: 'application/pdf'; type: 'base64'; } | { data: string; media_type: 'text/plain'; type: 'text'; } | { content: string | { text: string; type: 'text'; cache_control?: object; citations?: object | object | object[]; } | { source: object | object; type: 'image'; cache_control?: object; }[]; type: 'content'; } | { type: 'url'; url: string; }; type: 'document'; cache_control?: { type: 'ephemeral'; }; citations?: { enabled?: boolean; }; context?: string; title?: string; } | { signature: string; thinking: string; type: 'thinking'; } | { data: string; type: 'redacted_thinking'; }[]; role: 'user' | 'assistant'; }[]`\n  Input messages.\n\nOur models are trained to operate on alternating `user` and `assistant` conversational turns. When creating a new `Message`, you specify the prior conversational turns with the `messages` parameter, and the model then generates the next `Message` in the conversation. Consecutive `user` or `assistant` turns in your request will be combined into a single turn.\n\nEach input message must be an object with a `role` and `content`. You can specify a single `user`-role message, or you can include multiple `user` and `assistant` messages.\n\nIf the final message uses the `assistant` role, the response content will continue immediately from the content in that message. This can be used to constrain part of the model's response.\n\nExample with a single `user` message:\n\n```json\n[{\"role\": \"user\", \"content\": \"Hello, Claude\"}]\n```\n\nExample with multiple conversational turns:\n\n```json\n[\n  {\"role\": \"user\", \"content\": \"Hello there.\"},\n  {\"role\": \"assistant\", \"content\": \"Hi, I'm Claude. How can I help you?\"},\n  {\"role\": \"user\", \"content\": \"Can you explain LLMs in plain English?\"},\n]\n```\n\nExample with a partially-filled response from Claude:\n\n```json\n[\n  {\"role\": \"user\", \"content\": \"What's the Greek name for Sun? (A) Sol (B) Helios (C) Sun\"},\n  {\"role\": \"assistant\", \"content\": \"The best answer is (\"},\n]\n```\n\nEach input message `content` may be either a single `string` or an array of content blocks, where each block has a specific `type`. Using a `string` for `content` is shorthand for an array of one content block of type `\"text\"`. The following input messages are equivalent:\n\n```json\n{\"role\": \"user\", \"content\": \"Hello, Claude\"}\n```\n\n```json\n{\"role\": \"user\", \"content\": [{\"type\": \"text\", \"text\": \"Hello, Claude\"}]}\n```\n\nStarting with Claude 3 models, you can also send image content blocks:\n\n```json\n{\"role\": \"user\", \"content\": [\n  {\n    \"type\": \"image\",\n    \"source\": {\n      \"type\": \"base64\",\n      \"media_type\": \"image/jpeg\",\n      \"data\": \"/9j/4AAQSkZJRg...\",\n    }\n  },\n  {\"type\": \"text\", \"text\": \"What is in this image?\"}\n]}\n```\n\nWe currently support the `base64` source type for images, and the `image/jpeg`, `image/png`, `image/gif`, and `image/webp` media types.\n\nSee [examples](https://docs.anthropic.com/en/api/messages-examples#vision) for more input examples.\n\nNote that if you want to include a [system prompt](https://docs.anthropic.com/en/docs/system-prompts), you can use the top-level `system` parameter — there is no `\"system\"` role for input messages in the Messages API.\n\n- `model: string`\n  The model that will complete your prompt.\n\nSee [models](https://docs.anthropic.com/en/docs/models-overview) for additional details and options.\n\n- `system?: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; }[]`\n  System prompt.\n\nA system prompt is a way of providing context and instructions to Claude, such as specifying a particular goal or role. See our [guide to system prompts](https://docs.anthropic.com/en/docs/system-prompts).\n\n- `thinking?: { budget_tokens: number; type: 'enabled'; } | { type: 'disabled'; }`\n  Configuration for enabling Claude's extended thinking. \n\nWhen enabled, responses include `thinking` content blocks showing Claude's thinking process before the final answer. Requires a minimum budget of 1,024 tokens and counts towards your `max_tokens` limit.\n\nSee [extended thinking](https://docs.anthropic.com/en/docs/build-with-claude/extended-thinking) for details.\n\n- `tool_choice?: { type: 'auto'; disable_parallel_tool_use?: boolean; } | { type: 'any'; disable_parallel_tool_use?: boolean; } | { name: string; type: 'tool'; disable_parallel_tool_use?: boolean; } | { type: 'none'; }`\n  How the model should use the provided tools. The model can use a specific tool, any available tool, decide by itself, or not use tools at all.\n\n- `tools?: { input_schema: { type: 'object'; properties?: object; }; name: string; cache_control?: { type: 'ephemeral'; }; description?: string; } | { name: 'bash'; type: 'bash_20250124'; cache_control?: { type: 'ephemeral'; }; } | { name: 'str_replace_editor'; type: 'text_editor_20250124'; cache_control?: { type: 'ephemeral'; }; }[]`\n  Definitions of tools that the model may use.\n\nIf you include `tools` in your API request, the model may return `tool_use` content blocks that represent the model's use of those tools. You can then run those tools using the tool input generated by the model and then optionally return results back to the model using `tool_result` content blocks.\n\nEach tool definition includes:\n\n* `name`: Name of the tool.\n* `description`: Optional, but strongly-recommended description of the tool.\n* `input_schema`: [JSON schema](https://json-schema.org/draft/2020-12) for the tool `input` shape that the model will produce in `tool_use` output content blocks.\n\nFor example, if you defined `tools` as:\n\n```json\n[\n  {\n    \"name\": \"get_stock_price\",\n    \"description\": \"Get the current stock price for a given ticker symbol.\",\n    \"input_schema\": {\n      \"type\": \"object\",\n      \"properties\": {\n        \"ticker\": {\n          \"type\": \"string\",\n          \"description\": \"The stock ticker symbol, e.g. AAPL for Apple Inc.\"\n        }\n      },\n      \"required\": [\"ticker\"]\n    }\n  }\n]\n```\n\nAnd then asked the model \"What's the S&P 500 at today?\", the model might produce `tool_use` content blocks in the response like this:\n\n```json\n[\n  {\n    \"type\": \"tool_use\",\n    \"id\": \"toolu_01D7FLrfh4GYq7yT1ULFeyMV\",\n    \"name\": \"get_stock_price\",\n    \"input\": { \"ticker\": \"^GSPC\" }\n  }\n]\n```\n\nYou might then run your `get_stock_price` tool with `{\"ticker\": \"^GSPC\"}` as an input, and return the following back to the model in a subsequent `user` message:\n\n```json\n[\n  {\n    \"type\": \"tool_result\",\n    \"tool_use_id\": \"toolu_01D7FLrfh4GYq7yT1ULFeyMV\",\n    \"content\": \"259.75 USD\"\n  }\n]\n```\n\nTools can be used for workflows that include running client-side tools and functions, or more generally whenever you want the model to produce a particular JSON structure of output.\n\nSee our [guide](https://docs.anthropic.com/en/docs/tool-use) for more details.\n\n- `anthropic-beta?: string[]`\n  Optional header to specify the beta version(s) you want to use.\n\nTo use multiple betas, use a comma separated list like `beta1,beta2` or specify the header multiple times for each beta.\n\n- `anthropic-version?: string`\n  The version of the Anthropic API you want to use.\n\nRead more about versioning and our version history [here](https://docs.anthropic.com/en/api/versioning).\n\n- `x-api-key?: string`\n  Your unique API key for authentication.\n\nThis key is required in the header of all API requests, to authenticate your account and access Anthropic's services. Get your API key through the [Console](https://console.anthropic.com/settings/keys). Each key is scoped to a Workspace.\n\n### Returns\n\n- `{ input_tokens: number; }`\n\n  - `input_tokens: number`\n\n### Example\n\n```typescript\nimport Sam from 'sam';\n\nconst client = new Sam();\n\nconst response = await client.messages.countTokens({ messages: [{ content: 'string', role: 'user' }], model: 'claude-3-7-sonnet-20250219' });\n\nconsole.log(response);\n```",
    perLanguage: {
      go: {
        method: 'client.Messages.CountTokens',
        example:
          'package main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/DefinitelyATestOrg/sam-go"\n\t"github.com/DefinitelyATestOrg/sam-go/option"\n\t"github.com/DefinitelyATestOrg/sam-go/shared"\n)\n\nfunc main() {\n\tclient := sam.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\tresponse, err := client.Messages.CountTokens(context.TODO(), sam.MessageCountTokensParams{\n\t\tMessages: sam.F([]sam.MessageCountTokensParamsMessage{{\n\t\t\tContent: sam.F[sam.MessageCountTokensParamsMessagesContentUnion](shared.UnionString("string")),\n\t\t\tRole:    sam.F(sam.MessageCountTokensParamsMessagesRoleUser),\n\t\t}}),\n\t\tModel: sam.F("claude-3-7-sonnet-20250219"),\n\t})\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", response.InputTokens)\n}\n',
      },
      http: {
        example:
          'curl /api/v3/v1/messages/count_tokens \\\n    -H \'Content-Type: application/json\' \\\n    -d \'{\n          "messages": [\n            {\n              "content": "string",\n              "role": "user"\n            }\n          ],\n          "model": "claude-3-7-sonnet-20250219"\n        }\'',
      },
      java: {
        method: 'messages().countTokens',
        example:
          'package me.elborai.api.example;\n\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\nimport me.elborai.api.models.messages.MessageCountTokensParams;\nimport me.elborai.api.models.messages.MessageCountTokensResponse;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        SamClient client = SamOkHttpClient.fromEnv();\n\n        MessageCountTokensParams params = MessageCountTokensParams.builder()\n            .addMessage(MessageCountTokensParams.Message.builder()\n                .content("Hello, world")\n                .role(MessageCountTokensParams.Message.Role.USER)\n                .build())\n            .model("claude-3-7-sonnet-20250219")\n            .build();\n        MessageCountTokensResponse response = client.messages().countTokens(params);\n    }\n}',
      },
      kotlin: {
        method: 'messages().countTokens',
        example:
          'package me.elborai.api.example\n\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\nimport me.elborai.api.models.messages.MessageCountTokensParams\nimport me.elborai.api.models.messages.MessageCountTokensResponse\n\nfun main() {\n    val client: SamClient = SamOkHttpClient.fromEnv()\n\n    val params: MessageCountTokensParams = MessageCountTokensParams.builder()\n        .addMessage(MessageCountTokensParams.Message.builder()\n            .content("Hello, world")\n            .role(MessageCountTokensParams.Message.Role.USER)\n            .build())\n        .model("claude-3-7-sonnet-20250219")\n        .build()\n    val response: MessageCountTokensResponse = client.messages().countTokens(params)\n}',
      },
      python: {
        method: 'messages.count_tokens',
        example:
          'from sam import Sam\n\nclient = Sam()\nresponse = client.messages.count_tokens(\n    messages=[{\n        "content": "string",\n        "role": "user",\n    }],\n    model="claude-3-7-sonnet-20250219",\n)\nprint(response.input_tokens)',
      },
      ruby: {
        method: 'messages.count_tokens',
        example:
          'require "sam"\n\nsam = Sam::Client.new(api_key: "My API Key")\n\nresponse = sam.messages.count_tokens(\n  messages: [{content: "string", role: :user}],\n  model: "claude-3-7-sonnet-20250219"\n)\n\nputs(response)',
      },
      typescript: {
        method: 'client.messages.countTokens',
        example:
          "import Sam from 'sam';\n\nconst client = new Sam();\n\nconst response = await client.messages.countTokens({\n  messages: [{ content: 'string', role: 'user' }],\n  model: 'claude-3-7-sonnet-20250219',\n});\n\nconsole.log(response.input_tokens);",
      },
    },
  },
  {
    name: 'count_tokens_beta',
    endpoint: '/v1/messages/count_tokens?beta=true',
    httpMethod: 'post',
    summary: 'Count tokens in a Message',
    description:
      'Count the number of tokens in a Message.\n\nThe Token Count API can be used to count the number of tokens in a Message, including tools, images, and documents, without creating it.\n\nLearn more about token counting in our [user guide](/en/docs/build-with-claude/token-counting)',
    stainlessPath: '(resource) messages > (method) count_tokens_beta',
    qualified: 'client.messages.countTokensBeta',
    params: [
      "messages: { content: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; } | { source: { data: string; media_type: 'image/jpeg' | 'image/png' | 'image/gif' | 'image/webp'; type: 'base64'; } | { type: 'url'; url: string; }; type: 'image'; cache_control?: { type: 'ephemeral'; }; } | { id: string; input: object; name: string; type: 'tool_use'; cache_control?: { type: 'ephemeral'; }; } | { tool_use_id: string; type: 'tool_result'; cache_control?: { type: 'ephemeral'; }; content?: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; } | { source: { data: string; media_type: 'image/jpeg' | 'image/png' | 'image/gif' | 'image/webp'; type: 'base64'; } | { type: 'url'; url: string; }; type: 'image'; cache_control?: { type: 'ephemeral'; }; }[]; is_error?: boolean; } | { source: { data: string; media_type: 'application/pdf'; type: 'base64'; } | { data: string; media_type: 'text/plain'; type: 'text'; } | { content: string | { text: string; type: 'text'; cache_control?: object; citations?: object | object | object[]; } | { source: object | object; type: 'image'; cache_control?: object; }[]; type: 'content'; } | { type: 'url'; url: string; }; type: 'document'; cache_control?: { type: 'ephemeral'; }; citations?: { enabled?: boolean; }; context?: string; title?: string; } | { signature: string; thinking: string; type: 'thinking'; } | { data: string; type: 'redacted_thinking'; }[]; role: 'user' | 'assistant'; }[];",
      'model: string;',
      "system?: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; }[];",
      "thinking?: { budget_tokens: number; type: 'enabled'; } | { type: 'disabled'; };",
      "tool_choice?: { type: 'auto'; disable_parallel_tool_use?: boolean; } | { type: 'any'; disable_parallel_tool_use?: boolean; } | { name: string; type: 'tool'; disable_parallel_tool_use?: boolean; } | { type: 'none'; };",
      "tools?: { input_schema: { type: 'object'; properties?: object; }; name: string; cache_control?: { type: 'ephemeral'; }; description?: string; type?: 'custom'; } | { display_height_px: number; display_width_px: number; name: 'computer'; type: 'computer_20241022'; cache_control?: { type: 'ephemeral'; }; display_number?: number; } | { name: 'bash'; type: 'bash_20241022'; cache_control?: { type: 'ephemeral'; }; } | { name: 'str_replace_editor'; type: 'text_editor_20241022'; cache_control?: { type: 'ephemeral'; }; } | { display_height_px: number; display_width_px: number; name: 'computer'; type: 'computer_20250124'; cache_control?: { type: 'ephemeral'; }; display_number?: number; } | { name: 'bash'; type: 'bash_20250124'; cache_control?: { type: 'ephemeral'; }; } | { name: 'str_replace_editor'; type: 'text_editor_20250124'; cache_control?: { type: 'ephemeral'; }; }[];",
      'anthropic-beta?: string[];',
      'anthropic-version?: string;',
      'x-api-key?: string;',
    ],
    response: '{ input_tokens: number; }',
    markdown:
      "## count_tokens_beta\n\n`client.messages.countTokensBeta(messages: { content: string | { text: string; type: 'text'; cache_control?: object; citations?: object | object | object[]; } | { source: object | object; type: 'image'; cache_control?: object; } | { id: string; input: object; name: string; type: 'tool_use'; cache_control?: object; } | { tool_use_id: string; type: 'tool_result'; cache_control?: object; content?: string | object | object[]; is_error?: boolean; } | { source: object | object | object | object; type: 'document'; cache_control?: object; citations?: object; context?: string; title?: string; } | { signature: string; thinking: string; type: 'thinking'; } | { data: string; type: 'redacted_thinking'; }[]; role: 'user' | 'assistant'; }[], model: string, system?: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; }[], thinking?: { budget_tokens: number; type: 'enabled'; } | { type: 'disabled'; }, tool_choice?: { type: 'auto'; disable_parallel_tool_use?: boolean; } | { type: 'any'; disable_parallel_tool_use?: boolean; } | { name: string; type: 'tool'; disable_parallel_tool_use?: boolean; } | { type: 'none'; }, tools?: { input_schema: { type: 'object'; properties?: object; }; name: string; cache_control?: { type: 'ephemeral'; }; description?: string; type?: 'custom'; } | { display_height_px: number; display_width_px: number; name: 'computer'; type: 'computer_20241022'; cache_control?: { type: 'ephemeral'; }; display_number?: number; } | { name: 'bash'; type: 'bash_20241022'; cache_control?: { type: 'ephemeral'; }; } | { name: 'str_replace_editor'; type: 'text_editor_20241022'; cache_control?: { type: 'ephemeral'; }; } | { display_height_px: number; display_width_px: number; name: 'computer'; type: 'computer_20250124'; cache_control?: { type: 'ephemeral'; }; display_number?: number; } | { name: 'bash'; type: 'bash_20250124'; cache_control?: { type: 'ephemeral'; }; } | { name: 'str_replace_editor'; type: 'text_editor_20250124'; cache_control?: { type: 'ephemeral'; }; }[], anthropic-beta?: string[], anthropic-version?: string, x-api-key?: string): { input_tokens: number; }`\n\n**post** `/v1/messages/count_tokens?beta=true`\n\nCount the number of tokens in a Message.\n\nThe Token Count API can be used to count the number of tokens in a Message, including tools, images, and documents, without creating it.\n\nLearn more about token counting in our [user guide](/en/docs/build-with-claude/token-counting)\n\n### Parameters\n\n- `messages: { content: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; } | { source: { data: string; media_type: 'image/jpeg' | 'image/png' | 'image/gif' | 'image/webp'; type: 'base64'; } | { type: 'url'; url: string; }; type: 'image'; cache_control?: { type: 'ephemeral'; }; } | { id: string; input: object; name: string; type: 'tool_use'; cache_control?: { type: 'ephemeral'; }; } | { tool_use_id: string; type: 'tool_result'; cache_control?: { type: 'ephemeral'; }; content?: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; } | { source: { data: string; media_type: 'image/jpeg' | 'image/png' | 'image/gif' | 'image/webp'; type: 'base64'; } | { type: 'url'; url: string; }; type: 'image'; cache_control?: { type: 'ephemeral'; }; }[]; is_error?: boolean; } | { source: { data: string; media_type: 'application/pdf'; type: 'base64'; } | { data: string; media_type: 'text/plain'; type: 'text'; } | { content: string | { text: string; type: 'text'; cache_control?: object; citations?: object | object | object[]; } | { source: object | object; type: 'image'; cache_control?: object; }[]; type: 'content'; } | { type: 'url'; url: string; }; type: 'document'; cache_control?: { type: 'ephemeral'; }; citations?: { enabled?: boolean; }; context?: string; title?: string; } | { signature: string; thinking: string; type: 'thinking'; } | { data: string; type: 'redacted_thinking'; }[]; role: 'user' | 'assistant'; }[]`\n  Input messages.\n\nOur models are trained to operate on alternating `user` and `assistant` conversational turns. When creating a new `Message`, you specify the prior conversational turns with the `messages` parameter, and the model then generates the next `Message` in the conversation. Consecutive `user` or `assistant` turns in your request will be combined into a single turn.\n\nEach input message must be an object with a `role` and `content`. You can specify a single `user`-role message, or you can include multiple `user` and `assistant` messages.\n\nIf the final message uses the `assistant` role, the response content will continue immediately from the content in that message. This can be used to constrain part of the model's response.\n\nExample with a single `user` message:\n\n```json\n[{\"role\": \"user\", \"content\": \"Hello, Claude\"}]\n```\n\nExample with multiple conversational turns:\n\n```json\n[\n  {\"role\": \"user\", \"content\": \"Hello there.\"},\n  {\"role\": \"assistant\", \"content\": \"Hi, I'm Claude. How can I help you?\"},\n  {\"role\": \"user\", \"content\": \"Can you explain LLMs in plain English?\"},\n]\n```\n\nExample with a partially-filled response from Claude:\n\n```json\n[\n  {\"role\": \"user\", \"content\": \"What's the Greek name for Sun? (A) Sol (B) Helios (C) Sun\"},\n  {\"role\": \"assistant\", \"content\": \"The best answer is (\"},\n]\n```\n\nEach input message `content` may be either a single `string` or an array of content blocks, where each block has a specific `type`. Using a `string` for `content` is shorthand for an array of one content block of type `\"text\"`. The following input messages are equivalent:\n\n```json\n{\"role\": \"user\", \"content\": \"Hello, Claude\"}\n```\n\n```json\n{\"role\": \"user\", \"content\": [{\"type\": \"text\", \"text\": \"Hello, Claude\"}]}\n```\n\nStarting with Claude 3 models, you can also send image content blocks:\n\n```json\n{\"role\": \"user\", \"content\": [\n  {\n    \"type\": \"image\",\n    \"source\": {\n      \"type\": \"base64\",\n      \"media_type\": \"image/jpeg\",\n      \"data\": \"/9j/4AAQSkZJRg...\",\n    }\n  },\n  {\"type\": \"text\", \"text\": \"What is in this image?\"}\n]}\n```\n\nWe currently support the `base64` source type for images, and the `image/jpeg`, `image/png`, `image/gif`, and `image/webp` media types.\n\nSee [examples](https://docs.anthropic.com/en/api/messages-examples#vision) for more input examples.\n\nNote that if you want to include a [system prompt](https://docs.anthropic.com/en/docs/system-prompts), you can use the top-level `system` parameter — there is no `\"system\"` role for input messages in the Messages API.\n\n- `model: string`\n  The model that will complete your prompt.\n\nSee [models](https://docs.anthropic.com/en/docs/models-overview) for additional details and options.\n\n- `system?: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; }[]`\n  System prompt.\n\nA system prompt is a way of providing context and instructions to Claude, such as specifying a particular goal or role. See our [guide to system prompts](https://docs.anthropic.com/en/docs/system-prompts).\n\n- `thinking?: { budget_tokens: number; type: 'enabled'; } | { type: 'disabled'; }`\n  Configuration for enabling Claude's extended thinking. \n\nWhen enabled, responses include `thinking` content blocks showing Claude's thinking process before the final answer. Requires a minimum budget of 1,024 tokens and counts towards your `max_tokens` limit.\n\nSee [extended thinking](https://docs.anthropic.com/en/docs/build-with-claude/extended-thinking) for details.\n\n- `tool_choice?: { type: 'auto'; disable_parallel_tool_use?: boolean; } | { type: 'any'; disable_parallel_tool_use?: boolean; } | { name: string; type: 'tool'; disable_parallel_tool_use?: boolean; } | { type: 'none'; }`\n  How the model should use the provided tools. The model can use a specific tool, any available tool, decide by itself, or not use tools at all.\n\n- `tools?: { input_schema: { type: 'object'; properties?: object; }; name: string; cache_control?: { type: 'ephemeral'; }; description?: string; type?: 'custom'; } | { display_height_px: number; display_width_px: number; name: 'computer'; type: 'computer_20241022'; cache_control?: { type: 'ephemeral'; }; display_number?: number; } | { name: 'bash'; type: 'bash_20241022'; cache_control?: { type: 'ephemeral'; }; } | { name: 'str_replace_editor'; type: 'text_editor_20241022'; cache_control?: { type: 'ephemeral'; }; } | { display_height_px: number; display_width_px: number; name: 'computer'; type: 'computer_20250124'; cache_control?: { type: 'ephemeral'; }; display_number?: number; } | { name: 'bash'; type: 'bash_20250124'; cache_control?: { type: 'ephemeral'; }; } | { name: 'str_replace_editor'; type: 'text_editor_20250124'; cache_control?: { type: 'ephemeral'; }; }[]`\n  Definitions of tools that the model may use.\n\nIf you include `tools` in your API request, the model may return `tool_use` content blocks that represent the model's use of those tools. You can then run those tools using the tool input generated by the model and then optionally return results back to the model using `tool_result` content blocks.\n\nEach tool definition includes:\n\n* `name`: Name of the tool.\n* `description`: Optional, but strongly-recommended description of the tool.\n* `input_schema`: [JSON schema](https://json-schema.org/draft/2020-12) for the tool `input` shape that the model will produce in `tool_use` output content blocks.\n\nFor example, if you defined `tools` as:\n\n```json\n[\n  {\n    \"name\": \"get_stock_price\",\n    \"description\": \"Get the current stock price for a given ticker symbol.\",\n    \"input_schema\": {\n      \"type\": \"object\",\n      \"properties\": {\n        \"ticker\": {\n          \"type\": \"string\",\n          \"description\": \"The stock ticker symbol, e.g. AAPL for Apple Inc.\"\n        }\n      },\n      \"required\": [\"ticker\"]\n    }\n  }\n]\n```\n\nAnd then asked the model \"What's the S&P 500 at today?\", the model might produce `tool_use` content blocks in the response like this:\n\n```json\n[\n  {\n    \"type\": \"tool_use\",\n    \"id\": \"toolu_01D7FLrfh4GYq7yT1ULFeyMV\",\n    \"name\": \"get_stock_price\",\n    \"input\": { \"ticker\": \"^GSPC\" }\n  }\n]\n```\n\nYou might then run your `get_stock_price` tool with `{\"ticker\": \"^GSPC\"}` as an input, and return the following back to the model in a subsequent `user` message:\n\n```json\n[\n  {\n    \"type\": \"tool_result\",\n    \"tool_use_id\": \"toolu_01D7FLrfh4GYq7yT1ULFeyMV\",\n    \"content\": \"259.75 USD\"\n  }\n]\n```\n\nTools can be used for workflows that include running client-side tools and functions, or more generally whenever you want the model to produce a particular JSON structure of output.\n\nSee our [guide](https://docs.anthropic.com/en/docs/tool-use) for more details.\n\n- `anthropic-beta?: string[]`\n  Optional header to specify the beta version(s) you want to use.\n\nTo use multiple betas, use a comma separated list like `beta1,beta2` or specify the header multiple times for each beta.\n\n- `anthropic-version?: string`\n  The version of the Anthropic API you want to use.\n\nRead more about versioning and our version history [here](https://docs.anthropic.com/en/api/versioning).\n\n- `x-api-key?: string`\n  Your unique API key for authentication.\n\nThis key is required in the header of all API requests, to authenticate your account and access Anthropic's services. Get your API key through the [Console](https://console.anthropic.com/settings/keys). Each key is scoped to a Workspace.\n\n### Returns\n\n- `{ input_tokens: number; }`\n\n  - `input_tokens: number`\n\n### Example\n\n```typescript\nimport Sam from 'sam';\n\nconst client = new Sam();\n\nconst response = await client.messages.countTokensBeta({ messages: [{ content: 'string', role: 'user' }], model: 'claude-3-7-sonnet-20250219' });\n\nconsole.log(response);\n```",
    perLanguage: {
      go: {
        method: 'client.Messages.CountTokensBeta',
        example:
          'package main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/DefinitelyATestOrg/sam-go"\n\t"github.com/DefinitelyATestOrg/sam-go/option"\n\t"github.com/DefinitelyATestOrg/sam-go/shared"\n)\n\nfunc main() {\n\tclient := sam.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\tresponse, err := client.Messages.CountTokensBeta(context.TODO(), sam.MessageCountTokensBetaParams{\n\t\tMessages: sam.F([]sam.MessageCountTokensBetaParamsMessage{{\n\t\t\tContent: sam.F[sam.MessageCountTokensBetaParamsMessagesContentUnion](shared.UnionString("string")),\n\t\t\tRole:    sam.F(sam.MessageCountTokensBetaParamsMessagesRoleUser),\n\t\t}}),\n\t\tModel: sam.F("claude-3-7-sonnet-20250219"),\n\t})\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", response.InputTokens)\n}\n',
      },
      http: {
        example:
          'curl /api/v3/v1/messages/count_tokens \\\n    -H \'Content-Type: application/json\' \\\n    -d \'{\n          "messages": [\n            {\n              "content": "string",\n              "role": "user"\n            }\n          ],\n          "model": "claude-3-7-sonnet-20250219"\n        }\'',
      },
      java: {
        method: 'messages().countTokensBeta',
        example:
          'package me.elborai.api.example;\n\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\nimport me.elborai.api.models.messages.MessageCountTokensBetaParams;\nimport me.elborai.api.models.messages.MessageCountTokensBetaResponse;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        SamClient client = SamOkHttpClient.fromEnv();\n\n        MessageCountTokensBetaParams params = MessageCountTokensBetaParams.builder()\n            .addMessage(MessageCountTokensBetaParams.Message.builder()\n                .content("Hello, world")\n                .role(MessageCountTokensBetaParams.Message.Role.USER)\n                .build())\n            .model("claude-3-7-sonnet-20250219")\n            .build();\n        MessageCountTokensBetaResponse response = client.messages().countTokensBeta(params);\n    }\n}',
      },
      kotlin: {
        method: 'messages().countTokensBeta',
        example:
          'package me.elborai.api.example\n\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\nimport me.elborai.api.models.messages.MessageCountTokensBetaParams\nimport me.elborai.api.models.messages.MessageCountTokensBetaResponse\n\nfun main() {\n    val client: SamClient = SamOkHttpClient.fromEnv()\n\n    val params: MessageCountTokensBetaParams = MessageCountTokensBetaParams.builder()\n        .addMessage(MessageCountTokensBetaParams.Message.builder()\n            .content("Hello, world")\n            .role(MessageCountTokensBetaParams.Message.Role.USER)\n            .build())\n        .model("claude-3-7-sonnet-20250219")\n        .build()\n    val response: MessageCountTokensBetaResponse = client.messages().countTokensBeta(params)\n}',
      },
      python: {
        method: 'messages.count_tokens_beta',
        example:
          'from sam import Sam\n\nclient = Sam()\nresponse = client.messages.count_tokens_beta(\n    messages=[{\n        "content": "string",\n        "role": "user",\n    }],\n    model="claude-3-7-sonnet-20250219",\n)\nprint(response.input_tokens)',
      },
      ruby: {
        method: 'messages.count_tokens_beta',
        example:
          'require "sam"\n\nsam = Sam::Client.new(api_key: "My API Key")\n\nresponse = sam.messages.count_tokens_beta(\n  messages: [{content: "string", role: :user}],\n  model: "claude-3-7-sonnet-20250219"\n)\n\nputs(response)',
      },
      typescript: {
        method: 'client.messages.countTokensBeta',
        example:
          "import Sam from 'sam';\n\nconst client = new Sam();\n\nconst response = await client.messages.countTokensBeta({\n  messages: [{ content: 'string', role: 'user' }],\n  model: 'claude-3-7-sonnet-20250219',\n});\n\nconsole.log(response.input_tokens);",
      },
    },
  },
  {
    name: 'create',
    endpoint: '/v1/messages/batches',
    httpMethod: 'post',
    summary: 'Create a Message Batch',
    description:
      'Send a batch of Message creation requests.\n\nThe Message Batches API can be used to process multiple Messages API requests at once. Once a Message Batch is created, it begins processing immediately. Batches can take up to 24 hours to complete.\n\nLearn more about the Message Batches API in our [user guide](/en/docs/build-with-claude/batch-processing)',
    stainlessPath: '(resource) messages.batches > (method) create',
    qualified: 'client.messages.batches.create',
    params: [
      "requests: { custom_id: string; params: { max_tokens: number; messages: { content: string | { text: string; type: 'text'; cache_control?: object; citations?: object | object | object[]; } | { source: object | object; type: 'image'; cache_control?: object; } | { id: string; input: object; name: string; type: 'tool_use'; cache_control?: object; } | { tool_use_id: string; type: 'tool_result'; cache_control?: object; content?: string | object | object[]; is_error?: boolean; } | { source: object | object | object | object; type: 'document'; cache_control?: object; citations?: object; context?: string; title?: string; } | { signature: string; thinking: string; type: 'thinking'; } | { data: string; type: 'redacted_thinking'; }[]; role: 'user' | 'assistant'; }[]; model: string; metadata?: { user_id?: string; }; stop_sequences?: string[]; stream?: boolean; system?: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; }[]; temperature?: number; thinking?: { budget_tokens: number; type: 'enabled'; } | { type: 'disabled'; }; tool_choice?: { type: 'auto'; disable_parallel_tool_use?: boolean; } | { type: 'any'; disable_parallel_tool_use?: boolean; } | { name: string; type: 'tool'; disable_parallel_tool_use?: boolean; } | { type: 'none'; }; tools?: { input_schema: { type: 'object'; properties?: object; }; name: string; cache_control?: { type: 'ephemeral'; }; description?: string; } | { name: 'bash'; type: 'bash_20250124'; cache_control?: { type: 'ephemeral'; }; } | { name: 'str_replace_editor'; type: 'text_editor_20250124'; cache_control?: { type: 'ephemeral'; }; }[]; top_k?: number; top_p?: number; }; }[];",
      'anthropic-beta?: string[];',
      'anthropic-version?: string;',
      'x-api-key?: string;',
    ],
    response:
      "{ id: string; archived_at: string; cancel_initiated_at: string; created_at: string; ended_at: string; expires_at: string; processing_status: 'in_progress' | 'canceling' | 'ended'; request_counts: { canceled: number; errored: number; expired: number; processing: number; succeeded: number; }; results_url: string; type: 'message_batch'; }",
    markdown:
      "## create\n\n`client.messages.batches.create(requests: { custom_id: string; params: { max_tokens: number; messages: object[]; model: string; metadata?: object; stop_sequences?: string[]; stream?: boolean; system?: string | object[]; temperature?: number; thinking?: object | object; tool_choice?: object | object | object | object; tools?: object | object | object[]; top_k?: number; top_p?: number; }; }[], anthropic-beta?: string[], anthropic-version?: string, x-api-key?: string): { id: string; archived_at: string; cancel_initiated_at: string; created_at: string; ended_at: string; expires_at: string; processing_status: 'in_progress' | 'canceling' | 'ended'; request_counts: object; results_url: string; type: 'message_batch'; }`\n\n**post** `/v1/messages/batches`\n\nSend a batch of Message creation requests.\n\nThe Message Batches API can be used to process multiple Messages API requests at once. Once a Message Batch is created, it begins processing immediately. Batches can take up to 24 hours to complete.\n\nLearn more about the Message Batches API in our [user guide](/en/docs/build-with-claude/batch-processing)\n\n### Parameters\n\n- `requests: { custom_id: string; params: { max_tokens: number; messages: { content: string | { text: string; type: 'text'; cache_control?: object; citations?: object | object | object[]; } | { source: object | object; type: 'image'; cache_control?: object; } | { id: string; input: object; name: string; type: 'tool_use'; cache_control?: object; } | { tool_use_id: string; type: 'tool_result'; cache_control?: object; content?: string | object | object[]; is_error?: boolean; } | { source: object | object | object | object; type: 'document'; cache_control?: object; citations?: object; context?: string; title?: string; } | { signature: string; thinking: string; type: 'thinking'; } | { data: string; type: 'redacted_thinking'; }[]; role: 'user' | 'assistant'; }[]; model: string; metadata?: { user_id?: string; }; stop_sequences?: string[]; stream?: boolean; system?: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; }[]; temperature?: number; thinking?: { budget_tokens: number; type: 'enabled'; } | { type: 'disabled'; }; tool_choice?: { type: 'auto'; disable_parallel_tool_use?: boolean; } | { type: 'any'; disable_parallel_tool_use?: boolean; } | { name: string; type: 'tool'; disable_parallel_tool_use?: boolean; } | { type: 'none'; }; tools?: { input_schema: { type: 'object'; properties?: object; }; name: string; cache_control?: { type: 'ephemeral'; }; description?: string; } | { name: 'bash'; type: 'bash_20250124'; cache_control?: { type: 'ephemeral'; }; } | { name: 'str_replace_editor'; type: 'text_editor_20250124'; cache_control?: { type: 'ephemeral'; }; }[]; top_k?: number; top_p?: number; }; }[]`\n  List of requests for prompt completion. Each is an individual request to create a Message.\n\n- `anthropic-beta?: string[]`\n  Optional header to specify the beta version(s) you want to use.\n\nTo use multiple betas, use a comma separated list like `beta1,beta2` or specify the header multiple times for each beta.\n\n- `anthropic-version?: string`\n  The version of the Anthropic API you want to use.\n\nRead more about versioning and our version history [here](https://docs.anthropic.com/en/api/versioning).\n\n- `x-api-key?: string`\n  Your unique API key for authentication.\n\nThis key is required in the header of all API requests, to authenticate your account and access Anthropic's services. Get your API key through the [Console](https://console.anthropic.com/settings/keys). Each key is scoped to a Workspace.\n\n### Returns\n\n- `{ id: string; archived_at: string; cancel_initiated_at: string; created_at: string; ended_at: string; expires_at: string; processing_status: 'in_progress' | 'canceling' | 'ended'; request_counts: { canceled: number; errored: number; expired: number; processing: number; succeeded: number; }; results_url: string; type: 'message_batch'; }`\n\n  - `id: string`\n  - `archived_at: string`\n  - `cancel_initiated_at: string`\n  - `created_at: string`\n  - `ended_at: string`\n  - `expires_at: string`\n  - `processing_status: 'in_progress' | 'canceling' | 'ended'`\n  - `request_counts: { canceled: number; errored: number; expired: number; processing: number; succeeded: number; }`\n  - `results_url: string`\n  - `type: 'message_batch'`\n\n### Example\n\n```typescript\nimport Sam from 'sam';\n\nconst client = new Sam();\n\nconst batch = await client.messages.batches.create({ requests: [{\n  custom_id: 'my-custom-id-1',\n  params: {\n  max_tokens: 1024,\n  messages: [{ content: 'Hello, world', role: 'user' }],\n  model: 'claude-3-7-sonnet-20250219',\n},\n}] });\n\nconsole.log(batch);\n```",
    perLanguage: {
      go: {
        method: 'client.Messages.Batches.New',
        example:
          'package main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/DefinitelyATestOrg/sam-go"\n\t"github.com/DefinitelyATestOrg/sam-go/option"\n\t"github.com/DefinitelyATestOrg/sam-go/shared"\n)\n\nfunc main() {\n\tclient := sam.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\tbatch, err := client.Messages.Batches.New(context.TODO(), sam.MessageBatchNewParams{\n\t\tRequests: sam.F([]sam.MessageBatchNewParamsRequest{{\n\t\t\tCustomID: sam.F("my-custom-id-1"),\n\t\t\tParams: sam.F(sam.MessageBatchNewParamsRequestsParams{\n\t\t\t\tMaxTokens: sam.F(int64(1024)),\n\t\t\t\tMessages: sam.F([]sam.MessageBatchNewParamsRequestsParamsMessage{{\n\t\t\t\t\tContent: sam.F[sam.MessageBatchNewParamsRequestsParamsMessagesContentUnion](shared.UnionString("Hello, world")),\n\t\t\t\t\tRole:    sam.F(sam.MessageBatchNewParamsRequestsParamsMessagesRoleUser),\n\t\t\t\t}}),\n\t\t\t\tModel: sam.F("claude-3-7-sonnet-20250219"),\n\t\t\t}),\n\t\t}}),\n\t})\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", batch.ID)\n}\n',
      },
      http: {
        example:
          'curl /api/v3/v1/messages/batches \\\n    -H \'Content-Type: application/json\' \\\n    -d \'{\n          "requests": [\n            {\n              "custom_id": "my-custom-id-1",\n              "params": {\n                "max_tokens": 1024,\n                "messages": [\n                  {\n                    "content": "Hello, world",\n                    "role": "user"\n                  }\n                ],\n                "model": "claude-3-7-sonnet-20250219"\n              }\n            }\n          ]\n        }\'',
      },
      java: {
        method: 'messages().batches().create',
        example:
          'package me.elborai.api.example;\n\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\nimport me.elborai.api.models.messages.batches.BatchCreateParams;\nimport me.elborai.api.models.messages.batches.BatchCreateResponse;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        SamClient client = SamOkHttpClient.fromEnv();\n\n        BatchCreateParams params = BatchCreateParams.builder()\n            .addRequest(BatchCreateParams.Request.builder()\n                .customId("my-custom-id-1")\n                .params(BatchCreateParams.Request.Params.builder()\n                    .maxTokens(1024L)\n                    .addMessage(BatchCreateParams.Request.Params.Message.builder()\n                        .content("Hello, world")\n                        .role(BatchCreateParams.Request.Params.Message.Role.USER)\n                        .build())\n                    .model("claude-3-7-sonnet-20250219")\n                    .build())\n                .build())\n            .build();\n        BatchCreateResponse batch = client.messages().batches().create(params);\n    }\n}',
      },
      kotlin: {
        method: 'messages().batches().create',
        example:
          'package me.elborai.api.example\n\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\nimport me.elborai.api.models.messages.batches.BatchCreateParams\nimport me.elborai.api.models.messages.batches.BatchCreateResponse\n\nfun main() {\n    val client: SamClient = SamOkHttpClient.fromEnv()\n\n    val params: BatchCreateParams = BatchCreateParams.builder()\n        .addRequest(BatchCreateParams.Request.builder()\n            .customId("my-custom-id-1")\n            .params(BatchCreateParams.Request.Params.builder()\n                .maxTokens(1024L)\n                .addMessage(BatchCreateParams.Request.Params.Message.builder()\n                    .content("Hello, world")\n                    .role(BatchCreateParams.Request.Params.Message.Role.USER)\n                    .build())\n                .model("claude-3-7-sonnet-20250219")\n                .build())\n            .build())\n        .build()\n    val batch: BatchCreateResponse = client.messages().batches().create(params)\n}',
      },
      python: {
        method: 'messages.batches.create',
        example:
          'from sam import Sam\n\nclient = Sam()\nbatch = client.messages.batches.create(\n    requests=[{\n        "custom_id": "my-custom-id-1",\n        "params": {\n            "max_tokens": 1024,\n            "messages": [{\n                "content": "Hello, world",\n                "role": "user",\n            }],\n            "model": "claude-3-7-sonnet-20250219",\n        },\n    }],\n)\nprint(batch.id)',
      },
      ruby: {
        method: 'messages.batches.create',
        example:
          'require "sam"\n\nsam = Sam::Client.new(api_key: "My API Key")\n\nbatch = sam.messages.batches.create(\n  requests: [\n    {\n      custom_id: "my-custom-id-1",\n      params: {max_tokens: 1024, messages: [{content: "Hello, world", role: :user}], model: "claude-3-7-sonnet-20250219"}\n    }\n  ]\n)\n\nputs(batch)',
      },
      typescript: {
        method: 'client.messages.batches.create',
        example:
          "import Sam from 'sam';\n\nconst client = new Sam();\n\nconst batch = await client.messages.batches.create({\n  requests: [\n    {\n      custom_id: 'my-custom-id-1',\n      params: {\n        max_tokens: 1024,\n        messages: [{ content: 'Hello, world', role: 'user' }],\n        model: 'claude-3-7-sonnet-20250219',\n      },\n    },\n  ],\n});\n\nconsole.log(batch.id);",
      },
    },
  },
  {
    name: 'list',
    endpoint: '/v1/messages/batches',
    httpMethod: 'get',
    summary: 'List Message Batches',
    description:
      'List all Message Batches within a Workspace. Most recently created batches are returned first.\n\nLearn more about the Message Batches API in our [user guide](/en/docs/build-with-claude/batch-processing)',
    stainlessPath: '(resource) messages.batches > (method) list',
    qualified: 'client.messages.batches.list',
    params: [
      'after_id?: string;',
      'before_id?: string;',
      'limit?: number;',
      'anthropic-beta?: string[];',
      'anthropic-version?: string;',
      'x-api-key?: string;',
    ],
    response:
      "{ data: { id: string; archived_at: string; cancel_initiated_at: string; created_at: string; ended_at: string; expires_at: string; processing_status: 'in_progress' | 'canceling' | 'ended'; request_counts: { canceled: number; errored: number; expired: number; processing: number; succeeded: number; }; results_url: string; type: 'message_batch'; }[]; first_id: string; has_more: boolean; last_id: string; }",
    markdown:
      "## list\n\n`client.messages.batches.list(after_id?: string, before_id?: string, limit?: number, anthropic-beta?: string[], anthropic-version?: string, x-api-key?: string): { data: object[]; first_id: string; has_more: boolean; last_id: string; }`\n\n**get** `/v1/messages/batches`\n\nList all Message Batches within a Workspace. Most recently created batches are returned first.\n\nLearn more about the Message Batches API in our [user guide](/en/docs/build-with-claude/batch-processing)\n\n### Parameters\n\n- `after_id?: string`\n  ID of the object to use as a cursor for pagination. When provided, returns the page of results immediately after this object.\n\n- `before_id?: string`\n  ID of the object to use as a cursor for pagination. When provided, returns the page of results immediately before this object.\n\n- `limit?: number`\n  Number of items to return per page.\n\nDefaults to `20`. Ranges from `1` to `1000`.\n\n- `anthropic-beta?: string[]`\n  Optional header to specify the beta version(s) you want to use.\n\nTo use multiple betas, use a comma separated list like `beta1,beta2` or specify the header multiple times for each beta.\n\n- `anthropic-version?: string`\n  The version of the Anthropic API you want to use.\n\nRead more about versioning and our version history [here](https://docs.anthropic.com/en/api/versioning).\n\n- `x-api-key?: string`\n  Your unique API key for authentication.\n\nThis key is required in the header of all API requests, to authenticate your account and access Anthropic's services. Get your API key through the [Console](https://console.anthropic.com/settings/keys). Each key is scoped to a Workspace.\n\n### Returns\n\n- `{ data: { id: string; archived_at: string; cancel_initiated_at: string; created_at: string; ended_at: string; expires_at: string; processing_status: 'in_progress' | 'canceling' | 'ended'; request_counts: { canceled: number; errored: number; expired: number; processing: number; succeeded: number; }; results_url: string; type: 'message_batch'; }[]; first_id: string; has_more: boolean; last_id: string; }`\n\n  - `data: { id: string; archived_at: string; cancel_initiated_at: string; created_at: string; ended_at: string; expires_at: string; processing_status: 'in_progress' | 'canceling' | 'ended'; request_counts: { canceled: number; errored: number; expired: number; processing: number; succeeded: number; }; results_url: string; type: 'message_batch'; }[]`\n  - `first_id: string`\n  - `has_more: boolean`\n  - `last_id: string`\n\n### Example\n\n```typescript\nimport Sam from 'sam';\n\nconst client = new Sam();\n\nconst batches = await client.messages.batches.list();\n\nconsole.log(batches);\n```",
    perLanguage: {
      go: {
        method: 'client.Messages.Batches.List',
        example:
          'package main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/DefinitelyATestOrg/sam-go"\n\t"github.com/DefinitelyATestOrg/sam-go/option"\n)\n\nfunc main() {\n\tclient := sam.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\tbatches, err := client.Messages.Batches.List(context.TODO(), sam.MessageBatchListParams{})\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", batches.FirstID)\n}\n',
      },
      http: {
        example: 'curl /api/v3/v1/messages/batches',
      },
      java: {
        method: 'messages().batches().list',
        example:
          'package me.elborai.api.example;\n\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\nimport me.elborai.api.models.messages.batches.BatchListParams;\nimport me.elborai.api.models.messages.batches.BatchListResponse;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        SamClient client = SamOkHttpClient.fromEnv();\n\n        BatchListResponse batches = client.messages().batches().list();\n    }\n}',
      },
      kotlin: {
        method: 'messages().batches().list',
        example:
          'package me.elborai.api.example\n\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\nimport me.elborai.api.models.messages.batches.BatchListParams\nimport me.elborai.api.models.messages.batches.BatchListResponse\n\nfun main() {\n    val client: SamClient = SamOkHttpClient.fromEnv()\n\n    val batches: BatchListResponse = client.messages().batches().list()\n}',
      },
      python: {
        method: 'messages.batches.list',
        example:
          'from sam import Sam\n\nclient = Sam()\nbatches = client.messages.batches.list()\nprint(batches.first_id)',
      },
      ruby: {
        method: 'messages.batches.list',
        example:
          'require "sam"\n\nsam = Sam::Client.new(api_key: "My API Key")\n\nbatches = sam.messages.batches.list\n\nputs(batches)',
      },
      typescript: {
        method: 'client.messages.batches.list',
        example:
          "import Sam from 'sam';\n\nconst client = new Sam();\n\nconst batches = await client.messages.batches.list();\n\nconsole.log(batches.first_id);",
      },
    },
  },
  {
    name: 'retrieve',
    endpoint: '/v1/messages/batches/{message_batch_id}',
    httpMethod: 'get',
    summary: 'Retrieve a Message Batch',
    description:
      'This endpoint is idempotent and can be used to poll for Message Batch completion. To access the results of a Message Batch, make a request to the `results_url` field in the response.\n\nLearn more about the Message Batches API in our [user guide](/en/docs/build-with-claude/batch-processing)',
    stainlessPath: '(resource) messages.batches > (method) retrieve',
    qualified: 'client.messages.batches.retrieve',
    params: [
      'message_batch_id: string;',
      'anthropic-beta?: string[];',
      'anthropic-version?: string;',
      'x-api-key?: string;',
    ],
    response:
      "{ id: string; archived_at: string; cancel_initiated_at: string; created_at: string; ended_at: string; expires_at: string; processing_status: 'in_progress' | 'canceling' | 'ended'; request_counts: { canceled: number; errored: number; expired: number; processing: number; succeeded: number; }; results_url: string; type: 'message_batch'; }",
    markdown:
      "## retrieve\n\n`client.messages.batches.retrieve(message_batch_id: string, anthropic-beta?: string[], anthropic-version?: string, x-api-key?: string): { id: string; archived_at: string; cancel_initiated_at: string; created_at: string; ended_at: string; expires_at: string; processing_status: 'in_progress' | 'canceling' | 'ended'; request_counts: object; results_url: string; type: 'message_batch'; }`\n\n**get** `/v1/messages/batches/{message_batch_id}`\n\nThis endpoint is idempotent and can be used to poll for Message Batch completion. To access the results of a Message Batch, make a request to the `results_url` field in the response.\n\nLearn more about the Message Batches API in our [user guide](/en/docs/build-with-claude/batch-processing)\n\n### Parameters\n\n- `message_batch_id: string`\n  ID of the Message Batch.\n\n- `anthropic-beta?: string[]`\n  Optional header to specify the beta version(s) you want to use.\n\nTo use multiple betas, use a comma separated list like `beta1,beta2` or specify the header multiple times for each beta.\n\n- `anthropic-version?: string`\n  The version of the Anthropic API you want to use.\n\nRead more about versioning and our version history [here](https://docs.anthropic.com/en/api/versioning).\n\n- `x-api-key?: string`\n  Your unique API key for authentication.\n\nThis key is required in the header of all API requests, to authenticate your account and access Anthropic's services. Get your API key through the [Console](https://console.anthropic.com/settings/keys). Each key is scoped to a Workspace.\n\n### Returns\n\n- `{ id: string; archived_at: string; cancel_initiated_at: string; created_at: string; ended_at: string; expires_at: string; processing_status: 'in_progress' | 'canceling' | 'ended'; request_counts: { canceled: number; errored: number; expired: number; processing: number; succeeded: number; }; results_url: string; type: 'message_batch'; }`\n\n  - `id: string`\n  - `archived_at: string`\n  - `cancel_initiated_at: string`\n  - `created_at: string`\n  - `ended_at: string`\n  - `expires_at: string`\n  - `processing_status: 'in_progress' | 'canceling' | 'ended'`\n  - `request_counts: { canceled: number; errored: number; expired: number; processing: number; succeeded: number; }`\n  - `results_url: string`\n  - `type: 'message_batch'`\n\n### Example\n\n```typescript\nimport Sam from 'sam';\n\nconst client = new Sam();\n\nconst batch = await client.messages.batches.retrieve('message_batch_id');\n\nconsole.log(batch);\n```",
    perLanguage: {
      go: {
        method: 'client.Messages.Batches.Get',
        example:
          'package main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/DefinitelyATestOrg/sam-go"\n\t"github.com/DefinitelyATestOrg/sam-go/option"\n)\n\nfunc main() {\n\tclient := sam.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\tbatch, err := client.Messages.Batches.Get(\n\t\tcontext.TODO(),\n\t\t"message_batch_id",\n\t\tsam.MessageBatchGetParams{},\n\t)\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", batch.ID)\n}\n',
      },
      http: {
        example: 'curl /api/v3/v1/messages/batches/$MESSAGE_BATCH_ID',
      },
      java: {
        method: 'messages().batches().retrieve',
        example:
          'package me.elborai.api.example;\n\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\nimport me.elborai.api.models.messages.batches.BatchRetrieveParams;\nimport me.elborai.api.models.messages.batches.BatchRetrieveResponse;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        SamClient client = SamOkHttpClient.fromEnv();\n\n        BatchRetrieveResponse batch = client.messages().batches().retrieve("message_batch_id");\n    }\n}',
      },
      kotlin: {
        method: 'messages().batches().retrieve',
        example:
          'package me.elborai.api.example\n\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\nimport me.elborai.api.models.messages.batches.BatchRetrieveParams\nimport me.elborai.api.models.messages.batches.BatchRetrieveResponse\n\nfun main() {\n    val client: SamClient = SamOkHttpClient.fromEnv()\n\n    val batch: BatchRetrieveResponse = client.messages().batches().retrieve("message_batch_id")\n}',
      },
      python: {
        method: 'messages.batches.retrieve',
        example:
          'from sam import Sam\n\nclient = Sam()\nbatch = client.messages.batches.retrieve(\n    message_batch_id="message_batch_id",\n)\nprint(batch.id)',
      },
      ruby: {
        method: 'messages.batches.retrieve',
        example:
          'require "sam"\n\nsam = Sam::Client.new(api_key: "My API Key")\n\nbatch = sam.messages.batches.retrieve("message_batch_id")\n\nputs(batch)',
      },
      typescript: {
        method: 'client.messages.batches.retrieve',
        example:
          "import Sam from 'sam';\n\nconst client = new Sam();\n\nconst batch = await client.messages.batches.retrieve('message_batch_id');\n\nconsole.log(batch.id);",
      },
    },
  },
  {
    name: 'delete',
    endpoint: '/v1/messages/batches/{message_batch_id}',
    httpMethod: 'delete',
    summary: 'Delete a Message Batch',
    description:
      "Delete a Message Batch.\n\nMessage Batches can only be deleted once they've finished processing. If you'd like to delete an in-progress batch, you must first cancel it.\n\nLearn more about the Message Batches API in our [user guide](/en/docs/build-with-claude/batch-processing)",
    stainlessPath: '(resource) messages.batches > (method) delete',
    qualified: 'client.messages.batches.delete',
    params: [
      'message_batch_id: string;',
      'anthropic-beta?: string[];',
      'anthropic-version?: string;',
      'x-api-key?: string;',
    ],
    response: "{ id: string; type: 'message_batch_deleted'; }",
    markdown:
      "## delete\n\n`client.messages.batches.delete(message_batch_id: string, anthropic-beta?: string[], anthropic-version?: string, x-api-key?: string): { id: string; type: 'message_batch_deleted'; }`\n\n**delete** `/v1/messages/batches/{message_batch_id}`\n\nDelete a Message Batch.\n\nMessage Batches can only be deleted once they've finished processing. If you'd like to delete an in-progress batch, you must first cancel it.\n\nLearn more about the Message Batches API in our [user guide](/en/docs/build-with-claude/batch-processing)\n\n### Parameters\n\n- `message_batch_id: string`\n  ID of the Message Batch.\n\n- `anthropic-beta?: string[]`\n  Optional header to specify the beta version(s) you want to use.\n\nTo use multiple betas, use a comma separated list like `beta1,beta2` or specify the header multiple times for each beta.\n\n- `anthropic-version?: string`\n  The version of the Anthropic API you want to use.\n\nRead more about versioning and our version history [here](https://docs.anthropic.com/en/api/versioning).\n\n- `x-api-key?: string`\n  Your unique API key for authentication.\n\nThis key is required in the header of all API requests, to authenticate your account and access Anthropic's services. Get your API key through the [Console](https://console.anthropic.com/settings/keys). Each key is scoped to a Workspace.\n\n### Returns\n\n- `{ id: string; type: 'message_batch_deleted'; }`\n\n  - `id: string`\n  - `type: 'message_batch_deleted'`\n\n### Example\n\n```typescript\nimport Sam from 'sam';\n\nconst client = new Sam();\n\nconst batch = await client.messages.batches.delete('message_batch_id');\n\nconsole.log(batch);\n```",
    perLanguage: {
      go: {
        method: 'client.Messages.Batches.Delete',
        example:
          'package main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/DefinitelyATestOrg/sam-go"\n\t"github.com/DefinitelyATestOrg/sam-go/option"\n)\n\nfunc main() {\n\tclient := sam.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\tbatch, err := client.Messages.Batches.Delete(\n\t\tcontext.TODO(),\n\t\t"message_batch_id",\n\t\tsam.MessageBatchDeleteParams{},\n\t)\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", batch.ID)\n}\n',
      },
      http: {
        example: 'curl /api/v3/v1/messages/batches/$MESSAGE_BATCH_ID \\\n    -X DELETE',
      },
      java: {
        method: 'messages().batches().delete',
        example:
          'package me.elborai.api.example;\n\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\nimport me.elborai.api.models.messages.batches.BatchDeleteParams;\nimport me.elborai.api.models.messages.batches.BatchDeleteResponse;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        SamClient client = SamOkHttpClient.fromEnv();\n\n        BatchDeleteResponse batch = client.messages().batches().delete("message_batch_id");\n    }\n}',
      },
      kotlin: {
        method: 'messages().batches().delete',
        example:
          'package me.elborai.api.example\n\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\nimport me.elborai.api.models.messages.batches.BatchDeleteParams\nimport me.elborai.api.models.messages.batches.BatchDeleteResponse\n\nfun main() {\n    val client: SamClient = SamOkHttpClient.fromEnv()\n\n    val batch: BatchDeleteResponse = client.messages().batches().delete("message_batch_id")\n}',
      },
      python: {
        method: 'messages.batches.delete',
        example:
          'from sam import Sam\n\nclient = Sam()\nbatch = client.messages.batches.delete(\n    message_batch_id="message_batch_id",\n)\nprint(batch.id)',
      },
      ruby: {
        method: 'messages.batches.delete',
        example:
          'require "sam"\n\nsam = Sam::Client.new(api_key: "My API Key")\n\nbatch = sam.messages.batches.delete("message_batch_id")\n\nputs(batch)',
      },
      typescript: {
        method: 'client.messages.batches.delete',
        example:
          "import Sam from 'sam';\n\nconst client = new Sam();\n\nconst batch = await client.messages.batches.delete('message_batch_id');\n\nconsole.log(batch.id);",
      },
    },
  },
  {
    name: 'cancel',
    endpoint: '/v1/messages/batches/{message_batch_id}/cancel',
    httpMethod: 'post',
    summary: 'Cancel a Message Batch',
    description:
      'Batches may be canceled any time before processing ends. Once cancellation is initiated, the batch enters a `canceling` state, at which time the system may complete any in-progress, non-interruptible requests before finalizing cancellation.\n\nThe number of canceled requests is specified in `request_counts`. To determine which requests were canceled, check the individual results within the batch. Note that cancellation may not result in any canceled requests if they were non-interruptible.\n\nLearn more about the Message Batches API in our [user guide](/en/docs/build-with-claude/batch-processing)',
    stainlessPath: '(resource) messages.batches > (method) cancel',
    qualified: 'client.messages.batches.cancel',
    params: [
      'message_batch_id: string;',
      'anthropic-beta?: string[];',
      'anthropic-version?: string;',
      'x-api-key?: string;',
    ],
    response:
      "{ id: string; archived_at: string; cancel_initiated_at: string; created_at: string; ended_at: string; expires_at: string; processing_status: 'in_progress' | 'canceling' | 'ended'; request_counts: { canceled: number; errored: number; expired: number; processing: number; succeeded: number; }; results_url: string; type: 'message_batch'; }",
    markdown:
      "## cancel\n\n`client.messages.batches.cancel(message_batch_id: string, anthropic-beta?: string[], anthropic-version?: string, x-api-key?: string): { id: string; archived_at: string; cancel_initiated_at: string; created_at: string; ended_at: string; expires_at: string; processing_status: 'in_progress' | 'canceling' | 'ended'; request_counts: object; results_url: string; type: 'message_batch'; }`\n\n**post** `/v1/messages/batches/{message_batch_id}/cancel`\n\nBatches may be canceled any time before processing ends. Once cancellation is initiated, the batch enters a `canceling` state, at which time the system may complete any in-progress, non-interruptible requests before finalizing cancellation.\n\nThe number of canceled requests is specified in `request_counts`. To determine which requests were canceled, check the individual results within the batch. Note that cancellation may not result in any canceled requests if they were non-interruptible.\n\nLearn more about the Message Batches API in our [user guide](/en/docs/build-with-claude/batch-processing)\n\n### Parameters\n\n- `message_batch_id: string`\n  ID of the Message Batch.\n\n- `anthropic-beta?: string[]`\n  Optional header to specify the beta version(s) you want to use.\n\nTo use multiple betas, use a comma separated list like `beta1,beta2` or specify the header multiple times for each beta.\n\n- `anthropic-version?: string`\n  The version of the Anthropic API you want to use.\n\nRead more about versioning and our version history [here](https://docs.anthropic.com/en/api/versioning).\n\n- `x-api-key?: string`\n  Your unique API key for authentication.\n\nThis key is required in the header of all API requests, to authenticate your account and access Anthropic's services. Get your API key through the [Console](https://console.anthropic.com/settings/keys). Each key is scoped to a Workspace.\n\n### Returns\n\n- `{ id: string; archived_at: string; cancel_initiated_at: string; created_at: string; ended_at: string; expires_at: string; processing_status: 'in_progress' | 'canceling' | 'ended'; request_counts: { canceled: number; errored: number; expired: number; processing: number; succeeded: number; }; results_url: string; type: 'message_batch'; }`\n\n  - `id: string`\n  - `archived_at: string`\n  - `cancel_initiated_at: string`\n  - `created_at: string`\n  - `ended_at: string`\n  - `expires_at: string`\n  - `processing_status: 'in_progress' | 'canceling' | 'ended'`\n  - `request_counts: { canceled: number; errored: number; expired: number; processing: number; succeeded: number; }`\n  - `results_url: string`\n  - `type: 'message_batch'`\n\n### Example\n\n```typescript\nimport Sam from 'sam';\n\nconst client = new Sam();\n\nconst response = await client.messages.batches.cancel('message_batch_id');\n\nconsole.log(response);\n```",
    perLanguage: {
      go: {
        method: 'client.Messages.Batches.Cancel',
        example:
          'package main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/DefinitelyATestOrg/sam-go"\n\t"github.com/DefinitelyATestOrg/sam-go/option"\n)\n\nfunc main() {\n\tclient := sam.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\tresponse, err := client.Messages.Batches.Cancel(\n\t\tcontext.TODO(),\n\t\t"message_batch_id",\n\t\tsam.MessageBatchCancelParams{},\n\t)\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", response.ID)\n}\n',
      },
      http: {
        example: 'curl /api/v3/v1/messages/batches/$MESSAGE_BATCH_ID/cancel \\\n    -X POST',
      },
      java: {
        method: 'messages().batches().cancel',
        example:
          'package me.elborai.api.example;\n\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\nimport me.elborai.api.models.messages.batches.BatchCancelParams;\nimport me.elborai.api.models.messages.batches.BatchCancelResponse;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        SamClient client = SamOkHttpClient.fromEnv();\n\n        BatchCancelResponse response = client.messages().batches().cancel("message_batch_id");\n    }\n}',
      },
      kotlin: {
        method: 'messages().batches().cancel',
        example:
          'package me.elborai.api.example\n\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\nimport me.elborai.api.models.messages.batches.BatchCancelParams\nimport me.elborai.api.models.messages.batches.BatchCancelResponse\n\nfun main() {\n    val client: SamClient = SamOkHttpClient.fromEnv()\n\n    val response: BatchCancelResponse = client.messages().batches().cancel("message_batch_id")\n}',
      },
      python: {
        method: 'messages.batches.cancel',
        example:
          'from sam import Sam\n\nclient = Sam()\nresponse = client.messages.batches.cancel(\n    message_batch_id="message_batch_id",\n)\nprint(response.id)',
      },
      ruby: {
        method: 'messages.batches.cancel',
        example:
          'require "sam"\n\nsam = Sam::Client.new(api_key: "My API Key")\n\nresponse = sam.messages.batches.cancel("message_batch_id")\n\nputs(response)',
      },
      typescript: {
        method: 'client.messages.batches.cancel',
        example:
          "import Sam from 'sam';\n\nconst client = new Sam();\n\nconst response = await client.messages.batches.cancel('message_batch_id');\n\nconsole.log(response.id);",
      },
    },
  },
  {
    name: 'results',
    endpoint: '/v1/messages/batches/{message_batch_id}/results',
    httpMethod: 'get',
    summary: 'Retrieve Message Batch results',
    description:
      'Streams the results of a Message Batch as a `.jsonl` file.\n\nEach line in the file is a JSON object containing the result of a single request in the Message Batch. Results are not guaranteed to be in the same order as requests. Use the `custom_id` field to match results to requests.\n\nLearn more about the Message Batches API in our [user guide](/en/docs/build-with-claude/batch-processing)',
    stainlessPath: '(resource) messages.batches > (method) results',
    qualified: 'client.messages.batches.results',
    params: [
      'message_batch_id: string;',
      'anthropic-beta?: string[];',
      'anthropic-version?: string;',
      'x-api-key?: string;',
    ],
    response:
      "{ custom_id: string; result: { message: { id: string; content: object | object | object | object[]; model: string; role: 'assistant'; stop_reason: 'end_turn' | 'max_tokens' | 'stop_sequence' | 'tool_use'; stop_sequence: string; type: 'message'; usage: object; }; type: 'succeeded'; } | { error: { error: object | object | object | object | object | object | object | object | object; type: 'error'; }; type: 'errored'; } | { type: 'canceled'; } | { type: 'expired'; }; }",
    markdown:
      "## results\n\n`client.messages.batches.results(message_batch_id: string, anthropic-beta?: string[], anthropic-version?: string, x-api-key?: string): { custom_id: string; result: object | object | object | object; }`\n\n**get** `/v1/messages/batches/{message_batch_id}/results`\n\nStreams the results of a Message Batch as a `.jsonl` file.\n\nEach line in the file is a JSON object containing the result of a single request in the Message Batch. Results are not guaranteed to be in the same order as requests. Use the `custom_id` field to match results to requests.\n\nLearn more about the Message Batches API in our [user guide](/en/docs/build-with-claude/batch-processing)\n\n### Parameters\n\n- `message_batch_id: string`\n  ID of the Message Batch.\n\n- `anthropic-beta?: string[]`\n  Optional header to specify the beta version(s) you want to use.\n\nTo use multiple betas, use a comma separated list like `beta1,beta2` or specify the header multiple times for each beta.\n\n- `anthropic-version?: string`\n  The version of the Anthropic API you want to use.\n\nRead more about versioning and our version history [here](https://docs.anthropic.com/en/api/versioning).\n\n- `x-api-key?: string`\n  Your unique API key for authentication.\n\nThis key is required in the header of all API requests, to authenticate your account and access Anthropic's services. Get your API key through the [Console](https://console.anthropic.com/settings/keys). Each key is scoped to a Workspace.\n\n### Returns\n\n- `{ custom_id: string; result: { message: { id: string; content: object | object | object | object[]; model: string; role: 'assistant'; stop_reason: 'end_turn' | 'max_tokens' | 'stop_sequence' | 'tool_use'; stop_sequence: string; type: 'message'; usage: object; }; type: 'succeeded'; } | { error: { error: object | object | object | object | object | object | object | object | object; type: 'error'; }; type: 'errored'; } | { type: 'canceled'; } | { type: 'expired'; }; }`\n  This is a single line in the response `.jsonl` file and does not represent the response as a whole.\n\n  - `custom_id: string`\n  - `result: { message: { id: string; content: { citations: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; text: string; type: 'text'; } | { id: string; input: object; name: string; type: 'tool_use'; } | { signature: string; thinking: string; type: 'thinking'; } | { data: string; type: 'redacted_thinking'; }[]; model: string; role: 'assistant'; stop_reason: 'end_turn' | 'max_tokens' | 'stop_sequence' | 'tool_use'; stop_sequence: string; type: 'message'; usage: { cache_creation_input_tokens: number; cache_read_input_tokens: number; input_tokens: number; output_tokens: number; }; }; type: 'succeeded'; } | { error: { error: { message: string; type: 'invalid_request_error'; } | { message: string; type: 'authentication_error'; } | { message: string; type: 'billing_error'; } | { message: string; type: 'permission_error'; } | { message: string; type: 'not_found_error'; } | { message: string; type: 'rate_limit_error'; } | { message: string; type: 'timeout_error'; } | { message: string; type: 'api_error'; } | { message: string; type: 'overloaded_error'; }; type: 'error'; }; type: 'errored'; } | { type: 'canceled'; } | { type: 'expired'; }`\n\n### Example\n\n```typescript\nimport Sam from 'sam';\n\nconst client = new Sam();\n\nconst stream = await client.messages.batches.results('message_batch_id');\nfor await (const batchResultsResponse of stream) {\n  console.log(batchResultsResponse);\n}\n```",
    perLanguage: {
      go: {
        method: 'client.Messages.Batches.Results',
        example:
          'package main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/DefinitelyATestOrg/sam-go"\n\t"github.com/DefinitelyATestOrg/sam-go/option"\n)\n\nfunc main() {\n\tclient := sam.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\tstream := client.Messages.Batches.ResultsStreaming(\n\t\tcontext.TODO(),\n\t\t"message_batch_id",\n\t\tsam.MessageBatchResultsParams{},\n\t)\n\tfor stream.Next() {\n\t\tfmt.Printf("%+v\\n", stream.Current())\n\t}\n\terr := stream.Err()\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n}\n',
      },
      http: {
        example: 'curl /api/v3/v1/messages/batches/$MESSAGE_BATCH_ID/results',
      },
      java: {
        method: 'messages().batches().resultsStreaming',
        example:
          'package me.elborai.api.example;\n\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\nimport me.elborai.api.core.http.StreamResponse;\nimport me.elborai.api.models.messages.batches.BatchResultsParams;\nimport me.elborai.api.models.messages.batches.BatchResultsResponse;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        SamClient client = SamOkHttpClient.fromEnv();\n\n        StreamResponse<BatchResultsResponse> response = client.messages().batches().resultsStreaming("message_batch_id");\n    }\n}',
      },
      kotlin: {
        method: 'messages().batches().resultsStreaming',
        example:
          'package me.elborai.api.example\n\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\nimport me.elborai.api.core.http.StreamResponse\nimport me.elborai.api.models.messages.batches.BatchResultsParams\nimport me.elborai.api.models.messages.batches.BatchResultsResponse\n\nfun main() {\n    val client: SamClient = SamOkHttpClient.fromEnv()\n\n    val response: StreamResponse<BatchResultsResponse> = client.messages().batches().resultsStreaming("message_batch_id")\n}',
      },
      python: {
        method: 'messages.batches.results',
        example:
          'from sam import Sam\n\nclient = Sam()\nfor batch in client.messages.batches.results(\n    message_batch_id="message_batch_id",\n):\n  print(batch)',
      },
      ruby: {
        method: 'messages.batches.results',
        example:
          'require "sam"\n\nsam = Sam::Client.new(api_key: "My API Key")\n\nresponse = sam.messages.batches.results("message_batch_id")\n\nputs(response)',
      },
      typescript: {
        method: 'client.messages.batches.results',
        example:
          "import Sam from 'sam';\n\nconst client = new Sam();\n\nconst response = await client.messages.batches.results('message_batch_id');\n\nconsole.log(response.custom_id);",
      },
    },
  },
  {
    name: 'cancel_beta',
    endpoint: '/v1/messages/batches/{message_batch_id}/cancel?beta=true',
    httpMethod: 'post',
    summary: 'Cancel a Message Batch',
    description:
      'Batches may be canceled any time before processing ends. Once cancellation is initiated, the batch enters a `canceling` state, at which time the system may complete any in-progress, non-interruptible requests before finalizing cancellation.\n\nThe number of canceled requests is specified in `request_counts`. To determine which requests were canceled, check the individual results within the batch. Note that cancellation may not result in any canceled requests if they were non-interruptible.\n\nLearn more about the Message Batches API in our [user guide](/en/docs/build-with-claude/batch-processing)',
    stainlessPath: '(resource) messages.batches > (method) cancel_beta',
    qualified: 'client.messages.batches.cancelBeta',
    params: [
      'message_batch_id: string;',
      'anthropic-beta?: string[];',
      'anthropic-version?: string;',
      'x-api-key?: string;',
    ],
    response:
      "{ id: string; archived_at: string; cancel_initiated_at: string; created_at: string; ended_at: string; expires_at: string; processing_status: 'in_progress' | 'canceling' | 'ended'; request_counts: { canceled: number; errored: number; expired: number; processing: number; succeeded: number; }; results_url: string; type: 'message_batch'; }",
    markdown:
      "## cancel_beta\n\n`client.messages.batches.cancelBeta(message_batch_id: string, anthropic-beta?: string[], anthropic-version?: string, x-api-key?: string): { id: string; archived_at: string; cancel_initiated_at: string; created_at: string; ended_at: string; expires_at: string; processing_status: 'in_progress' | 'canceling' | 'ended'; request_counts: object; results_url: string; type: 'message_batch'; }`\n\n**post** `/v1/messages/batches/{message_batch_id}/cancel?beta=true`\n\nBatches may be canceled any time before processing ends. Once cancellation is initiated, the batch enters a `canceling` state, at which time the system may complete any in-progress, non-interruptible requests before finalizing cancellation.\n\nThe number of canceled requests is specified in `request_counts`. To determine which requests were canceled, check the individual results within the batch. Note that cancellation may not result in any canceled requests if they were non-interruptible.\n\nLearn more about the Message Batches API in our [user guide](/en/docs/build-with-claude/batch-processing)\n\n### Parameters\n\n- `message_batch_id: string`\n  ID of the Message Batch.\n\n- `anthropic-beta?: string[]`\n  Optional header to specify the beta version(s) you want to use.\n\nTo use multiple betas, use a comma separated list like `beta1,beta2` or specify the header multiple times for each beta.\n\n- `anthropic-version?: string`\n  The version of the Anthropic API you want to use.\n\nRead more about versioning and our version history [here](https://docs.anthropic.com/en/api/versioning).\n\n- `x-api-key?: string`\n  Your unique API key for authentication.\n\nThis key is required in the header of all API requests, to authenticate your account and access Anthropic's services. Get your API key through the [Console](https://console.anthropic.com/settings/keys). Each key is scoped to a Workspace.\n\n### Returns\n\n- `{ id: string; archived_at: string; cancel_initiated_at: string; created_at: string; ended_at: string; expires_at: string; processing_status: 'in_progress' | 'canceling' | 'ended'; request_counts: { canceled: number; errored: number; expired: number; processing: number; succeeded: number; }; results_url: string; type: 'message_batch'; }`\n\n  - `id: string`\n  - `archived_at: string`\n  - `cancel_initiated_at: string`\n  - `created_at: string`\n  - `ended_at: string`\n  - `expires_at: string`\n  - `processing_status: 'in_progress' | 'canceling' | 'ended'`\n  - `request_counts: { canceled: number; errored: number; expired: number; processing: number; succeeded: number; }`\n  - `results_url: string`\n  - `type: 'message_batch'`\n\n### Example\n\n```typescript\nimport Sam from 'sam';\n\nconst client = new Sam();\n\nconst response = await client.messages.batches.cancelBeta('message_batch_id');\n\nconsole.log(response);\n```",
    perLanguage: {
      go: {
        method: 'client.Messages.Batches.CancelBeta',
        example:
          'package main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/DefinitelyATestOrg/sam-go"\n\t"github.com/DefinitelyATestOrg/sam-go/option"\n)\n\nfunc main() {\n\tclient := sam.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\tresponse, err := client.Messages.Batches.CancelBeta(\n\t\tcontext.TODO(),\n\t\t"message_batch_id",\n\t\tsam.MessageBatchCancelBetaParams{},\n\t)\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", response.ID)\n}\n',
      },
      http: {
        example: 'curl /api/v3/v1/messages/batches/$MESSAGE_BATCH_ID/cancel \\\n    -X POST',
      },
      java: {
        method: 'messages().batches().cancelBeta',
        example:
          'package me.elborai.api.example;\n\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\nimport me.elborai.api.models.messages.batches.BatchCancelBetaParams;\nimport me.elborai.api.models.messages.batches.BatchCancelBetaResponse;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        SamClient client = SamOkHttpClient.fromEnv();\n\n        BatchCancelBetaResponse response = client.messages().batches().cancelBeta("message_batch_id");\n    }\n}',
      },
      kotlin: {
        method: 'messages().batches().cancelBeta',
        example:
          'package me.elborai.api.example\n\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\nimport me.elborai.api.models.messages.batches.BatchCancelBetaParams\nimport me.elborai.api.models.messages.batches.BatchCancelBetaResponse\n\nfun main() {\n    val client: SamClient = SamOkHttpClient.fromEnv()\n\n    val response: BatchCancelBetaResponse = client.messages().batches().cancelBeta("message_batch_id")\n}',
      },
      python: {
        method: 'messages.batches.cancel_beta',
        example:
          'from sam import Sam\n\nclient = Sam()\nresponse = client.messages.batches.cancel_beta(\n    message_batch_id="message_batch_id",\n)\nprint(response.id)',
      },
      ruby: {
        method: 'messages.batches.cancel_beta',
        example:
          'require "sam"\n\nsam = Sam::Client.new(api_key: "My API Key")\n\nresponse = sam.messages.batches.cancel_beta("message_batch_id")\n\nputs(response)',
      },
      typescript: {
        method: 'client.messages.batches.cancelBeta',
        example:
          "import Sam from 'sam';\n\nconst client = new Sam();\n\nconst response = await client.messages.batches.cancelBeta('message_batch_id');\n\nconsole.log(response.id);",
      },
    },
  },
  {
    name: 'results_beta',
    endpoint: '/v1/messages/batches/{message_batch_id}/results?beta=true',
    httpMethod: 'get',
    summary: 'Retrieve Message Batch results',
    description:
      'Streams the results of a Message Batch as a `.jsonl` file.\n\nEach line in the file is a JSON object containing the result of a single request in the Message Batch. Results are not guaranteed to be in the same order as requests. Use the `custom_id` field to match results to requests.\n\nLearn more about the Message Batches API in our [user guide](/en/docs/build-with-claude/batch-processing)',
    stainlessPath: '(resource) messages.batches > (method) results_beta',
    qualified: 'client.messages.batches.resultsBeta',
    params: [
      'message_batch_id: string;',
      'anthropic-beta?: string[];',
      'anthropic-version?: string;',
      'x-api-key?: string;',
    ],
    response:
      "{ custom_id: string; result: { message: { id: string; content: object | object | object | object[]; model: string; role: 'assistant'; stop_reason: 'end_turn' | 'max_tokens' | 'stop_sequence' | 'tool_use'; stop_sequence: string; type: 'message'; usage: object; }; type: 'succeeded'; } | { error: { error: object | object | object | object | object | object | object | object | object; type: 'error'; }; type: 'errored'; } | { type: 'canceled'; } | { type: 'expired'; }; }",
    markdown:
      "## results_beta\n\n`client.messages.batches.resultsBeta(message_batch_id: string, anthropic-beta?: string[], anthropic-version?: string, x-api-key?: string): { custom_id: string; result: object | object | object | object; }`\n\n**get** `/v1/messages/batches/{message_batch_id}/results?beta=true`\n\nStreams the results of a Message Batch as a `.jsonl` file.\n\nEach line in the file is a JSON object containing the result of a single request in the Message Batch. Results are not guaranteed to be in the same order as requests. Use the `custom_id` field to match results to requests.\n\nLearn more about the Message Batches API in our [user guide](/en/docs/build-with-claude/batch-processing)\n\n### Parameters\n\n- `message_batch_id: string`\n  ID of the Message Batch.\n\n- `anthropic-beta?: string[]`\n  Optional header to specify the beta version(s) you want to use.\n\nTo use multiple betas, use a comma separated list like `beta1,beta2` or specify the header multiple times for each beta.\n\n- `anthropic-version?: string`\n  The version of the Anthropic API you want to use.\n\nRead more about versioning and our version history [here](https://docs.anthropic.com/en/api/versioning).\n\n- `x-api-key?: string`\n  Your unique API key for authentication.\n\nThis key is required in the header of all API requests, to authenticate your account and access Anthropic's services. Get your API key through the [Console](https://console.anthropic.com/settings/keys). Each key is scoped to a Workspace.\n\n### Returns\n\n- `{ custom_id: string; result: { message: { id: string; content: object | object | object | object[]; model: string; role: 'assistant'; stop_reason: 'end_turn' | 'max_tokens' | 'stop_sequence' | 'tool_use'; stop_sequence: string; type: 'message'; usage: object; }; type: 'succeeded'; } | { error: { error: object | object | object | object | object | object | object | object | object; type: 'error'; }; type: 'errored'; } | { type: 'canceled'; } | { type: 'expired'; }; }`\n  This is a single line in the response `.jsonl` file and does not represent the response as a whole.\n\n  - `custom_id: string`\n  - `result: { message: { id: string; content: { citations: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; text: string; type: 'text'; } | { id: string; input: object; name: string; type: 'tool_use'; } | { signature: string; thinking: string; type: 'thinking'; } | { data: string; type: 'redacted_thinking'; }[]; model: string; role: 'assistant'; stop_reason: 'end_turn' | 'max_tokens' | 'stop_sequence' | 'tool_use'; stop_sequence: string; type: 'message'; usage: { cache_creation_input_tokens: number; cache_read_input_tokens: number; input_tokens: number; output_tokens: number; }; }; type: 'succeeded'; } | { error: { error: { message: string; type: 'invalid_request_error'; } | { message: string; type: 'authentication_error'; } | { message: string; type: 'billing_error'; } | { message: string; type: 'permission_error'; } | { message: string; type: 'not_found_error'; } | { message: string; type: 'rate_limit_error'; } | { message: string; type: 'timeout_error'; } | { message: string; type: 'api_error'; } | { message: string; type: 'overloaded_error'; }; type: 'error'; }; type: 'errored'; } | { type: 'canceled'; } | { type: 'expired'; }`\n\n### Example\n\n```typescript\nimport Sam from 'sam';\n\nconst client = new Sam();\n\nconst stream = await client.messages.batches.resultsBeta('message_batch_id');\nfor await (const batchResultsBetaResponse of stream) {\n  console.log(batchResultsBetaResponse);\n}\n```",
    perLanguage: {
      go: {
        method: 'client.Messages.Batches.ResultsBeta',
        example:
          'package main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/DefinitelyATestOrg/sam-go"\n\t"github.com/DefinitelyATestOrg/sam-go/option"\n)\n\nfunc main() {\n\tclient := sam.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\tstream := client.Messages.Batches.ResultsBetaStreaming(\n\t\tcontext.TODO(),\n\t\t"message_batch_id",\n\t\tsam.MessageBatchResultsBetaParams{},\n\t)\n\tfor stream.Next() {\n\t\tfmt.Printf("%+v\\n", stream.Current())\n\t}\n\terr := stream.Err()\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n}\n',
      },
      http: {
        example: 'curl /api/v3/v1/messages/batches/$MESSAGE_BATCH_ID/results',
      },
      java: {
        method: 'messages().batches().resultsBetaStreaming',
        example:
          'package me.elborai.api.example;\n\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\nimport me.elborai.api.core.http.StreamResponse;\nimport me.elborai.api.models.messages.batches.BatchResultsBetaParams;\nimport me.elborai.api.models.messages.batches.BatchResultsBetaResponse;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        SamClient client = SamOkHttpClient.fromEnv();\n\n        StreamResponse<BatchResultsBetaResponse> response = client.messages().batches().resultsBetaStreaming("message_batch_id");\n    }\n}',
      },
      kotlin: {
        method: 'messages().batches().resultsBetaStreaming',
        example:
          'package me.elborai.api.example\n\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\nimport me.elborai.api.core.http.StreamResponse\nimport me.elborai.api.models.messages.batches.BatchResultsBetaParams\nimport me.elborai.api.models.messages.batches.BatchResultsBetaResponse\n\nfun main() {\n    val client: SamClient = SamOkHttpClient.fromEnv()\n\n    val response: StreamResponse<BatchResultsBetaResponse> = client.messages().batches().resultsBetaStreaming("message_batch_id")\n}',
      },
      python: {
        method: 'messages.batches.results_beta',
        example:
          'from sam import Sam\n\nclient = Sam()\nfor batch in client.messages.batches.results_beta(\n    message_batch_id="message_batch_id",\n):\n  print(batch)',
      },
      ruby: {
        method: 'messages.batches.results_beta',
        example:
          'require "sam"\n\nsam = Sam::Client.new(api_key: "My API Key")\n\nresponse = sam.messages.batches.results_beta("message_batch_id")\n\nputs(response)',
      },
      typescript: {
        method: 'client.messages.batches.resultsBeta',
        example:
          "import Sam from 'sam';\n\nconst client = new Sam();\n\nconst response = await client.messages.batches.resultsBeta('message_batch_id');\n\nconsole.log(response.custom_id);",
      },
    },
  },
  {
    name: 'retrieve',
    endpoint: '/v1/messages/batches/{message_batch_id}?beta=true',
    httpMethod: 'get',
    summary: 'Retrieve a Message Batch',
    description:
      'This endpoint is idempotent and can be used to poll for Message Batch completion. To access the results of a Message Batch, make a request to the `results_url` field in the response.\n\nLearn more about the Message Batches API in our [user guide](/en/docs/build-with-claude/batch-processing)',
    stainlessPath: '(resource) messages.batches.beta_true > (method) retrieve',
    qualified: 'client.messages.batches.betaTrue.retrieve',
    params: [
      'message_batch_id: string;',
      'anthropic-beta?: string[];',
      'anthropic-version?: string;',
      'x-api-key?: string;',
    ],
    response:
      "{ id: string; archived_at: string; cancel_initiated_at: string; created_at: string; ended_at: string; expires_at: string; processing_status: 'in_progress' | 'canceling' | 'ended'; request_counts: { canceled: number; errored: number; expired: number; processing: number; succeeded: number; }; results_url: string; type: 'message_batch'; }",
    markdown:
      "## retrieve\n\n`client.messages.batches.betaTrue.retrieve(message_batch_id: string, anthropic-beta?: string[], anthropic-version?: string, x-api-key?: string): { id: string; archived_at: string; cancel_initiated_at: string; created_at: string; ended_at: string; expires_at: string; processing_status: 'in_progress' | 'canceling' | 'ended'; request_counts: object; results_url: string; type: 'message_batch'; }`\n\n**get** `/v1/messages/batches/{message_batch_id}?beta=true`\n\nThis endpoint is idempotent and can be used to poll for Message Batch completion. To access the results of a Message Batch, make a request to the `results_url` field in the response.\n\nLearn more about the Message Batches API in our [user guide](/en/docs/build-with-claude/batch-processing)\n\n### Parameters\n\n- `message_batch_id: string`\n  ID of the Message Batch.\n\n- `anthropic-beta?: string[]`\n  Optional header to specify the beta version(s) you want to use.\n\nTo use multiple betas, use a comma separated list like `beta1,beta2` or specify the header multiple times for each beta.\n\n- `anthropic-version?: string`\n  The version of the Anthropic API you want to use.\n\nRead more about versioning and our version history [here](https://docs.anthropic.com/en/api/versioning).\n\n- `x-api-key?: string`\n  Your unique API key for authentication.\n\nThis key is required in the header of all API requests, to authenticate your account and access Anthropic's services. Get your API key through the [Console](https://console.anthropic.com/settings/keys). Each key is scoped to a Workspace.\n\n### Returns\n\n- `{ id: string; archived_at: string; cancel_initiated_at: string; created_at: string; ended_at: string; expires_at: string; processing_status: 'in_progress' | 'canceling' | 'ended'; request_counts: { canceled: number; errored: number; expired: number; processing: number; succeeded: number; }; results_url: string; type: 'message_batch'; }`\n\n  - `id: string`\n  - `archived_at: string`\n  - `cancel_initiated_at: string`\n  - `created_at: string`\n  - `ended_at: string`\n  - `expires_at: string`\n  - `processing_status: 'in_progress' | 'canceling' | 'ended'`\n  - `request_counts: { canceled: number; errored: number; expired: number; processing: number; succeeded: number; }`\n  - `results_url: string`\n  - `type: 'message_batch'`\n\n### Example\n\n```typescript\nimport Sam from 'sam';\n\nconst client = new Sam();\n\nconst betaTrue = await client.messages.batches.betaTrue.retrieve('message_batch_id');\n\nconsole.log(betaTrue);\n```",
    perLanguage: {
      go: {
        method: 'client.Messages.Batches.BetaTrue.Get',
        example:
          'package main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/DefinitelyATestOrg/sam-go"\n\t"github.com/DefinitelyATestOrg/sam-go/option"\n)\n\nfunc main() {\n\tclient := sam.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\tbetaTrue, err := client.Messages.Batches.BetaTrue.Get(\n\t\tcontext.TODO(),\n\t\t"message_batch_id",\n\t\tsam.MessageBatchBetaTrueGetParams{},\n\t)\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", betaTrue.ID)\n}\n',
      },
      http: {
        example: 'curl /api/v3/v1/messages/batches/$MESSAGE_BATCH_ID',
      },
      java: {
        method: 'messages().batches().betaTrue().retrieve',
        example:
          'package me.elborai.api.example;\n\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\nimport me.elborai.api.models.messages.batches.betatrue.BetaTrueRetrieveParams;\nimport me.elborai.api.models.messages.batches.betatrue.BetaTrueRetrieveResponse;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        SamClient client = SamOkHttpClient.fromEnv();\n\n        BetaTrueRetrieveResponse betaTrue = client.messages().batches().betaTrue().retrieve("message_batch_id");\n    }\n}',
      },
      kotlin: {
        method: 'messages().batches().betaTrue().retrieve',
        example:
          'package me.elborai.api.example\n\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\nimport me.elborai.api.models.messages.batches.betatrue.BetaTrueRetrieveParams\nimport me.elborai.api.models.messages.batches.betatrue.BetaTrueRetrieveResponse\n\nfun main() {\n    val client: SamClient = SamOkHttpClient.fromEnv()\n\n    val betaTrue: BetaTrueRetrieveResponse = client.messages().batches().betaTrue().retrieve("message_batch_id")\n}',
      },
      python: {
        method: 'messages.batches.beta_true.retrieve',
        example:
          'from sam import Sam\n\nclient = Sam()\nbeta_true = client.messages.batches.beta_true.retrieve(\n    message_batch_id="message_batch_id",\n)\nprint(beta_true.id)',
      },
      ruby: {
        method: 'messages.batches.beta_true.retrieve',
        example:
          'require "sam"\n\nsam = Sam::Client.new(api_key: "My API Key")\n\nbeta_true = sam.messages.batches.beta_true.retrieve("message_batch_id")\n\nputs(beta_true)',
      },
      typescript: {
        method: 'client.messages.batches.betaTrue.retrieve',
        example:
          "import Sam from 'sam';\n\nconst client = new Sam();\n\nconst betaTrue = await client.messages.batches.betaTrue.retrieve('message_batch_id');\n\nconsole.log(betaTrue.id);",
      },
    },
  },
  {
    name: 'delete',
    endpoint: '/v1/messages/batches/{message_batch_id}?beta=true',
    httpMethod: 'delete',
    summary: 'Delete a Message Batch',
    description:
      "Delete a Message Batch.\n\nMessage Batches can only be deleted once they've finished processing. If you'd like to delete an in-progress batch, you must first cancel it.\n\nLearn more about the Message Batches API in our [user guide](/en/docs/build-with-claude/batch-processing)",
    stainlessPath: '(resource) messages.batches.beta_true > (method) delete',
    qualified: 'client.messages.batches.betaTrue.delete',
    params: [
      'message_batch_id: string;',
      'anthropic-beta?: string[];',
      'anthropic-version?: string;',
      'x-api-key?: string;',
    ],
    response: "{ id: string; type: 'message_batch_deleted'; }",
    markdown:
      "## delete\n\n`client.messages.batches.betaTrue.delete(message_batch_id: string, anthropic-beta?: string[], anthropic-version?: string, x-api-key?: string): { id: string; type: 'message_batch_deleted'; }`\n\n**delete** `/v1/messages/batches/{message_batch_id}?beta=true`\n\nDelete a Message Batch.\n\nMessage Batches can only be deleted once they've finished processing. If you'd like to delete an in-progress batch, you must first cancel it.\n\nLearn more about the Message Batches API in our [user guide](/en/docs/build-with-claude/batch-processing)\n\n### Parameters\n\n- `message_batch_id: string`\n  ID of the Message Batch.\n\n- `anthropic-beta?: string[]`\n  Optional header to specify the beta version(s) you want to use.\n\nTo use multiple betas, use a comma separated list like `beta1,beta2` or specify the header multiple times for each beta.\n\n- `anthropic-version?: string`\n  The version of the Anthropic API you want to use.\n\nRead more about versioning and our version history [here](https://docs.anthropic.com/en/api/versioning).\n\n- `x-api-key?: string`\n  Your unique API key for authentication.\n\nThis key is required in the header of all API requests, to authenticate your account and access Anthropic's services. Get your API key through the [Console](https://console.anthropic.com/settings/keys). Each key is scoped to a Workspace.\n\n### Returns\n\n- `{ id: string; type: 'message_batch_deleted'; }`\n\n  - `id: string`\n  - `type: 'message_batch_deleted'`\n\n### Example\n\n```typescript\nimport Sam from 'sam';\n\nconst client = new Sam();\n\nconst betaTrue = await client.messages.batches.betaTrue.delete('message_batch_id');\n\nconsole.log(betaTrue);\n```",
    perLanguage: {
      go: {
        method: 'client.Messages.Batches.BetaTrue.Delete',
        example:
          'package main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/DefinitelyATestOrg/sam-go"\n\t"github.com/DefinitelyATestOrg/sam-go/option"\n)\n\nfunc main() {\n\tclient := sam.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\tbetaTrue, err := client.Messages.Batches.BetaTrue.Delete(\n\t\tcontext.TODO(),\n\t\t"message_batch_id",\n\t\tsam.MessageBatchBetaTrueDeleteParams{},\n\t)\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", betaTrue.ID)\n}\n',
      },
      http: {
        example: 'curl /api/v3/v1/messages/batches/$MESSAGE_BATCH_ID \\\n    -X DELETE',
      },
      java: {
        method: 'messages().batches().betaTrue().delete',
        example:
          'package me.elborai.api.example;\n\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\nimport me.elborai.api.models.messages.batches.betatrue.BetaTrueDeleteParams;\nimport me.elborai.api.models.messages.batches.betatrue.BetaTrueDeleteResponse;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        SamClient client = SamOkHttpClient.fromEnv();\n\n        BetaTrueDeleteResponse betaTrue = client.messages().batches().betaTrue().delete("message_batch_id");\n    }\n}',
      },
      kotlin: {
        method: 'messages().batches().betaTrue().delete',
        example:
          'package me.elborai.api.example\n\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\nimport me.elborai.api.models.messages.batches.betatrue.BetaTrueDeleteParams\nimport me.elborai.api.models.messages.batches.betatrue.BetaTrueDeleteResponse\n\nfun main() {\n    val client: SamClient = SamOkHttpClient.fromEnv()\n\n    val betaTrue: BetaTrueDeleteResponse = client.messages().batches().betaTrue().delete("message_batch_id")\n}',
      },
      python: {
        method: 'messages.batches.beta_true.delete',
        example:
          'from sam import Sam\n\nclient = Sam()\nbeta_true = client.messages.batches.beta_true.delete(\n    message_batch_id="message_batch_id",\n)\nprint(beta_true.id)',
      },
      ruby: {
        method: 'messages.batches.beta_true.delete',
        example:
          'require "sam"\n\nsam = Sam::Client.new(api_key: "My API Key")\n\nbeta_true = sam.messages.batches.beta_true.delete("message_batch_id")\n\nputs(beta_true)',
      },
      typescript: {
        method: 'client.messages.batches.betaTrue.delete',
        example:
          "import Sam from 'sam';\n\nconst client = new Sam();\n\nconst betaTrue = await client.messages.batches.betaTrue.delete('message_batch_id');\n\nconsole.log(betaTrue.id);",
      },
    },
  },
  {
    name: 'create',
    endpoint: '/v1/messages/batches?beta=true',
    httpMethod: 'post',
    summary: 'Create a Message Batch',
    description:
      'Send a batch of Message creation requests.\n\nThe Message Batches API can be used to process multiple Messages API requests at once. Once a Message Batch is created, it begins processing immediately. Batches can take up to 24 hours to complete.\n\nLearn more about the Message Batches API in our [user guide](/en/docs/build-with-claude/batch-processing)',
    stainlessPath: '(resource) messages.batches_beta_true > (method) create',
    qualified: 'client.messages.batchesBetaTrue.create',
    params: [
      "requests: { custom_id: string; params: { max_tokens: number; messages: { content: string | { text: string; type: 'text'; cache_control?: object; citations?: object | object | object[]; } | { source: object | object; type: 'image'; cache_control?: object; } | { id: string; input: object; name: string; type: 'tool_use'; cache_control?: object; } | { tool_use_id: string; type: 'tool_result'; cache_control?: object; content?: string | object | object[]; is_error?: boolean; } | { source: object | object | object | object; type: 'document'; cache_control?: object; citations?: object; context?: string; title?: string; } | { signature: string; thinking: string; type: 'thinking'; } | { data: string; type: 'redacted_thinking'; }[]; role: 'user' | 'assistant'; }[]; model: string; metadata?: { user_id?: string; }; stop_sequences?: string[]; stream?: boolean; system?: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; }[]; temperature?: number; thinking?: { budget_tokens: number; type: 'enabled'; } | { type: 'disabled'; }; tool_choice?: { type: 'auto'; disable_parallel_tool_use?: boolean; } | { type: 'any'; disable_parallel_tool_use?: boolean; } | { name: string; type: 'tool'; disable_parallel_tool_use?: boolean; } | { type: 'none'; }; tools?: { input_schema: { type: 'object'; properties?: object; }; name: string; cache_control?: { type: 'ephemeral'; }; description?: string; type?: 'custom'; } | { display_height_px: number; display_width_px: number; name: 'computer'; type: 'computer_20241022'; cache_control?: { type: 'ephemeral'; }; display_number?: number; } | { name: 'bash'; type: 'bash_20241022'; cache_control?: { type: 'ephemeral'; }; } | { name: 'str_replace_editor'; type: 'text_editor_20241022'; cache_control?: { type: 'ephemeral'; }; } | { display_height_px: number; display_width_px: number; name: 'computer'; type: 'computer_20250124'; cache_control?: { type: 'ephemeral'; }; display_number?: number; } | { name: 'bash'; type: 'bash_20250124'; cache_control?: { type: 'ephemeral'; }; } | { name: 'str_replace_editor'; type: 'text_editor_20250124'; cache_control?: { type: 'ephemeral'; }; }[]; top_k?: number; top_p?: number; }; }[];",
      'anthropic-beta?: string[];',
      'anthropic-version?: string;',
      'x-api-key?: string;',
    ],
    response:
      "{ id: string; archived_at: string; cancel_initiated_at: string; created_at: string; ended_at: string; expires_at: string; processing_status: 'in_progress' | 'canceling' | 'ended'; request_counts: { canceled: number; errored: number; expired: number; processing: number; succeeded: number; }; results_url: string; type: 'message_batch'; }",
    markdown:
      "## create\n\n`client.messages.batchesBetaTrue.create(requests: { custom_id: string; params: { max_tokens: number; messages: object[]; model: string; metadata?: object; stop_sequences?: string[]; stream?: boolean; system?: string | object[]; temperature?: number; thinking?: object | object; tool_choice?: object | object | object | object; tools?: object | object | object | object | object | object | object[]; top_k?: number; top_p?: number; }; }[], anthropic-beta?: string[], anthropic-version?: string, x-api-key?: string): { id: string; archived_at: string; cancel_initiated_at: string; created_at: string; ended_at: string; expires_at: string; processing_status: 'in_progress' | 'canceling' | 'ended'; request_counts: object; results_url: string; type: 'message_batch'; }`\n\n**post** `/v1/messages/batches?beta=true`\n\nSend a batch of Message creation requests.\n\nThe Message Batches API can be used to process multiple Messages API requests at once. Once a Message Batch is created, it begins processing immediately. Batches can take up to 24 hours to complete.\n\nLearn more about the Message Batches API in our [user guide](/en/docs/build-with-claude/batch-processing)\n\n### Parameters\n\n- `requests: { custom_id: string; params: { max_tokens: number; messages: { content: string | { text: string; type: 'text'; cache_control?: object; citations?: object | object | object[]; } | { source: object | object; type: 'image'; cache_control?: object; } | { id: string; input: object; name: string; type: 'tool_use'; cache_control?: object; } | { tool_use_id: string; type: 'tool_result'; cache_control?: object; content?: string | object | object[]; is_error?: boolean; } | { source: object | object | object | object; type: 'document'; cache_control?: object; citations?: object; context?: string; title?: string; } | { signature: string; thinking: string; type: 'thinking'; } | { data: string; type: 'redacted_thinking'; }[]; role: 'user' | 'assistant'; }[]; model: string; metadata?: { user_id?: string; }; stop_sequences?: string[]; stream?: boolean; system?: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; }[]; temperature?: number; thinking?: { budget_tokens: number; type: 'enabled'; } | { type: 'disabled'; }; tool_choice?: { type: 'auto'; disable_parallel_tool_use?: boolean; } | { type: 'any'; disable_parallel_tool_use?: boolean; } | { name: string; type: 'tool'; disable_parallel_tool_use?: boolean; } | { type: 'none'; }; tools?: { input_schema: { type: 'object'; properties?: object; }; name: string; cache_control?: { type: 'ephemeral'; }; description?: string; type?: 'custom'; } | { display_height_px: number; display_width_px: number; name: 'computer'; type: 'computer_20241022'; cache_control?: { type: 'ephemeral'; }; display_number?: number; } | { name: 'bash'; type: 'bash_20241022'; cache_control?: { type: 'ephemeral'; }; } | { name: 'str_replace_editor'; type: 'text_editor_20241022'; cache_control?: { type: 'ephemeral'; }; } | { display_height_px: number; display_width_px: number; name: 'computer'; type: 'computer_20250124'; cache_control?: { type: 'ephemeral'; }; display_number?: number; } | { name: 'bash'; type: 'bash_20250124'; cache_control?: { type: 'ephemeral'; }; } | { name: 'str_replace_editor'; type: 'text_editor_20250124'; cache_control?: { type: 'ephemeral'; }; }[]; top_k?: number; top_p?: number; }; }[]`\n  List of requests for prompt completion. Each is an individual request to create a Message.\n\n- `anthropic-beta?: string[]`\n  Optional header to specify the beta version(s) you want to use.\n\nTo use multiple betas, use a comma separated list like `beta1,beta2` or specify the header multiple times for each beta.\n\n- `anthropic-version?: string`\n  The version of the Anthropic API you want to use.\n\nRead more about versioning and our version history [here](https://docs.anthropic.com/en/api/versioning).\n\n- `x-api-key?: string`\n  Your unique API key for authentication.\n\nThis key is required in the header of all API requests, to authenticate your account and access Anthropic's services. Get your API key through the [Console](https://console.anthropic.com/settings/keys). Each key is scoped to a Workspace.\n\n### Returns\n\n- `{ id: string; archived_at: string; cancel_initiated_at: string; created_at: string; ended_at: string; expires_at: string; processing_status: 'in_progress' | 'canceling' | 'ended'; request_counts: { canceled: number; errored: number; expired: number; processing: number; succeeded: number; }; results_url: string; type: 'message_batch'; }`\n\n  - `id: string`\n  - `archived_at: string`\n  - `cancel_initiated_at: string`\n  - `created_at: string`\n  - `ended_at: string`\n  - `expires_at: string`\n  - `processing_status: 'in_progress' | 'canceling' | 'ended'`\n  - `request_counts: { canceled: number; errored: number; expired: number; processing: number; succeeded: number; }`\n  - `results_url: string`\n  - `type: 'message_batch'`\n\n### Example\n\n```typescript\nimport Sam from 'sam';\n\nconst client = new Sam();\n\nconst batchesBetaTrue = await client.messages.batchesBetaTrue.create({ requests: [{\n  custom_id: 'my-custom-id-1',\n  params: {\n  max_tokens: 1024,\n  messages: [{ content: 'Hello, world', role: 'user' }],\n  model: 'claude-3-7-sonnet-20250219',\n},\n}] });\n\nconsole.log(batchesBetaTrue);\n```",
    perLanguage: {
      go: {
        method: 'client.Messages.BatchesBetaTrue.New',
        example:
          'package main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/DefinitelyATestOrg/sam-go"\n\t"github.com/DefinitelyATestOrg/sam-go/option"\n\t"github.com/DefinitelyATestOrg/sam-go/shared"\n)\n\nfunc main() {\n\tclient := sam.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\tbatchesBetaTrue, err := client.Messages.BatchesBetaTrue.New(context.TODO(), sam.MessageBatchesBetaTrueNewParams{\n\t\tRequests: sam.F([]sam.MessageBatchesBetaTrueNewParamsRequest{{\n\t\t\tCustomID: sam.F("my-custom-id-1"),\n\t\t\tParams: sam.F(sam.MessageBatchesBetaTrueNewParamsRequestsParams{\n\t\t\t\tMaxTokens: sam.F(int64(1024)),\n\t\t\t\tMessages: sam.F([]sam.MessageBatchesBetaTrueNewParamsRequestsParamsMessage{{\n\t\t\t\t\tContent: sam.F[sam.MessageBatchesBetaTrueNewParamsRequestsParamsMessagesContentUnion](shared.UnionString("Hello, world")),\n\t\t\t\t\tRole:    sam.F(sam.MessageBatchesBetaTrueNewParamsRequestsParamsMessagesRoleUser),\n\t\t\t\t}}),\n\t\t\t\tModel: sam.F("claude-3-7-sonnet-20250219"),\n\t\t\t}),\n\t\t}}),\n\t})\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", batchesBetaTrue.ID)\n}\n',
      },
      http: {
        example:
          'curl /api/v3/v1/messages/batches \\\n    -H \'Content-Type: application/json\' \\\n    -d \'{\n          "requests": [\n            {\n              "custom_id": "my-custom-id-1",\n              "params": {\n                "max_tokens": 1024,\n                "messages": [\n                  {\n                    "content": "Hello, world",\n                    "role": "user"\n                  }\n                ],\n                "model": "claude-3-7-sonnet-20250219"\n              }\n            }\n          ]\n        }\'',
      },
      java: {
        method: 'messages().batchesBetaTrue().create',
        example:
          'package me.elborai.api.example;\n\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\nimport me.elborai.api.models.messages.batchesbetatrue.BatchesBetaTrueCreateParams;\nimport me.elborai.api.models.messages.batchesbetatrue.BatchesBetaTrueCreateResponse;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        SamClient client = SamOkHttpClient.fromEnv();\n\n        BatchesBetaTrueCreateParams params = BatchesBetaTrueCreateParams.builder()\n            .addRequest(BatchesBetaTrueCreateParams.Request.builder()\n                .customId("my-custom-id-1")\n                .params(BatchesBetaTrueCreateParams.Request.Params.builder()\n                    .maxTokens(1024L)\n                    .addMessage(BatchesBetaTrueCreateParams.Request.Params.Message.builder()\n                        .content("Hello, world")\n                        .role(BatchesBetaTrueCreateParams.Request.Params.Message.Role.USER)\n                        .build())\n                    .model("claude-3-7-sonnet-20250219")\n                    .build())\n                .build())\n            .build();\n        BatchesBetaTrueCreateResponse batchesBetaTrue = client.messages().batchesBetaTrue().create(params);\n    }\n}',
      },
      kotlin: {
        method: 'messages().batchesBetaTrue().create',
        example:
          'package me.elborai.api.example\n\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\nimport me.elborai.api.models.messages.batchesbetatrue.BatchesBetaTrueCreateParams\nimport me.elborai.api.models.messages.batchesbetatrue.BatchesBetaTrueCreateResponse\n\nfun main() {\n    val client: SamClient = SamOkHttpClient.fromEnv()\n\n    val params: BatchesBetaTrueCreateParams = BatchesBetaTrueCreateParams.builder()\n        .addRequest(BatchesBetaTrueCreateParams.Request.builder()\n            .customId("my-custom-id-1")\n            .params(BatchesBetaTrueCreateParams.Request.Params.builder()\n                .maxTokens(1024L)\n                .addMessage(BatchesBetaTrueCreateParams.Request.Params.Message.builder()\n                    .content("Hello, world")\n                    .role(BatchesBetaTrueCreateParams.Request.Params.Message.Role.USER)\n                    .build())\n                .model("claude-3-7-sonnet-20250219")\n                .build())\n            .build())\n        .build()\n    val batchesBetaTrue: BatchesBetaTrueCreateResponse = client.messages().batchesBetaTrue().create(params)\n}',
      },
      python: {
        method: 'messages.batches_beta_true.create',
        example:
          'from sam import Sam\n\nclient = Sam()\nbatches_beta_true = client.messages.batches_beta_true.create(\n    requests=[{\n        "custom_id": "my-custom-id-1",\n        "params": {\n            "max_tokens": 1024,\n            "messages": [{\n                "content": "Hello, world",\n                "role": "user",\n            }],\n            "model": "claude-3-7-sonnet-20250219",\n        },\n    }],\n)\nprint(batches_beta_true.id)',
      },
      ruby: {
        method: 'messages.batches_beta_true.create',
        example:
          'require "sam"\n\nsam = Sam::Client.new(api_key: "My API Key")\n\nbatches_beta_true = sam.messages.batches_beta_true.create(\n  requests: [\n    {\n      custom_id: "my-custom-id-1",\n      params: {max_tokens: 1024, messages: [{content: "Hello, world", role: :user}], model: "claude-3-7-sonnet-20250219"}\n    }\n  ]\n)\n\nputs(batches_beta_true)',
      },
      typescript: {
        method: 'client.messages.batchesBetaTrue.create',
        example:
          "import Sam from 'sam';\n\nconst client = new Sam();\n\nconst batchesBetaTrue = await client.messages.batchesBetaTrue.create({\n  requests: [\n    {\n      custom_id: 'my-custom-id-1',\n      params: {\n        max_tokens: 1024,\n        messages: [{ content: 'Hello, world', role: 'user' }],\n        model: 'claude-3-7-sonnet-20250219',\n      },\n    },\n  ],\n});\n\nconsole.log(batchesBetaTrue.id);",
      },
    },
  },
  {
    name: 'list',
    endpoint: '/v1/messages/batches?beta=true',
    httpMethod: 'get',
    summary: 'List Message Batches',
    description:
      'List all Message Batches within a Workspace. Most recently created batches are returned first.\n\nLearn more about the Message Batches API in our [user guide](/en/docs/build-with-claude/batch-processing)',
    stainlessPath: '(resource) messages.batches_beta_true > (method) list',
    qualified: 'client.messages.batchesBetaTrue.list',
    params: [
      'after_id?: string;',
      'before_id?: string;',
      'limit?: number;',
      'anthropic-beta?: string[];',
      'anthropic-version?: string;',
      'x-api-key?: string;',
    ],
    response:
      "{ data: { id: string; archived_at: string; cancel_initiated_at: string; created_at: string; ended_at: string; expires_at: string; processing_status: 'in_progress' | 'canceling' | 'ended'; request_counts: { canceled: number; errored: number; expired: number; processing: number; succeeded: number; }; results_url: string; type: 'message_batch'; }[]; first_id: string; has_more: boolean; last_id: string; }",
    markdown:
      "## list\n\n`client.messages.batchesBetaTrue.list(after_id?: string, before_id?: string, limit?: number, anthropic-beta?: string[], anthropic-version?: string, x-api-key?: string): { data: object[]; first_id: string; has_more: boolean; last_id: string; }`\n\n**get** `/v1/messages/batches?beta=true`\n\nList all Message Batches within a Workspace. Most recently created batches are returned first.\n\nLearn more about the Message Batches API in our [user guide](/en/docs/build-with-claude/batch-processing)\n\n### Parameters\n\n- `after_id?: string`\n  ID of the object to use as a cursor for pagination. When provided, returns the page of results immediately after this object.\n\n- `before_id?: string`\n  ID of the object to use as a cursor for pagination. When provided, returns the page of results immediately before this object.\n\n- `limit?: number`\n  Number of items to return per page.\n\nDefaults to `20`. Ranges from `1` to `1000`.\n\n- `anthropic-beta?: string[]`\n  Optional header to specify the beta version(s) you want to use.\n\nTo use multiple betas, use a comma separated list like `beta1,beta2` or specify the header multiple times for each beta.\n\n- `anthropic-version?: string`\n  The version of the Anthropic API you want to use.\n\nRead more about versioning and our version history [here](https://docs.anthropic.com/en/api/versioning).\n\n- `x-api-key?: string`\n  Your unique API key for authentication.\n\nThis key is required in the header of all API requests, to authenticate your account and access Anthropic's services. Get your API key through the [Console](https://console.anthropic.com/settings/keys). Each key is scoped to a Workspace.\n\n### Returns\n\n- `{ data: { id: string; archived_at: string; cancel_initiated_at: string; created_at: string; ended_at: string; expires_at: string; processing_status: 'in_progress' | 'canceling' | 'ended'; request_counts: { canceled: number; errored: number; expired: number; processing: number; succeeded: number; }; results_url: string; type: 'message_batch'; }[]; first_id: string; has_more: boolean; last_id: string; }`\n\n  - `data: { id: string; archived_at: string; cancel_initiated_at: string; created_at: string; ended_at: string; expires_at: string; processing_status: 'in_progress' | 'canceling' | 'ended'; request_counts: { canceled: number; errored: number; expired: number; processing: number; succeeded: number; }; results_url: string; type: 'message_batch'; }[]`\n  - `first_id: string`\n  - `has_more: boolean`\n  - `last_id: string`\n\n### Example\n\n```typescript\nimport Sam from 'sam';\n\nconst client = new Sam();\n\nconst batchesBetaTrues = await client.messages.batchesBetaTrue.list();\n\nconsole.log(batchesBetaTrues);\n```",
    perLanguage: {
      go: {
        method: 'client.Messages.BatchesBetaTrue.List',
        example:
          'package main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/DefinitelyATestOrg/sam-go"\n\t"github.com/DefinitelyATestOrg/sam-go/option"\n)\n\nfunc main() {\n\tclient := sam.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\tbatchesBetaTrues, err := client.Messages.BatchesBetaTrue.List(context.TODO(), sam.MessageBatchesBetaTrueListParams{})\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", batchesBetaTrues.FirstID)\n}\n',
      },
      http: {
        example: 'curl /api/v3/v1/messages/batches',
      },
      java: {
        method: 'messages().batchesBetaTrue().list',
        example:
          'package me.elborai.api.example;\n\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\nimport me.elborai.api.models.messages.batchesbetatrue.BatchesBetaTrueListParams;\nimport me.elborai.api.models.messages.batchesbetatrue.BatchesBetaTrueListResponse;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        SamClient client = SamOkHttpClient.fromEnv();\n\n        BatchesBetaTrueListResponse batchesBetaTrues = client.messages().batchesBetaTrue().list();\n    }\n}',
      },
      kotlin: {
        method: 'messages().batchesBetaTrue().list',
        example:
          'package me.elborai.api.example\n\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\nimport me.elborai.api.models.messages.batchesbetatrue.BatchesBetaTrueListParams\nimport me.elborai.api.models.messages.batchesbetatrue.BatchesBetaTrueListResponse\n\nfun main() {\n    val client: SamClient = SamOkHttpClient.fromEnv()\n\n    val batchesBetaTrues: BatchesBetaTrueListResponse = client.messages().batchesBetaTrue().list()\n}',
      },
      python: {
        method: 'messages.batches_beta_true.list',
        example:
          'from sam import Sam\n\nclient = Sam()\nbatches_beta_trues = client.messages.batches_beta_true.list()\nprint(batches_beta_trues.first_id)',
      },
      ruby: {
        method: 'messages.batches_beta_true.list',
        example:
          'require "sam"\n\nsam = Sam::Client.new(api_key: "My API Key")\n\nbatches_beta_trues = sam.messages.batches_beta_true.list\n\nputs(batches_beta_trues)',
      },
      typescript: {
        method: 'client.messages.batchesBetaTrue.list',
        example:
          "import Sam from 'sam';\n\nconst client = new Sam();\n\nconst batchesBetaTrues = await client.messages.batchesBetaTrue.list();\n\nconsole.log(batchesBetaTrues.first_id);",
      },
    },
  },
  {
    name: 'create',
    endpoint: '/v1/complete',
    httpMethod: 'post',
    summary: 'Create a Text Completion',
    description:
      '[Legacy] Create a Text Completion.\n\nThe Text Completions API is a legacy API. We recommend using the [Messages API](https://docs.anthropic.com/en/api/messages) going forward.\n\nFuture models and features will not be compatible with Text Completions. See our [migration guide](https://docs.anthropic.com/en/api/migrating-from-text-completions-to-messages) for guidance in migrating from Text Completions to Messages.',
    stainlessPath: '(resource) complete > (method) create',
    qualified: 'client.complete.create',
    params: [
      'max_tokens_to_sample: number;',
      'model: string;',
      'prompt: string;',
      'metadata?: { user_id?: string; };',
      'stop_sequences?: string[];',
      'stream?: boolean;',
      'temperature?: number;',
      'top_k?: number;',
      'top_p?: number;',
      'anthropic-version?: string;',
      'x-api-key?: string;',
    ],
    response: "{ id: string; completion: string; model: string; stop_reason: string; type: 'completion'; }",
    markdown:
      "## create\n\n`client.complete.create(max_tokens_to_sample: number, model: string, prompt: string, metadata?: { user_id?: string; }, stop_sequences?: string[], stream?: boolean, temperature?: number, top_k?: number, top_p?: number, anthropic-version?: string, x-api-key?: string): { id: string; completion: string; model: string; stop_reason: string; type: 'completion'; }`\n\n**post** `/v1/complete`\n\n[Legacy] Create a Text Completion.\n\nThe Text Completions API is a legacy API. We recommend using the [Messages API](https://docs.anthropic.com/en/api/messages) going forward.\n\nFuture models and features will not be compatible with Text Completions. See our [migration guide](https://docs.anthropic.com/en/api/migrating-from-text-completions-to-messages) for guidance in migrating from Text Completions to Messages.\n\n### Parameters\n\n- `max_tokens_to_sample: number`\n  The maximum number of tokens to generate before stopping.\n\nNote that our models may stop _before_ reaching this maximum. This parameter only specifies the absolute maximum number of tokens to generate.\n\n- `model: string`\n  The model that will complete your prompt.\n\nSee [models](https://docs.anthropic.com/en/docs/models-overview) for additional details and options.\n\n- `prompt: string`\n  The prompt that you want Claude to complete.\n\nFor proper response generation you will need to format your prompt using alternating `\\n\\nHuman:` and `\\n\\nAssistant:` conversational turns. For example:\n\n```\n\"\\n\\nHuman: {userQuestion}\\n\\nAssistant:\"\n```\n\nSee [prompt validation](https://docs.anthropic.com/en/api/prompt-validation) and our guide to [prompt design](https://docs.anthropic.com/en/docs/intro-to-prompting) for more details.\n\n- `metadata?: { user_id?: string; }`\n  An object describing metadata about the request.\n  - `user_id?: string`\n    An external identifier for the user who is associated with the request.\n\nThis should be a uuid, hash value, or other opaque identifier. Anthropic may use this id to help detect abuse. Do not include any identifying information such as name, email address, or phone number.\n\n- `stop_sequences?: string[]`\n  Sequences that will cause the model to stop generating.\n\nOur models stop on `\"\\n\\nHuman:\"`, and may include additional built-in stop sequences in the future. By providing the stop_sequences parameter, you may include additional strings that will cause the model to stop generating.\n\n- `stream?: boolean`\n  Whether to incrementally stream the response using server-sent events.\n\nSee [streaming](https://docs.anthropic.com/en/api/streaming) for details.\n\n- `temperature?: number`\n  Amount of randomness injected into the response.\n\nDefaults to `1.0`. Ranges from `0.0` to `1.0`. Use `temperature` closer to `0.0` for analytical / multiple choice, and closer to `1.0` for creative and generative tasks.\n\nNote that even with `temperature` of `0.0`, the results will not be fully deterministic.\n\n- `top_k?: number`\n  Only sample from the top K options for each subsequent token.\n\nUsed to remove \"long tail\" low probability responses. [Learn more technical details here](https://towardsdatascience.com/how-to-sample-from-language-models-682bceb97277).\n\nRecommended for advanced use cases only. You usually only need to use `temperature`.\n\n- `top_p?: number`\n  Use nucleus sampling.\n\nIn nucleus sampling, we compute the cumulative distribution over all the options for each subsequent token in decreasing probability order and cut it off once it reaches a particular probability specified by `top_p`. You should either alter `temperature` or `top_p`, but not both.\n\nRecommended for advanced use cases only. You usually only need to use `temperature`.\n\n- `anthropic-version?: string`\n  The version of the Anthropic API you want to use.\n\nRead more about versioning and our version history [here](https://docs.anthropic.com/en/api/versioning).\n\n- `x-api-key?: string`\n  Your unique API key for authentication.\n\nThis key is required in the header of all API requests, to authenticate your account and access Anthropic's services. Get your API key through the [Console](https://console.anthropic.com/settings/keys). Each key is scoped to a Workspace.\n\n### Returns\n\n- `{ id: string; completion: string; model: string; stop_reason: string; type: 'completion'; }`\n\n  - `id: string`\n  - `completion: string`\n  - `model: string`\n  - `stop_reason: string`\n  - `type: 'completion'`\n\n### Example\n\n```typescript\nimport Sam from 'sam';\n\nconst client = new Sam();\n\nconst complete = await client.complete.create({\n  max_tokens_to_sample: 256,\n  model: 'claude-2.1',\n  prompt: '\\n\\nHuman: Hello, world!\\n\\nAssistant:',\n});\n\nconsole.log(complete);\n```",
    perLanguage: {
      go: {
        method: 'client.Complete.New',
        example:
          'package main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/DefinitelyATestOrg/sam-go"\n\t"github.com/DefinitelyATestOrg/sam-go/option"\n)\n\nfunc main() {\n\tclient := sam.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\tcomplete, err := client.Complete.New(context.TODO(), sam.CompleteNewParams{\n\t\tMaxTokensToSample: sam.F(int64(256)),\n\t\tModel:             sam.F("claude-2.1"),\n\t\tPrompt:            sam.F("\\n\\nHuman: Hello, world!\\n\\nAssistant:"),\n\t})\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", complete.ID)\n}\n',
      },
      http: {
        example:
          'curl /api/v3/v1/complete \\\n    -H \'Content-Type: application/json\' \\\n    -d \'{\n          "max_tokens_to_sample": 256,\n          "model": "claude-2.1",\n          "prompt": "\\\\n\\\\nHuman: Hello, world!\\\\n\\\\nAssistant:"\n        }\'',
      },
      java: {
        method: 'complete().create',
        example:
          'package me.elborai.api.example;\n\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\nimport me.elborai.api.models.complete.CompleteCreateParams;\nimport me.elborai.api.models.complete.CompleteCreateResponse;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        SamClient client = SamOkHttpClient.fromEnv();\n\n        CompleteCreateParams params = CompleteCreateParams.builder()\n            .maxTokensToSample(256L)\n            .model("claude-2.1")\n            .prompt("\\n\\nHuman: Hello, world!\\n\\nAssistant:")\n            .build();\n        CompleteCreateResponse complete = client.complete().create(params);\n    }\n}',
      },
      kotlin: {
        method: 'complete().create',
        example:
          'package me.elborai.api.example\n\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\nimport me.elborai.api.models.complete.CompleteCreateParams\nimport me.elborai.api.models.complete.CompleteCreateResponse\n\nfun main() {\n    val client: SamClient = SamOkHttpClient.fromEnv()\n\n    val params: CompleteCreateParams = CompleteCreateParams.builder()\n        .maxTokensToSample(256L)\n        .model("claude-2.1")\n        .prompt("\\n\\nHuman: Hello, world!\\n\\nAssistant:")\n        .build()\n    val complete: CompleteCreateResponse = client.complete().create(params)\n}',
      },
      python: {
        method: 'complete.create',
        example:
          'from sam import Sam\n\nclient = Sam()\ncomplete = client.complete.create(\n    max_tokens_to_sample=256,\n    model="claude-2.1",\n    prompt="\\n\\nHuman: Hello, world!\\n\\nAssistant:",\n)\nprint(complete.id)',
      },
      ruby: {
        method: 'complete.create',
        example:
          'require "sam"\n\nsam = Sam::Client.new(api_key: "My API Key")\n\ncomplete = sam.complete.create(\n  max_tokens_to_sample: 256,\n  model: "claude-2.1",\n  prompt: "\\n\\nHuman: Hello, world!\\n\\nAssistant:"\n)\n\nputs(complete)',
      },
      typescript: {
        method: 'client.complete.create',
        example:
          "import Sam from 'sam';\n\nconst client = new Sam();\n\nconst complete = await client.complete.create({\n  max_tokens_to_sample: 256,\n  model: 'claude-2.1',\n  prompt: '\\n\\nHuman: Hello, world!\\n\\nAssistant:',\n});\n\nconsole.log(complete.id);",
      },
    },
  },
  {
    name: 'list',
    endpoint: '/v1/models',
    httpMethod: 'get',
    summary: 'List Models',
    description:
      'List available models.\n\nThe Models API response can be used to determine which models are available for use in the API. More recently released models are listed first.',
    stainlessPath: '(resource) models > (method) list',
    qualified: 'client.models.list',
    params: [
      'after_id?: string;',
      'before_id?: string;',
      'limit?: number;',
      'anthropic-version?: string;',
      'x-api-key?: string;',
    ],
    response:
      "{ data: { id: string; created_at: string; display_name: string; type: 'model'; }[]; first_id: string; has_more: boolean; last_id: string; }",
    markdown:
      "## list\n\n`client.models.list(after_id?: string, before_id?: string, limit?: number, anthropic-version?: string, x-api-key?: string): { data: object[]; first_id: string; has_more: boolean; last_id: string; }`\n\n**get** `/v1/models`\n\nList available models.\n\nThe Models API response can be used to determine which models are available for use in the API. More recently released models are listed first.\n\n### Parameters\n\n- `after_id?: string`\n  ID of the object to use as a cursor for pagination. When provided, returns the page of results immediately after this object.\n\n- `before_id?: string`\n  ID of the object to use as a cursor for pagination. When provided, returns the page of results immediately before this object.\n\n- `limit?: number`\n  Number of items to return per page.\n\nDefaults to `20`. Ranges from `1` to `1000`.\n\n- `anthropic-version?: string`\n  The version of the Anthropic API you want to use.\n\nRead more about versioning and our version history [here](https://docs.anthropic.com/en/api/versioning).\n\n- `x-api-key?: string`\n  Your unique API key for authentication.\n\nThis key is required in the header of all API requests, to authenticate your account and access Anthropic's services. Get your API key through the [Console](https://console.anthropic.com/settings/keys). Each key is scoped to a Workspace.\n\n### Returns\n\n- `{ data: { id: string; created_at: string; display_name: string; type: 'model'; }[]; first_id: string; has_more: boolean; last_id: string; }`\n\n  - `data: { id: string; created_at: string; display_name: string; type: 'model'; }[]`\n  - `first_id: string`\n  - `has_more: boolean`\n  - `last_id: string`\n\n### Example\n\n```typescript\nimport Sam from 'sam';\n\nconst client = new Sam();\n\nconst models = await client.models.list();\n\nconsole.log(models);\n```",
    perLanguage: {
      go: {
        method: 'client.Models.List',
        example:
          'package main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/DefinitelyATestOrg/sam-go"\n\t"github.com/DefinitelyATestOrg/sam-go/option"\n)\n\nfunc main() {\n\tclient := sam.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\tmodels, err := client.Models.List(context.TODO(), sam.ModelListParams{})\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", models.FirstID)\n}\n',
      },
      http: {
        example: 'curl /api/v3/v1/models',
      },
      java: {
        method: 'models().list',
        example:
          'package me.elborai.api.example;\n\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\nimport me.elborai.api.models.models.ModelListParams;\nimport me.elborai.api.models.models.ModelListResponse;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        SamClient client = SamOkHttpClient.fromEnv();\n\n        ModelListResponse models = client.models().list();\n    }\n}',
      },
      kotlin: {
        method: 'models().list',
        example:
          'package me.elborai.api.example\n\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\nimport me.elborai.api.models.models.ModelListParams\nimport me.elborai.api.models.models.ModelListResponse\n\nfun main() {\n    val client: SamClient = SamOkHttpClient.fromEnv()\n\n    val models: ModelListResponse = client.models().list()\n}',
      },
      python: {
        method: 'models.list',
        example:
          'from sam import Sam\n\nclient = Sam()\nmodels = client.models.list()\nprint(models.first_id)',
      },
      ruby: {
        method: 'models.list',
        example:
          'require "sam"\n\nsam = Sam::Client.new(api_key: "My API Key")\n\nmodels = sam.models.list\n\nputs(models)',
      },
      typescript: {
        method: 'client.models.list',
        example:
          "import Sam from 'sam';\n\nconst client = new Sam();\n\nconst models = await client.models.list();\n\nconsole.log(models.first_id);",
      },
    },
  },
  {
    name: 'retrieve',
    endpoint: '/v1/models/{model_id}',
    httpMethod: 'get',
    summary: 'Get a Model',
    description:
      'Get a specific model.\n\nThe Models API response can be used to determine information about a specific model or resolve a model alias to a model ID.',
    stainlessPath: '(resource) models > (method) retrieve',
    qualified: 'client.models.retrieve',
    params: ['model_id: string;', 'anthropic-version?: string;', 'x-api-key?: string;'],
    response: "{ id: string; created_at: string; display_name: string; type: 'model'; }",
    markdown:
      "## retrieve\n\n`client.models.retrieve(model_id: string, anthropic-version?: string, x-api-key?: string): { id: string; created_at: string; display_name: string; type: 'model'; }`\n\n**get** `/v1/models/{model_id}`\n\nGet a specific model.\n\nThe Models API response can be used to determine information about a specific model or resolve a model alias to a model ID.\n\n### Parameters\n\n- `model_id: string`\n  Model identifier or alias.\n\n- `anthropic-version?: string`\n  The version of the Anthropic API you want to use.\n\nRead more about versioning and our version history [here](https://docs.anthropic.com/en/api/versioning).\n\n- `x-api-key?: string`\n  Your unique API key for authentication.\n\nThis key is required in the header of all API requests, to authenticate your account and access Anthropic's services. Get your API key through the [Console](https://console.anthropic.com/settings/keys). Each key is scoped to a Workspace.\n\n### Returns\n\n- `{ id: string; created_at: string; display_name: string; type: 'model'; }`\n\n  - `id: string`\n  - `created_at: string`\n  - `display_name: string`\n  - `type: 'model'`\n\n### Example\n\n```typescript\nimport Sam from 'sam';\n\nconst client = new Sam();\n\nconst model = await client.models.retrieve('model_id');\n\nconsole.log(model);\n```",
    perLanguage: {
      go: {
        method: 'client.Models.Get',
        example:
          'package main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/DefinitelyATestOrg/sam-go"\n\t"github.com/DefinitelyATestOrg/sam-go/option"\n)\n\nfunc main() {\n\tclient := sam.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\tmodel, err := client.Models.Get(\n\t\tcontext.TODO(),\n\t\t"model_id",\n\t\tsam.ModelGetParams{},\n\t)\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", model.ID)\n}\n',
      },
      http: {
        example: 'curl /api/v3/v1/models/$MODEL_ID',
      },
      java: {
        method: 'models().retrieve',
        example:
          'package me.elborai.api.example;\n\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\nimport me.elborai.api.models.models.ModelRetrieveParams;\nimport me.elborai.api.models.models.ModelRetrieveResponse;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        SamClient client = SamOkHttpClient.fromEnv();\n\n        ModelRetrieveResponse model = client.models().retrieve("model_id");\n    }\n}',
      },
      kotlin: {
        method: 'models().retrieve',
        example:
          'package me.elborai.api.example\n\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\nimport me.elborai.api.models.models.ModelRetrieveParams\nimport me.elborai.api.models.models.ModelRetrieveResponse\n\nfun main() {\n    val client: SamClient = SamOkHttpClient.fromEnv()\n\n    val model: ModelRetrieveResponse = client.models().retrieve("model_id")\n}',
      },
      python: {
        method: 'models.retrieve',
        example:
          'from sam import Sam\n\nclient = Sam()\nmodel = client.models.retrieve(\n    model_id="model_id",\n)\nprint(model.id)',
      },
      ruby: {
        method: 'models.retrieve',
        example:
          'require "sam"\n\nsam = Sam::Client.new(api_key: "My API Key")\n\nmodel = sam.models.retrieve("model_id")\n\nputs(model)',
      },
      typescript: {
        method: 'client.models.retrieve',
        example:
          "import Sam from 'sam';\n\nconst client = new Sam();\n\nconst model = await client.models.retrieve('model_id');\n\nconsole.log(model.id);",
      },
    },
  },
  {
    name: 'retrieve_beta',
    endpoint: '/v1/models/{model_id}?beta=true',
    httpMethod: 'get',
    summary: 'Get a Model',
    description:
      'Get a specific model.\n\nThe Models API response can be used to determine information about a specific model or resolve a model alias to a model ID.',
    stainlessPath: '(resource) models > (method) retrieve_beta',
    qualified: 'client.models.retrieveBeta',
    params: ['model_id: string;', 'anthropic-version?: string;', 'x-api-key?: string;'],
    response: "{ id: string; created_at: string; display_name: string; type: 'model'; }",
    markdown:
      "## retrieve_beta\n\n`client.models.retrieveBeta(model_id: string, anthropic-version?: string, x-api-key?: string): { id: string; created_at: string; display_name: string; type: 'model'; }`\n\n**get** `/v1/models/{model_id}?beta=true`\n\nGet a specific model.\n\nThe Models API response can be used to determine information about a specific model or resolve a model alias to a model ID.\n\n### Parameters\n\n- `model_id: string`\n  Model identifier or alias.\n\n- `anthropic-version?: string`\n  The version of the Anthropic API you want to use.\n\nRead more about versioning and our version history [here](https://docs.anthropic.com/en/api/versioning).\n\n- `x-api-key?: string`\n  Your unique API key for authentication.\n\nThis key is required in the header of all API requests, to authenticate your account and access Anthropic's services. Get your API key through the [Console](https://console.anthropic.com/settings/keys). Each key is scoped to a Workspace.\n\n### Returns\n\n- `{ id: string; created_at: string; display_name: string; type: 'model'; }`\n\n  - `id: string`\n  - `created_at: string`\n  - `display_name: string`\n  - `type: 'model'`\n\n### Example\n\n```typescript\nimport Sam from 'sam';\n\nconst client = new Sam();\n\nconst response = await client.models.retrieveBeta('model_id');\n\nconsole.log(response);\n```",
    perLanguage: {
      go: {
        method: 'client.Models.GetBeta',
        example:
          'package main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/DefinitelyATestOrg/sam-go"\n\t"github.com/DefinitelyATestOrg/sam-go/option"\n)\n\nfunc main() {\n\tclient := sam.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\tresponse, err := client.Models.GetBeta(\n\t\tcontext.TODO(),\n\t\t"model_id",\n\t\tsam.ModelGetBetaParams{},\n\t)\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", response.ID)\n}\n',
      },
      http: {
        example: 'curl /api/v3/v1/models/$MODEL_ID',
      },
      java: {
        method: 'models().retrieveBeta',
        example:
          'package me.elborai.api.example;\n\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\nimport me.elborai.api.models.models.ModelRetrieveBetaParams;\nimport me.elborai.api.models.models.ModelRetrieveBetaResponse;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        SamClient client = SamOkHttpClient.fromEnv();\n\n        ModelRetrieveBetaResponse response = client.models().retrieveBeta("model_id");\n    }\n}',
      },
      kotlin: {
        method: 'models().retrieveBeta',
        example:
          'package me.elborai.api.example\n\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\nimport me.elborai.api.models.models.ModelRetrieveBetaParams\nimport me.elborai.api.models.models.ModelRetrieveBetaResponse\n\nfun main() {\n    val client: SamClient = SamOkHttpClient.fromEnv()\n\n    val response: ModelRetrieveBetaResponse = client.models().retrieveBeta("model_id")\n}',
      },
      python: {
        method: 'models.retrieve_beta',
        example:
          'from sam import Sam\n\nclient = Sam()\nresponse = client.models.retrieve_beta(\n    model_id="model_id",\n)\nprint(response.id)',
      },
      ruby: {
        method: 'models.retrieve_beta',
        example:
          'require "sam"\n\nsam = Sam::Client.new(api_key: "My API Key")\n\nresponse = sam.models.retrieve_beta("model_id")\n\nputs(response)',
      },
      typescript: {
        method: 'client.models.retrieveBeta',
        example:
          "import Sam from 'sam';\n\nconst client = new Sam();\n\nconst response = await client.models.retrieveBeta('model_id');\n\nconsole.log(response.id);",
      },
    },
  },
  {
    name: 'create',
    endpoint: '/v1/messages?beta=true',
    httpMethod: 'post',
    summary: 'Create a Message',
    description:
      'Send a structured list of input messages with text and/or image content, and the model will generate the next message in the conversation.\n\nThe Messages API can be used for either single queries or stateless multi-turn conversations.\n\nLearn more about the Messages API in our [user guide](/en/docs/initial-setup)',
    stainlessPath: '(resource) messages_beta_true > (method) create',
    qualified: 'client.messagesBetaTrue.create',
    params: [
      'max_tokens: number;',
      "messages: { content: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; } | { source: { data: string; media_type: 'image/jpeg' | 'image/png' | 'image/gif' | 'image/webp'; type: 'base64'; } | { type: 'url'; url: string; }; type: 'image'; cache_control?: { type: 'ephemeral'; }; } | { id: string; input: object; name: string; type: 'tool_use'; cache_control?: { type: 'ephemeral'; }; } | { tool_use_id: string; type: 'tool_result'; cache_control?: { type: 'ephemeral'; }; content?: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; } | { source: { data: string; media_type: 'image/jpeg' | 'image/png' | 'image/gif' | 'image/webp'; type: 'base64'; } | { type: 'url'; url: string; }; type: 'image'; cache_control?: { type: 'ephemeral'; }; }[]; is_error?: boolean; } | { source: { data: string; media_type: 'application/pdf'; type: 'base64'; } | { data: string; media_type: 'text/plain'; type: 'text'; } | { content: string | { text: string; type: 'text'; cache_control?: object; citations?: object | object | object[]; } | { source: object | object; type: 'image'; cache_control?: object; }[]; type: 'content'; } | { type: 'url'; url: string; }; type: 'document'; cache_control?: { type: 'ephemeral'; }; citations?: { enabled?: boolean; }; context?: string; title?: string; } | { signature: string; thinking: string; type: 'thinking'; } | { data: string; type: 'redacted_thinking'; }[]; role: 'user' | 'assistant'; }[];",
      'model: string;',
      'metadata?: { user_id?: string; };',
      'stop_sequences?: string[];',
      'stream?: boolean;',
      "system?: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; }[];",
      'temperature?: number;',
      "thinking?: { budget_tokens: number; type: 'enabled'; } | { type: 'disabled'; };",
      "tool_choice?: { type: 'auto'; disable_parallel_tool_use?: boolean; } | { type: 'any'; disable_parallel_tool_use?: boolean; } | { name: string; type: 'tool'; disable_parallel_tool_use?: boolean; } | { type: 'none'; };",
      "tools?: { input_schema: { type: 'object'; properties?: object; }; name: string; cache_control?: { type: 'ephemeral'; }; description?: string; type?: 'custom'; } | { display_height_px: number; display_width_px: number; name: 'computer'; type: 'computer_20241022'; cache_control?: { type: 'ephemeral'; }; display_number?: number; } | { name: 'bash'; type: 'bash_20241022'; cache_control?: { type: 'ephemeral'; }; } | { name: 'str_replace_editor'; type: 'text_editor_20241022'; cache_control?: { type: 'ephemeral'; }; } | { display_height_px: number; display_width_px: number; name: 'computer'; type: 'computer_20250124'; cache_control?: { type: 'ephemeral'; }; display_number?: number; } | { name: 'bash'; type: 'bash_20250124'; cache_control?: { type: 'ephemeral'; }; } | { name: 'str_replace_editor'; type: 'text_editor_20250124'; cache_control?: { type: 'ephemeral'; }; }[];",
      'top_k?: number;',
      'top_p?: number;',
      'anthropic-beta?: string[];',
      'anthropic-version?: string;',
      'x-api-key?: string;',
    ],
    response:
      "{ id: string; content: { citations: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; text: string; type: 'text'; } | { id: string; input: object; name: string; type: 'tool_use'; } | { signature: string; thinking: string; type: 'thinking'; } | { data: string; type: 'redacted_thinking'; }[]; model: string; role: 'assistant'; stop_reason: 'end_turn' | 'max_tokens' | 'stop_sequence' | 'tool_use'; stop_sequence: string; type: 'message'; usage: { cache_creation_input_tokens: number; cache_read_input_tokens: number; input_tokens: number; output_tokens: number; }; }",
    markdown:
      "## create\n\n`client.messagesBetaTrue.create(max_tokens: number, messages: { content: string | { text: string; type: 'text'; cache_control?: object; citations?: object | object | object[]; } | { source: object | object; type: 'image'; cache_control?: object; } | { id: string; input: object; name: string; type: 'tool_use'; cache_control?: object; } | { tool_use_id: string; type: 'tool_result'; cache_control?: object; content?: string | object | object[]; is_error?: boolean; } | { source: object | object | object | object; type: 'document'; cache_control?: object; citations?: object; context?: string; title?: string; } | { signature: string; thinking: string; type: 'thinking'; } | { data: string; type: 'redacted_thinking'; }[]; role: 'user' | 'assistant'; }[], model: string, metadata?: { user_id?: string; }, stop_sequences?: string[], stream?: boolean, system?: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; }[], temperature?: number, thinking?: { budget_tokens: number; type: 'enabled'; } | { type: 'disabled'; }, tool_choice?: { type: 'auto'; disable_parallel_tool_use?: boolean; } | { type: 'any'; disable_parallel_tool_use?: boolean; } | { name: string; type: 'tool'; disable_parallel_tool_use?: boolean; } | { type: 'none'; }, tools?: { input_schema: { type: 'object'; properties?: object; }; name: string; cache_control?: { type: 'ephemeral'; }; description?: string; type?: 'custom'; } | { display_height_px: number; display_width_px: number; name: 'computer'; type: 'computer_20241022'; cache_control?: { type: 'ephemeral'; }; display_number?: number; } | { name: 'bash'; type: 'bash_20241022'; cache_control?: { type: 'ephemeral'; }; } | { name: 'str_replace_editor'; type: 'text_editor_20241022'; cache_control?: { type: 'ephemeral'; }; } | { display_height_px: number; display_width_px: number; name: 'computer'; type: 'computer_20250124'; cache_control?: { type: 'ephemeral'; }; display_number?: number; } | { name: 'bash'; type: 'bash_20250124'; cache_control?: { type: 'ephemeral'; }; } | { name: 'str_replace_editor'; type: 'text_editor_20250124'; cache_control?: { type: 'ephemeral'; }; }[], top_k?: number, top_p?: number, anthropic-beta?: string[], anthropic-version?: string, x-api-key?: string): { id: string; content: object | object | object | object[]; model: string; role: 'assistant'; stop_reason: 'end_turn' | 'max_tokens' | 'stop_sequence' | 'tool_use'; stop_sequence: string; type: 'message'; usage: object; }`\n\n**post** `/v1/messages?beta=true`\n\nSend a structured list of input messages with text and/or image content, and the model will generate the next message in the conversation.\n\nThe Messages API can be used for either single queries or stateless multi-turn conversations.\n\nLearn more about the Messages API in our [user guide](/en/docs/initial-setup)\n\n### Parameters\n\n- `max_tokens: number`\n  The maximum number of tokens to generate before stopping.\n\nNote that our models may stop _before_ reaching this maximum. This parameter only specifies the absolute maximum number of tokens to generate.\n\nDifferent models have different maximum values for this parameter.  See [models](https://docs.anthropic.com/en/docs/models-overview) for details.\n\n- `messages: { content: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; } | { source: { data: string; media_type: 'image/jpeg' | 'image/png' | 'image/gif' | 'image/webp'; type: 'base64'; } | { type: 'url'; url: string; }; type: 'image'; cache_control?: { type: 'ephemeral'; }; } | { id: string; input: object; name: string; type: 'tool_use'; cache_control?: { type: 'ephemeral'; }; } | { tool_use_id: string; type: 'tool_result'; cache_control?: { type: 'ephemeral'; }; content?: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; } | { source: { data: string; media_type: 'image/jpeg' | 'image/png' | 'image/gif' | 'image/webp'; type: 'base64'; } | { type: 'url'; url: string; }; type: 'image'; cache_control?: { type: 'ephemeral'; }; }[]; is_error?: boolean; } | { source: { data: string; media_type: 'application/pdf'; type: 'base64'; } | { data: string; media_type: 'text/plain'; type: 'text'; } | { content: string | { text: string; type: 'text'; cache_control?: object; citations?: object | object | object[]; } | { source: object | object; type: 'image'; cache_control?: object; }[]; type: 'content'; } | { type: 'url'; url: string; }; type: 'document'; cache_control?: { type: 'ephemeral'; }; citations?: { enabled?: boolean; }; context?: string; title?: string; } | { signature: string; thinking: string; type: 'thinking'; } | { data: string; type: 'redacted_thinking'; }[]; role: 'user' | 'assistant'; }[]`\n  Input messages.\n\nOur models are trained to operate on alternating `user` and `assistant` conversational turns. When creating a new `Message`, you specify the prior conversational turns with the `messages` parameter, and the model then generates the next `Message` in the conversation. Consecutive `user` or `assistant` turns in your request will be combined into a single turn.\n\nEach input message must be an object with a `role` and `content`. You can specify a single `user`-role message, or you can include multiple `user` and `assistant` messages.\n\nIf the final message uses the `assistant` role, the response content will continue immediately from the content in that message. This can be used to constrain part of the model's response.\n\nExample with a single `user` message:\n\n```json\n[{\"role\": \"user\", \"content\": \"Hello, Claude\"}]\n```\n\nExample with multiple conversational turns:\n\n```json\n[\n  {\"role\": \"user\", \"content\": \"Hello there.\"},\n  {\"role\": \"assistant\", \"content\": \"Hi, I'm Claude. How can I help you?\"},\n  {\"role\": \"user\", \"content\": \"Can you explain LLMs in plain English?\"},\n]\n```\n\nExample with a partially-filled response from Claude:\n\n```json\n[\n  {\"role\": \"user\", \"content\": \"What's the Greek name for Sun? (A) Sol (B) Helios (C) Sun\"},\n  {\"role\": \"assistant\", \"content\": \"The best answer is (\"},\n]\n```\n\nEach input message `content` may be either a single `string` or an array of content blocks, where each block has a specific `type`. Using a `string` for `content` is shorthand for an array of one content block of type `\"text\"`. The following input messages are equivalent:\n\n```json\n{\"role\": \"user\", \"content\": \"Hello, Claude\"}\n```\n\n```json\n{\"role\": \"user\", \"content\": [{\"type\": \"text\", \"text\": \"Hello, Claude\"}]}\n```\n\nStarting with Claude 3 models, you can also send image content blocks:\n\n```json\n{\"role\": \"user\", \"content\": [\n  {\n    \"type\": \"image\",\n    \"source\": {\n      \"type\": \"base64\",\n      \"media_type\": \"image/jpeg\",\n      \"data\": \"/9j/4AAQSkZJRg...\",\n    }\n  },\n  {\"type\": \"text\", \"text\": \"What is in this image?\"}\n]}\n```\n\nWe currently support the `base64` source type for images, and the `image/jpeg`, `image/png`, `image/gif`, and `image/webp` media types.\n\nSee [examples](https://docs.anthropic.com/en/api/messages-examples#vision) for more input examples.\n\nNote that if you want to include a [system prompt](https://docs.anthropic.com/en/docs/system-prompts), you can use the top-level `system` parameter — there is no `\"system\"` role for input messages in the Messages API.\n\n- `model: string`\n  The model that will complete your prompt.\n\nSee [models](https://docs.anthropic.com/en/docs/models-overview) for additional details and options.\n\n- `metadata?: { user_id?: string; }`\n  An object describing metadata about the request.\n  - `user_id?: string`\n    An external identifier for the user who is associated with the request.\n\nThis should be a uuid, hash value, or other opaque identifier. Anthropic may use this id to help detect abuse. Do not include any identifying information such as name, email address, or phone number.\n\n- `stop_sequences?: string[]`\n  Custom text sequences that will cause the model to stop generating.\n\nOur models will normally stop when they have naturally completed their turn, which will result in a response `stop_reason` of `\"end_turn\"`.\n\nIf you want the model to stop generating when it encounters custom strings of text, you can use the `stop_sequences` parameter. If the model encounters one of the custom sequences, the response `stop_reason` value will be `\"stop_sequence\"` and the response `stop_sequence` value will contain the matched stop sequence.\n\n- `stream?: boolean`\n  Whether to incrementally stream the response using server-sent events.\n\nSee [streaming](https://docs.anthropic.com/en/api/messages-streaming) for details.\n\n- `system?: string | { text: string; type: 'text'; cache_control?: { type: 'ephemeral'; }; citations?: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; }[]`\n  System prompt.\n\nA system prompt is a way of providing context and instructions to Claude, such as specifying a particular goal or role. See our [guide to system prompts](https://docs.anthropic.com/en/docs/system-prompts).\n\n- `temperature?: number`\n  Amount of randomness injected into the response.\n\nDefaults to `1.0`. Ranges from `0.0` to `1.0`. Use `temperature` closer to `0.0` for analytical / multiple choice, and closer to `1.0` for creative and generative tasks.\n\nNote that even with `temperature` of `0.0`, the results will not be fully deterministic.\n\n- `thinking?: { budget_tokens: number; type: 'enabled'; } | { type: 'disabled'; }`\n  Configuration for enabling Claude's extended thinking. \n\nWhen enabled, responses include `thinking` content blocks showing Claude's thinking process before the final answer. Requires a minimum budget of 1,024 tokens and counts towards your `max_tokens` limit.\n\nSee [extended thinking](https://docs.anthropic.com/en/docs/build-with-claude/extended-thinking) for details.\n\n- `tool_choice?: { type: 'auto'; disable_parallel_tool_use?: boolean; } | { type: 'any'; disable_parallel_tool_use?: boolean; } | { name: string; type: 'tool'; disable_parallel_tool_use?: boolean; } | { type: 'none'; }`\n  How the model should use the provided tools. The model can use a specific tool, any available tool, decide by itself, or not use tools at all.\n\n- `tools?: { input_schema: { type: 'object'; properties?: object; }; name: string; cache_control?: { type: 'ephemeral'; }; description?: string; type?: 'custom'; } | { display_height_px: number; display_width_px: number; name: 'computer'; type: 'computer_20241022'; cache_control?: { type: 'ephemeral'; }; display_number?: number; } | { name: 'bash'; type: 'bash_20241022'; cache_control?: { type: 'ephemeral'; }; } | { name: 'str_replace_editor'; type: 'text_editor_20241022'; cache_control?: { type: 'ephemeral'; }; } | { display_height_px: number; display_width_px: number; name: 'computer'; type: 'computer_20250124'; cache_control?: { type: 'ephemeral'; }; display_number?: number; } | { name: 'bash'; type: 'bash_20250124'; cache_control?: { type: 'ephemeral'; }; } | { name: 'str_replace_editor'; type: 'text_editor_20250124'; cache_control?: { type: 'ephemeral'; }; }[]`\n  Definitions of tools that the model may use.\n\nIf you include `tools` in your API request, the model may return `tool_use` content blocks that represent the model's use of those tools. You can then run those tools using the tool input generated by the model and then optionally return results back to the model using `tool_result` content blocks.\n\nEach tool definition includes:\n\n* `name`: Name of the tool.\n* `description`: Optional, but strongly-recommended description of the tool.\n* `input_schema`: [JSON schema](https://json-schema.org/draft/2020-12) for the tool `input` shape that the model will produce in `tool_use` output content blocks.\n\nFor example, if you defined `tools` as:\n\n```json\n[\n  {\n    \"name\": \"get_stock_price\",\n    \"description\": \"Get the current stock price for a given ticker symbol.\",\n    \"input_schema\": {\n      \"type\": \"object\",\n      \"properties\": {\n        \"ticker\": {\n          \"type\": \"string\",\n          \"description\": \"The stock ticker symbol, e.g. AAPL for Apple Inc.\"\n        }\n      },\n      \"required\": [\"ticker\"]\n    }\n  }\n]\n```\n\nAnd then asked the model \"What's the S&P 500 at today?\", the model might produce `tool_use` content blocks in the response like this:\n\n```json\n[\n  {\n    \"type\": \"tool_use\",\n    \"id\": \"toolu_01D7FLrfh4GYq7yT1ULFeyMV\",\n    \"name\": \"get_stock_price\",\n    \"input\": { \"ticker\": \"^GSPC\" }\n  }\n]\n```\n\nYou might then run your `get_stock_price` tool with `{\"ticker\": \"^GSPC\"}` as an input, and return the following back to the model in a subsequent `user` message:\n\n```json\n[\n  {\n    \"type\": \"tool_result\",\n    \"tool_use_id\": \"toolu_01D7FLrfh4GYq7yT1ULFeyMV\",\n    \"content\": \"259.75 USD\"\n  }\n]\n```\n\nTools can be used for workflows that include running client-side tools and functions, or more generally whenever you want the model to produce a particular JSON structure of output.\n\nSee our [guide](https://docs.anthropic.com/en/docs/tool-use) for more details.\n\n- `top_k?: number`\n  Only sample from the top K options for each subsequent token.\n\nUsed to remove \"long tail\" low probability responses. [Learn more technical details here](https://towardsdatascience.com/how-to-sample-from-language-models-682bceb97277).\n\nRecommended for advanced use cases only. You usually only need to use `temperature`.\n\n- `top_p?: number`\n  Use nucleus sampling.\n\nIn nucleus sampling, we compute the cumulative distribution over all the options for each subsequent token in decreasing probability order and cut it off once it reaches a particular probability specified by `top_p`. You should either alter `temperature` or `top_p`, but not both.\n\nRecommended for advanced use cases only. You usually only need to use `temperature`.\n\n- `anthropic-beta?: string[]`\n  Optional header to specify the beta version(s) you want to use.\n\nTo use multiple betas, use a comma separated list like `beta1,beta2` or specify the header multiple times for each beta.\n\n- `anthropic-version?: string`\n  The version of the Anthropic API you want to use.\n\nRead more about versioning and our version history [here](https://docs.anthropic.com/en/api/versioning).\n\n- `x-api-key?: string`\n  Your unique API key for authentication.\n\nThis key is required in the header of all API requests, to authenticate your account and access Anthropic's services. Get your API key through the [Console](https://console.anthropic.com/settings/keys). Each key is scoped to a Workspace.\n\n### Returns\n\n- `{ id: string; content: { citations: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; text: string; type: 'text'; } | { id: string; input: object; name: string; type: 'tool_use'; } | { signature: string; thinking: string; type: 'thinking'; } | { data: string; type: 'redacted_thinking'; }[]; model: string; role: 'assistant'; stop_reason: 'end_turn' | 'max_tokens' | 'stop_sequence' | 'tool_use'; stop_sequence: string; type: 'message'; usage: { cache_creation_input_tokens: number; cache_read_input_tokens: number; input_tokens: number; output_tokens: number; }; }`\n\n  - `id: string`\n  - `content: { citations: { cited_text: string; document_index: number; document_title: string; end_char_index: number; start_char_index: number; type: 'char_location'; } | { cited_text: string; document_index: number; document_title: string; end_page_number: number; start_page_number: number; type: 'page_location'; } | { cited_text: string; document_index: number; document_title: string; end_block_index: number; start_block_index: number; type: 'content_block_location'; }[]; text: string; type: 'text'; } | { id: string; input: object; name: string; type: 'tool_use'; } | { signature: string; thinking: string; type: 'thinking'; } | { data: string; type: 'redacted_thinking'; }[]`\n  - `model: string`\n  - `role: 'assistant'`\n  - `stop_reason: 'end_turn' | 'max_tokens' | 'stop_sequence' | 'tool_use'`\n  - `stop_sequence: string`\n  - `type: 'message'`\n  - `usage: { cache_creation_input_tokens: number; cache_read_input_tokens: number; input_tokens: number; output_tokens: number; }`\n\n### Example\n\n```typescript\nimport Sam from 'sam';\n\nconst client = new Sam();\n\nconst messagesBetaTrue = await client.messagesBetaTrue.create({\n  max_tokens: 1024,\n  messages: [{ content: 'Hello, world', role: 'user' }],\n  model: 'claude-3-7-sonnet-20250219',\n});\n\nconsole.log(messagesBetaTrue);\n```",
    perLanguage: {
      go: {
        method: 'client.MessagesBetaTrue.New',
        example:
          'package main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/DefinitelyATestOrg/sam-go"\n\t"github.com/DefinitelyATestOrg/sam-go/option"\n\t"github.com/DefinitelyATestOrg/sam-go/shared"\n)\n\nfunc main() {\n\tclient := sam.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\tmessagesBetaTrue, err := client.MessagesBetaTrue.New(context.TODO(), sam.MessagesBetaTrueNewParams{\n\t\tMaxTokens: sam.F(int64(1024)),\n\t\tMessages: sam.F([]sam.MessagesBetaTrueNewParamsMessage{{\n\t\t\tContent: sam.F[sam.MessagesBetaTrueNewParamsMessagesContentUnion](shared.UnionString("Hello, world")),\n\t\t\tRole:    sam.F(sam.MessagesBetaTrueNewParamsMessagesRoleUser),\n\t\t}}),\n\t\tModel: sam.F("claude-3-7-sonnet-20250219"),\n\t})\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", messagesBetaTrue.ID)\n}\n',
      },
      http: {
        example:
          'curl /api/v3/v1/messages \\\n    -H \'Content-Type: application/json\' \\\n    -d \'{\n          "max_tokens": 1024,\n          "messages": [\n            {\n              "content": "Hello, world",\n              "role": "user"\n            }\n          ],\n          "model": "claude-3-7-sonnet-20250219"\n        }\'',
      },
      java: {
        method: 'messagesBetaTrue().create',
        example:
          'package me.elborai.api.example;\n\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\nimport me.elborai.api.models.messagesbetatrue.MessagesBetaTrueCreateParams;\nimport me.elborai.api.models.messagesbetatrue.MessagesBetaTrueCreateResponse;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        SamClient client = SamOkHttpClient.fromEnv();\n\n        MessagesBetaTrueCreateParams params = MessagesBetaTrueCreateParams.builder()\n            .maxTokens(1024L)\n            .addMessage(MessagesBetaTrueCreateParams.Message.builder()\n                .content("Hello, world")\n                .role(MessagesBetaTrueCreateParams.Message.Role.USER)\n                .build())\n            .model("claude-3-7-sonnet-20250219")\n            .build();\n        MessagesBetaTrueCreateResponse messagesBetaTrue = client.messagesBetaTrue().create(params);\n    }\n}',
      },
      kotlin: {
        method: 'messagesBetaTrue().create',
        example:
          'package me.elborai.api.example\n\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\nimport me.elborai.api.models.messagesbetatrue.MessagesBetaTrueCreateParams\nimport me.elborai.api.models.messagesbetatrue.MessagesBetaTrueCreateResponse\n\nfun main() {\n    val client: SamClient = SamOkHttpClient.fromEnv()\n\n    val params: MessagesBetaTrueCreateParams = MessagesBetaTrueCreateParams.builder()\n        .maxTokens(1024L)\n        .addMessage(MessagesBetaTrueCreateParams.Message.builder()\n            .content("Hello, world")\n            .role(MessagesBetaTrueCreateParams.Message.Role.USER)\n            .build())\n        .model("claude-3-7-sonnet-20250219")\n        .build()\n    val messagesBetaTrue: MessagesBetaTrueCreateResponse = client.messagesBetaTrue().create(params)\n}',
      },
      python: {
        method: 'messages_beta_true.create',
        example:
          'from sam import Sam\n\nclient = Sam()\nmessages_beta_true = client.messages_beta_true.create(\n    max_tokens=1024,\n    messages=[{\n        "content": "Hello, world",\n        "role": "user",\n    }],\n    model="claude-3-7-sonnet-20250219",\n)\nprint(messages_beta_true.id)',
      },
      ruby: {
        method: 'messages_beta_true.create',
        example:
          'require "sam"\n\nsam = Sam::Client.new(api_key: "My API Key")\n\nmessages_beta_true = sam.messages_beta_true.create(\n  max_tokens: 1024,\n  messages: [{content: "Hello, world", role: :user}],\n  model: "claude-3-7-sonnet-20250219"\n)\n\nputs(messages_beta_true)',
      },
      typescript: {
        method: 'client.messagesBetaTrue.create',
        example:
          "import Sam from 'sam';\n\nconst client = new Sam();\n\nconst messagesBetaTrue = await client.messagesBetaTrue.create({\n  max_tokens: 1024,\n  messages: [{ content: 'Hello, world', role: 'user' }],\n  model: 'claude-3-7-sonnet-20250219',\n});\n\nconsole.log(messagesBetaTrue.id);",
      },
    },
  },
  {
    name: 'list',
    endpoint: '/v1/models?beta=true',
    httpMethod: 'get',
    summary: 'List Models',
    description:
      'List available models.\n\nThe Models API response can be used to determine which models are available for use in the API. More recently released models are listed first.',
    stainlessPath: '(resource) models_beta_true > (method) list',
    qualified: 'client.modelsBetaTrue.list',
    params: [
      'after_id?: string;',
      'before_id?: string;',
      'limit?: number;',
      'anthropic-version?: string;',
      'x-api-key?: string;',
    ],
    response:
      "{ data: { id: string; created_at: string; display_name: string; type: 'model'; }[]; first_id: string; has_more: boolean; last_id: string; }",
    markdown:
      "## list\n\n`client.modelsBetaTrue.list(after_id?: string, before_id?: string, limit?: number, anthropic-version?: string, x-api-key?: string): { data: object[]; first_id: string; has_more: boolean; last_id: string; }`\n\n**get** `/v1/models?beta=true`\n\nList available models.\n\nThe Models API response can be used to determine which models are available for use in the API. More recently released models are listed first.\n\n### Parameters\n\n- `after_id?: string`\n  ID of the object to use as a cursor for pagination. When provided, returns the page of results immediately after this object.\n\n- `before_id?: string`\n  ID of the object to use as a cursor for pagination. When provided, returns the page of results immediately before this object.\n\n- `limit?: number`\n  Number of items to return per page.\n\nDefaults to `20`. Ranges from `1` to `1000`.\n\n- `anthropic-version?: string`\n  The version of the Anthropic API you want to use.\n\nRead more about versioning and our version history [here](https://docs.anthropic.com/en/api/versioning).\n\n- `x-api-key?: string`\n  Your unique API key for authentication.\n\nThis key is required in the header of all API requests, to authenticate your account and access Anthropic's services. Get your API key through the [Console](https://console.anthropic.com/settings/keys). Each key is scoped to a Workspace.\n\n### Returns\n\n- `{ data: { id: string; created_at: string; display_name: string; type: 'model'; }[]; first_id: string; has_more: boolean; last_id: string; }`\n\n  - `data: { id: string; created_at: string; display_name: string; type: 'model'; }[]`\n  - `first_id: string`\n  - `has_more: boolean`\n  - `last_id: string`\n\n### Example\n\n```typescript\nimport Sam from 'sam';\n\nconst client = new Sam();\n\nconst modelsBetaTrues = await client.modelsBetaTrue.list();\n\nconsole.log(modelsBetaTrues);\n```",
    perLanguage: {
      go: {
        method: 'client.ModelsBetaTrue.List',
        example:
          'package main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/DefinitelyATestOrg/sam-go"\n\t"github.com/DefinitelyATestOrg/sam-go/option"\n)\n\nfunc main() {\n\tclient := sam.NewClient(\n\t\toption.WithAPIKey("My API Key"),\n\t)\n\tmodelsBetaTrues, err := client.ModelsBetaTrue.List(context.TODO(), sam.ModelsBetaTrueListParams{})\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", modelsBetaTrues.FirstID)\n}\n',
      },
      http: {
        example: 'curl /api/v3/v1/models',
      },
      java: {
        method: 'modelsBetaTrue().list',
        example:
          'package me.elborai.api.example;\n\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\nimport me.elborai.api.models.modelsbetatrue.ModelsBetaTrueListParams;\nimport me.elborai.api.models.modelsbetatrue.ModelsBetaTrueListResponse;\n\npublic final class Main {\n    private Main() {}\n\n    public static void main(String[] args) {\n        SamClient client = SamOkHttpClient.fromEnv();\n\n        ModelsBetaTrueListResponse modelsBetaTrues = client.modelsBetaTrue().list();\n    }\n}',
      },
      kotlin: {
        method: 'modelsBetaTrue().list',
        example:
          'package me.elborai.api.example\n\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\nimport me.elborai.api.models.modelsbetatrue.ModelsBetaTrueListParams\nimport me.elborai.api.models.modelsbetatrue.ModelsBetaTrueListResponse\n\nfun main() {\n    val client: SamClient = SamOkHttpClient.fromEnv()\n\n    val modelsBetaTrues: ModelsBetaTrueListResponse = client.modelsBetaTrue().list()\n}',
      },
      python: {
        method: 'models_beta_true.list',
        example:
          'from sam import Sam\n\nclient = Sam()\nmodels_beta_trues = client.models_beta_true.list()\nprint(models_beta_trues.first_id)',
      },
      ruby: {
        method: 'models_beta_true.list',
        example:
          'require "sam"\n\nsam = Sam::Client.new(api_key: "My API Key")\n\nmodels_beta_trues = sam.models_beta_true.list\n\nputs(models_beta_trues)',
      },
      typescript: {
        method: 'client.modelsBetaTrue.list',
        example:
          "import Sam from 'sam';\n\nconst client = new Sam();\n\nconst modelsBetaTrues = await client.modelsBetaTrue.list();\n\nconsole.log(modelsBetaTrues.first_id);",
      },
    },
  },
];

const EMBEDDED_READMES: { language: string; content: string }[] = [
  {
    language: 'python',
    content:
      '# Sam Python API library\n\n<!-- prettier-ignore -->\n[![PyPI version](https://img.shields.io/pypi/v/sam.svg?label=pypi%20(stable))](https://pypi.org/project/sam/)\n\nThe Sam Python library provides convenient access to the Sam REST API from any Python 3.9+\napplication. The library includes type definitions for all request params and response fields,\nand offers both synchronous and asynchronous clients powered by [httpx](https://github.com/encode/httpx).\n\n\n\nIt is generated with [Stainless](https://www.stainless.com/).\n\n## MCP Server\n\nUse the Sam MCP Server to enable AI assistants to interact with this API, allowing them to explore endpoints, make test requests, and use documentation to help integrate this SDK into your application.\n\n[![Add to Cursor](https://cursor.com/deeplink/mcp-install-dark.svg)](https://cursor.com/en-US/install-mcp?name=sam-mcp&config=eyJjb21tYW5kIjoibnB4IiwiYXJncyI6WyIteSIsInNhbS1tY3AiXSwiZW52Ijp7IkFQSV9LRVkiOiJNeSBBUEkgS2V5In19)\n[![Install in VS Code](https://img.shields.io/badge/_-Add_to_VS_Code-blue?style=for-the-badge&logo=data:image/svg%2bxml;base64,)](https://vscode.stainless.com/mcp/%7B%22name%22%3A%22sam-mcp%22%2C%22command%22%3A%22npx%22%2C%22args%22%3A%5B%22-y%22%2C%22sam-mcp%22%5D%2C%22env%22%3A%7B%22API_KEY%22%3A%22My%20API%20Key%22%7D%7D)\n\n> Note: You may need to set environment variables in your MCP client.\n\n## Documentation\n\nThe REST API documentation can be found on [docs.sam.com](https://docs.sam.com). The full API of this library can be found in [api.md](api.md).\n\n## Installation\n\n```sh\n# install from the production repo\npip install git+ssh://git@github.com/DefinitelyATestOrg/sam-python.git\n```\n> [!NOTE]\n> Once this package is [published to PyPI](https://www.stainless.com/docs/guides/publish), this will become: `pip install sam`\n\n## Usage\n\nThe full API of this library can be found in [api.md](api.md).\n\n```python\nfrom sam import Sam\n\nclient = Sam(\n    api_key="My API Key",\n)\n\nmessage = client.messages.create(\n    max_tokens=1024,\n    messages=[{\n        "content": "Hello, world",\n        "role": "user",\n    }],\n    model="claude-3-7-sonnet-20250219",\n)\nprint(message.id)\n```\n\n\n\n## Async usage\n\nSimply import `AsyncSam` instead of `Sam` and use `await` with each API call:\n\n```python\nimport asyncio\nfrom sam import AsyncSam\n\nclient = AsyncSam(\n    api_key="My API Key",\n)\n\nasync def main() -> None:\n  message = await client.messages.create(\n      max_tokens=1024,\n      messages=[{\n          "content": "Hello, world",\n          "role": "user",\n      }],\n      model="claude-3-7-sonnet-20250219",\n  )\n  print(message.id)\n\nasyncio.run(main())\n```\n\nFunctionality between the synchronous and asynchronous clients is otherwise identical.\n\n### With aiohttp\n\nBy default, the async client uses `httpx` for HTTP requests. However, for improved concurrency performance you may also use `aiohttp` as the HTTP backend.\n\nYou can enable this by installing `aiohttp`:\n\n```sh\n# install from the production repo\npip install \'sam[aiohttp] @ git+ssh://git@github.com/DefinitelyATestOrg/sam-python.git\'\n```\n\nThen you can enable it by instantiating the client with `http_client=DefaultAioHttpClient()`:\n\n```python\nimport asyncio\nfrom sam import DefaultAioHttpClient\nfrom sam import AsyncSam\n\nasync def main() -> None:\n  async with AsyncSam(\n    api_key="My API Key",\n    http_client=DefaultAioHttpClient(),\n) as client:\n    message = await client.messages.create(\n        max_tokens=1024,\n        messages=[{\n            "content": "Hello, world",\n            "role": "user",\n        }],\n        model="claude-3-7-sonnet-20250219",\n    )\n    print(message.id)\n\nasyncio.run(main())\n```\n\n\n\n## Using types\n\nNested request parameters are [TypedDicts](https://docs.python.org/3/library/typing.html#typing.TypedDict). Responses are [Pydantic models](https://docs.pydantic.dev) which also provide helper methods for things like:\n\n- Serializing back into JSON, `model.to_json()`\n- Converting to a dictionary, `model.to_dict()`\n\nTyped requests and responses provide autocomplete and documentation within your editor. If you would like to see type errors in VS Code to help catch bugs earlier, set `python.analysis.typeCheckingMode` to `basic`.\n\n\n\n## Nested params\n\nNested parameters are dictionaries, typed using `TypedDict`, for example:\n\n```python\nfrom sam import Sam\n\nclient = Sam(\n    api_key="My API Key",\n)\n\nmessage = client.messages.create(\n    max_tokens=1024,\n    messages=[{\n        "content": "Hello, world",\n        "role": "user",\n    }],\n    model="claude-3-7-sonnet-20250219",\n    metadata={},\n)\nprint(message.metadata)\n```\n\n\n\n## Handling errors\n\nWhen the library is unable to connect to the API (for example, due to network connection problems or a timeout), a subclass of `sam.APIConnectionError` is raised.\n\nWhen the API returns a non-success status code (that is, 4xx or 5xx\nresponse), a subclass of `sam.APIStatusError` is raised, containing `status_code` and `response` properties.\n\nAll errors inherit from `sam.APIError`.\n\n```python\nimport sam\nfrom sam import Sam\n\nclient = Sam(\n    api_key="My API Key",\n)\n\ntry:\n    client.messages.create(\n        max_tokens=1024,\n        messages=[{\n            "content": "Hello, world",\n            "role": "user",\n        }],\n        model="claude-3-7-sonnet-20250219",\n    )\nexcept sam.APIConnectionError as e:\n    print("The server could not be reached")\n    print(e.__cause__) # an underlying Exception, likely raised within httpx.\nexcept sam.RateLimitError as e:\n    print("A 429 status code was received; we should back off a bit.")\nexcept sam.APIStatusError as e:\n    print("Another non-200-range status code was received")\n    print(e.status_code)\n    print(e.response)\n```\n\nError codes are as follows:\n\n| Status Code | Error Type                 |\n| ----------- | -------------------------- |\n| 400         | `BadRequestError`          |\n| 401         | `AuthenticationError`      |\n| 403         | `PermissionDeniedError`    |\n| 404         | `NotFoundError`            |\n| 422         | `UnprocessableEntityError` |\n| 429         | `RateLimitError`           |\n| >=500       | `InternalServerError`      |\n| N/A         | `APIConnectionError`       |\n\n### Retries\n\nCertain errors are automatically retried 2 times by default, with a short exponential backoff.\nConnection errors (for example, due to a network connectivity problem), 408 Request Timeout, 409 Conflict,\n429 Rate Limit, and >=500 Internal errors are all retried by default.\n\nYou can use the `max_retries` option to configure or disable retry settings:\n\n```python\nfrom sam import Sam\n\n# Configure the default for all requests:\nclient = Sam(\n    api_key="My API Key",\n    # default is 2\n    max_retries=0,\n)\n\n# Or, configure per-request:\nclient.with_options(max_retries = 5).messages.create(\n    max_tokens=1024,\n    messages=[{\n        "content": "Hello, world",\n        "role": "user",\n    }],\n    model="claude-3-7-sonnet-20250219",\n)\n```\n\n### Timeouts\n\nBy default requests time out after 1 minute. You can configure this with a `timeout` option,\nwhich accepts a float or an [`httpx.Timeout`](https://www.python-httpx.org/advanced/timeouts/#fine-tuning-the-configuration) object:\n\n```python\nfrom sam import Sam\n\n# Configure the default for all requests:\nclient = Sam(\n    api_key="My API Key",\n    # 20 seconds (default is 1 minute)\n    timeout=20.0,\n)\n\n# More granular control:\nclient = Sam(\n    api_key="My API Key",\n    timeout=httpx.Timeout(60.0, read=5.0, write=10.0, connect=2.0),\n)\n\n# Override per-request:\nclient.with_options(timeout = 5.0).messages.create(\n    max_tokens=1024,\n    messages=[{\n        "content": "Hello, world",\n        "role": "user",\n    }],\n    model="claude-3-7-sonnet-20250219",\n)\n```\n\nOn timeout, an `APITimeoutError` is thrown.\n\nNote that requests that time out are [retried twice by default](#retries).\n\n\n\n## Advanced\n\n### Logging\n\nWe use the standard library [`logging`](https://docs.python.org/3/library/logging.html) module.\n\nYou can enable logging by setting the environment variable `SAM_LOG` to `info`.\n\n```shell\n$ export SAM_LOG=info\n```\n\nOr to `debug` for more verbose logging.\n\n### How to tell whether `None` means `null` or missing\n\nIn an API response, a field may be explicitly `null`, or missing entirely; in either case, its value is `None` in this library. You can differentiate the two cases with `.model_fields_set`:\n\n```py\nif response.my_field is None:\n  if \'my_field\' not in response.model_fields_set:\n    print(\'Got json like {}, without a "my_field" key present at all.\')\n  else:\n    print(\'Got json like {"my_field": null}.\')\n```\n\n### Accessing raw response data (e.g. headers)\n\nThe "raw" Response object can be accessed by prefixing `.with_raw_response.` to any HTTP method call, e.g.,\n\n```py\nfrom sam import Sam\n\nclient = Sam(\n    api_key="My API Key",\n)\nresponse = client.messages.with_raw_response.create(\n    max_tokens=1024,\n    messages=[{\n        "content": "Hello, world",\n        "role": "user",\n    }],\n    model="claude-3-7-sonnet-20250219",\n)\nprint(response.headers.get(\'X-My-Header\'))\n\nmessage = response.parse()  # get the object that `messages.create()` would have returned\nprint(message.id)\n```\n\nThese methods return an [`APIResponse`](https://github.com/DefinitelyATestOrg/sam-python/tree/main/src/sam/_response.py) object.\n\nThe async client returns an [`AsyncAPIResponse`](https://github.com/DefinitelyATestOrg/sam-python/tree/main/src/sam/_response.py) with the same structure, the only difference being `await`able methods for reading the response content.\n\n#### `.with_streaming_response`\n\nThe above interface eagerly reads the full response body when you make the request, which may not always be what you want.\n\nTo stream the response body, use `.with_streaming_response` instead, which requires a context manager and only reads the response body once you call `.read()`, `.text()`, `.json()`, `.iter_bytes()`, `.iter_text()`, `.iter_lines()` or `.parse()`. In the async client, these are async methods.\n\n```python\nwith client.messages.with_streaming_response.create(\n    max_tokens=1024,\n    messages=[{\n        "content": "Hello, world",\n        "role": "user",\n    }],\n    model="claude-3-7-sonnet-20250219",\n) as response :\n    print(response.headers.get(\'X-My-Header\'))\n\n    for line in response.iter_lines():\n      print(line)\n```\n\nThe context manager is required so that the response will reliably be closed.\n\n### Making custom/undocumented requests\n\nThis library is typed for convenient access to the documented API.\n\nIf you need to access undocumented endpoints, params, or response properties, the library can still be used.\n\n#### Undocumented endpoints\n\nTo make requests to undocumented endpoints, you can make requests using `client.get`, `client.post`, and other\nhttp verbs. Options on the client will be respected (such as retries) when making this request.\n\n```py\nimport httpx\n\nresponse = client.post(\n    "/foo",\n    cast_to=httpx.Response,\n    body={"my_param": True},\n)\n\nprint(response.headers.get("x-foo"))\n```\n\n#### Undocumented request params\n\nIf you want to explicitly send an extra param, you can do so with the `extra_query`, `extra_body`, and `extra_headers` request\noptions.\n\n#### Undocumented response properties\n\nTo access undocumented response properties, you can access the extra fields like `response.unknown_prop`. You\ncan also get all the extra fields on the Pydantic model as a dict with\n[`response.model_extra`](https://docs.pydantic.dev/latest/api/base_model/#pydantic.BaseModel.model_extra).\n\n### Configuring the HTTP client\n\nYou can directly override the [httpx client](https://www.python-httpx.org/api/#client) to customize it for your use case, including:\n\n- Support for [proxies](https://www.python-httpx.org/advanced/proxies/)\n- Custom [transports](https://www.python-httpx.org/advanced/transports/)\n- Additional [advanced](https://www.python-httpx.org/advanced/clients/) functionality\n\n```python\nimport httpx\nfrom sam import Sam, DefaultHttpxClient\n\nclient = Sam(\n    api_key="My API Key",\n    # Or use the `SAM_BASE_URL` env var\n    base_url="http://my.test.server.example.com:8083",\n    http_client=DefaultHttpxClient(proxy="http://my.test.proxy.example.com", transport=httpx.HTTPTransport(local_address="0.0.0.0")),\n)\n```\n\nYou can also customize the client on a per-request basis by using `with_options()`:\n\n```python\nclient.with_options(http_client=DefaultHttpxClient(...))\n```\n\n### Managing HTTP resources\n\nBy default the library closes underlying HTTP connections whenever the client is [garbage collected](https://docs.python.org/3/reference/datamodel.html#object.__del__). You can manually close the client using the `.close()` method if desired, or with a context manager that closes when exiting.\n\n```py\nfrom sam import Sam\n\nwith Sam(\n    api_key="My API Key",\n) as client:\n  # make requests here\n  ...\n\n# HTTP client is now closed\n```\n\n## Versioning\n\nThis package generally follows [SemVer](https://semver.org/spec/v2.0.0.html) conventions, though certain backwards-incompatible changes may be released as minor versions:\n\n1. Changes that only affect static types, without breaking runtime behavior.\n2. Changes to library internals which are technically public but not intended or documented for external use. _(Please open a GitHub issue to let us know if you are relying on such internals.)_\n3. Changes that we do not expect to impact the vast majority of users in practice.\n\nWe take backwards-compatibility seriously and work hard to ensure you can rely on a smooth upgrade experience.\n\nWe are keen for your feedback; please open an [issue](https://www.github.com/DefinitelyATestOrg/sam-python/issues) with questions, bugs, or suggestions.\n\n### Determining the installed version\n\nIf you\'ve upgraded to the latest version but aren\'t seeing any new features you were expecting then your python environment is likely still using an older version.\n\nYou can determine the version that is being used at runtime with:\n\n```py\nimport sam\nprint(sam.__version__)\n```\n\n## Requirements\n\nPython 3.9 or higher.\n\n## Contributing\n\nSee [the contributing documentation](./CONTRIBUTING.md).\n',
  },
  {
    language: 'go',
    content:
      '# Sam Go API Library\n\n<a href="https://pkg.go.dev/github.com/DefinitelyATestOrg/sam-go"><img src="https://pkg.go.dev/badge/github.com/DefinitelyATestOrg/sam-go.svg" alt="Go Reference"></a>\n\nThe Sam Go library provides convenient access to the [Sam REST API](https://docs.sam.com)\nfrom applications written in Go.\n\nIt is generated with [Stainless](https://www.stainless.com/).\n\n## MCP Server\n\nUse the Sam MCP Server to enable AI assistants to interact with this API, allowing them to explore endpoints, make test requests, and use documentation to help integrate this SDK into your application.\n\n[![Add to Cursor](https://cursor.com/deeplink/mcp-install-dark.svg)](https://cursor.com/en-US/install-mcp?name=sam-mcp&config=eyJjb21tYW5kIjoibnB4IiwiYXJncyI6WyIteSIsInNhbS1tY3AiXSwiZW52Ijp7IkFQSV9LRVkiOiJNeSBBUEkgS2V5In19)\n[![Install in VS Code](https://img.shields.io/badge/_-Add_to_VS_Code-blue?style=for-the-badge&logo=data:image/svg%2bxml;base64,)](https://vscode.stainless.com/mcp/%7B%22name%22%3A%22sam-mcp%22%2C%22command%22%3A%22npx%22%2C%22args%22%3A%5B%22-y%22%2C%22sam-mcp%22%5D%2C%22env%22%3A%7B%22API_KEY%22%3A%22My%20API%20Key%22%7D%7D)\n\n> Note: You may need to set environment variables in your MCP client.\n\n## Installation\n\n<!-- x-release-please-start-version -->\n\n```go\nimport (\n\t"github.com/DefinitelyATestOrg/sam-go" // imported as SDK_PackageName\n)\n```\n\n<!-- x-release-please-end -->\n\nOr to pin the version:\n\n<!-- x-release-please-start-version -->\n\n```sh\ngo get -u \'github.com/DefinitelyATestOrg/sam-go@v0.0.1\'\n```\n\n<!-- x-release-please-end -->\n\n## Requirements\n\nThis library requires Go 1.22+.\n\n## Usage\n\nThe full API of this library can be found in [api.md](api.md).\n\n```go\npackage main\n\nimport (\n\t"context"\n\t"fmt"\n\n\t"github.com/DefinitelyATestOrg/sam-go"\n\t"github.com/DefinitelyATestOrg/sam-go/option"\n\t"github.com/DefinitelyATestOrg/sam-go/shared"\n)\n\nfunc main() {\n\tclient := sam.NewClient(\n\t\toption.WithAPIKey("My API Key"), // defaults to os.LookupEnv("API_KEY")\n\t)\n\tmessage, err := client.Messages.New(context.TODO(), sam.MessageNewParams{\n\t\tMaxTokens: sam.F(int64(1024)),\n\t\tMessages: sam.F([]sam.MessageNewParamsMessage{{\n\t\t\tContent: sam.F[sam.MessageNewParamsMessagesContentUnion](shared.UnionString("Hello, world")),\n\t\t\tRole:    sam.F(sam.MessageNewParamsMessagesRoleUser),\n\t\t}}),\n\t\tModel: sam.F("claude-3-7-sonnet-20250219"),\n\t})\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf("%+v\\n", message.ID)\n}\n\n```\n\n### Request fields\n\nAll request parameters are wrapped in a generic `Field` type,\nwhich we use to distinguish zero values from null or omitted fields.\n\nThis prevents accidentally sending a zero value if you forget a required parameter,\nand enables explicitly sending `null`, `false`, `\'\'`, or `0` on optional parameters.\nAny field not specified is not sent.\n\nTo construct fields with values, use the helpers `String()`, `Int()`, `Float()`, or most commonly, the generic `F[T]()`.\nTo send a null, use `Null[T]()`, and to send a nonconforming value, use `Raw[T](any)`. For example:\n\n```go\nparams := FooParams{\n\tName: SDK_PackageName.F("hello"),\n\n\t// Explicitly send `"description": null`\n\tDescription: SDK_PackageName.Null[string](),\n\n\tPoint: SDK_PackageName.F(SDK_PackageName.Point{\n\t\tX: SDK_PackageName.Int(0),\n\t\tY: SDK_PackageName.Int(1),\n\n\t\t// In cases where the API specifies a given type,\n\t\t// but you want to send something else, use `Raw`:\n\t\tZ: SDK_PackageName.Raw[int64](0.01), // sends a float\n\t}),\n}\n```\n\n### Response objects\n\nAll fields in response structs are value types (not pointers or wrappers).\n\nIf a given field is `null`, not present, or invalid, the corresponding field\nwill simply be its zero value.\n\nAll response structs also include a special `JSON` field, containing more detailed\ninformation about each property, which you can use like so:\n\n```go\nif res.Name == "" {\n\t// true if `"name"` is either not present or explicitly null\n\tres.JSON.Name.IsNull()\n\n\t// true if the `"name"` key was not present in the response JSON at all\n\tres.JSON.Name.IsMissing()\n\n\t// When the API returns data that cannot be coerced to the expected type:\n\tif res.JSON.Name.IsInvalid() {\n\t\traw := res.JSON.Name.Raw()\n\n\t\tlegacyName := struct{\n\t\t\tFirst string `json:"first"`\n\t\t\tLast  string `json:"last"`\n\t\t}{}\n\t\tjson.Unmarshal([]byte(raw), &legacyName)\n\t\tname = legacyName.First + " " + legacyName.Last\n\t}\n}\n```\n\nThese `.JSON` structs also include an `Extras` map containing\nany properties in the json response that were not specified\nin the struct. This can be useful for API features not yet\npresent in the SDK.\n\n```go\nbody := res.JSON.ExtraFields["my_unexpected_field"].Raw()\n```\n\n### RequestOptions\n\nThis library uses the functional options pattern. Functions defined in the\n`SDK_PackageOptionName` package return a `RequestOption`, which is a closure that mutates a\n`RequestConfig`. These options can be supplied to the client or at individual\nrequests. For example:\n\n```go\nclient := SDK_PackageName.SDK_ClientInitializerName(\n\t// Adds a header to every request made by the client\n\tSDK_PackageOptionName.WithHeader("X-Some-Header", "custom_header_info"),\n)\n\nclient.Messages.New(context.TODO(), ...,\n\t// Override the header\n\tSDK_PackageOptionName.WithHeader("X-Some-Header", "some_other_custom_header_info"),\n\t// Add an undocumented field to the request body, using sjson syntax\n\tSDK_PackageOptionName.WithJSONSet("some.json.path", map[string]string{"my": "object"}),\n)\n```\n\nSee the [full list of request options](https://pkg.go.dev/github.com/DefinitelyATestOrg/sam-go/SDK_PackageOptionName).\n\n### Pagination\n\nThis library provides some conveniences for working with paginated list endpoints.\n\nYou can use `.ListAutoPaging()` methods to iterate through items across all pages:\n\n\n\nOr you can use simple `.List()` methods to fetch a single page and receive a standard response object\nwith additional helper methods like `.GetNextPage()`, e.g.:\n\n\n\n### Errors\n\nWhen the API returns a non-success status code, we return an error with type\n`*SDK_PackageName.Error`. This contains the `StatusCode`, `*http.Request`, and\n`*http.Response` values of the request, as well as the JSON of the error body\n(much like other response objects in the SDK).\n\nTo handle errors, we recommend that you use the `errors.As` pattern:\n\n```go\n_, err := client.Messages.New(context.TODO(), sam.MessageNewParams{\n\tMaxTokens: sam.F(int64(1024)),\n\tMessages: sam.F([]sam.MessageNewParamsMessage{{\n\t\tContent: sam.F[sam.MessageNewParamsMessagesContentUnion](shared.UnionString("Hello, world")),\n\t\tRole:    sam.F(sam.MessageNewParamsMessagesRoleUser),\n\t}}),\n\tModel: sam.F("claude-3-7-sonnet-20250219"),\n})\nif err != nil {\n\tvar apierr *sam.Error\n\tif errors.As(err, &apierr) {\n\t\tprintln(string(apierr.DumpRequest(true)))  // Prints the serialized HTTP request\n\t\tprintln(string(apierr.DumpResponse(true))) // Prints the serialized HTTP response\n\t}\n\tpanic(err.Error()) // GET "/v1/messages": 400 Bad Request { ... }\n}\n```\n\nWhen other errors occur, they are returned unwrapped; for example,\nif HTTP transport fails, you might receive `*url.Error` wrapping `*net.OpError`.\n\n### Timeouts\n\nRequests do not time out by default; use context to configure a timeout for a request lifecycle.\n\nNote that if a request is [retried](#retries), the context timeout does not start over.\nTo set a per-retry timeout, use `SDK_PackageOptionName.WithRequestTimeout()`.\n\n```go\n// This sets the timeout for the request, including all the retries.\nctx, cancel := context.WithTimeout(context.Background(), 5*time.Minute)\ndefer cancel()\nclient.Messages.New(\n\tctx,\n\tsam.MessageNewParams{\n\t\tMaxTokens: sam.F(int64(1024)),\n\t\tMessages: sam.F([]sam.MessageNewParamsMessage{{\n\t\t\tContent: sam.F[sam.MessageNewParamsMessagesContentUnion](shared.UnionString("Hello, world")),\n\t\t\tRole:    sam.F(sam.MessageNewParamsMessagesRoleUser),\n\t\t}}),\n\t\tModel: sam.F("claude-3-7-sonnet-20250219"),\n\t},\n\t// This sets the per-retry timeout\n\toption.WithRequestTimeout(20*time.Second),\n)\n```\n\n### File uploads\n\nRequest parameters that correspond to file uploads in multipart requests are typed as\n`param.Field[io.Reader]`. The contents of the `io.Reader` will by default be sent as a multipart form\npart with the file name of "anonymous_file" and content-type of "application/octet-stream".\n\nThe file name and content-type can be customized by implementing `Name() string` or `ContentType()\nstring` on the run-time type of `io.Reader`. Note that `os.File` implements `Name() string`, so a\nfile returned by `os.Open` will be sent with the file name on disk.\n\nWe also provide a helper `SDK_PackageName.FileParam(reader io.Reader, filename string, contentType string)`\nwhich can be used to wrap any `io.Reader` with the appropriate file name and content type.\n\n\n\n### Retries\n\nCertain errors will be automatically retried 2 times by default, with a short exponential backoff.\nWe retry by default all connection errors, 408 Request Timeout, 409 Conflict, 429 Rate Limit,\nand >=500 Internal errors.\n\nYou can use the `WithMaxRetries` option to configure or disable this:\n\n```go\n// Configure the default for all requests:\nclient := sam.NewClient(\n\toption.WithMaxRetries(0), // default is 2\n)\n\n// Override per-request:\nclient.Messages.New(\n\tcontext.TODO(),\n\tsam.MessageNewParams{\n\t\tMaxTokens: sam.F(int64(1024)),\n\t\tMessages: sam.F([]sam.MessageNewParamsMessage{{\n\t\t\tContent: sam.F[sam.MessageNewParamsMessagesContentUnion](shared.UnionString("Hello, world")),\n\t\t\tRole:    sam.F(sam.MessageNewParamsMessagesRoleUser),\n\t\t}}),\n\t\tModel: sam.F("claude-3-7-sonnet-20250219"),\n\t},\n\toption.WithMaxRetries(5),\n)\n```\n\n\n### Accessing raw response data (e.g. response headers)\n\nYou can access the raw HTTP response data by using the `option.WithResponseInto()` request option. This is useful when\nyou need to examine response headers, status codes, or other details.\n\n```go\n// Create a variable to store the HTTP response\nvar response *http.Response\nmessage, err := client.Messages.New(\n\tcontext.TODO(),\n\tsam.MessageNewParams{\n\t\tMaxTokens: sam.F(int64(1024)),\n\t\tMessages: sam.F([]sam.MessageNewParamsMessage{{\n\t\t\tContent: sam.F[sam.MessageNewParamsMessagesContentUnion](shared.UnionString("Hello, world")),\n\t\t\tRole:    sam.F(sam.MessageNewParamsMessagesRoleUser),\n\t\t}}),\n\t\tModel: sam.F("claude-3-7-sonnet-20250219"),\n\t},\n\toption.WithResponseInto(&response),\n)\nif err != nil {\n\t// handle error\n}\nfmt.Printf("%+v\\n", message)\n\nfmt.Printf("Status Code: %d\\n", response.StatusCode)\nfmt.Printf("Headers: %+#v\\n", response.Header)\n```\n\n### Making custom/undocumented requests\n\nThis library is typed for convenient access to the documented API. If you need to access undocumented\nendpoints, params, or response properties, the library can still be used.\n\n#### Undocumented endpoints\n\nTo make requests to undocumented endpoints, you can use `client.Get`, `client.Post`, and other HTTP verbs.\n`RequestOptions` on the client, such as retries, will be respected when making these requests.\n\n```go\nvar (\n    // params can be an io.Reader, a []byte, an encoding/json serializable object,\n    // or a "…Params" struct defined in this library.\n    params map[string]interface{}\n\n    // result can be an []byte, *http.Response, a encoding/json deserializable object,\n    // or a model defined in this library.\n    result *http.Response\n)\nerr := client.Post(context.Background(), "/unspecified", params, &result)\nif err != nil {\n    …\n}\n```\n\n#### Undocumented request params\n\nTo make requests using undocumented parameters, you may use either the `SDK_PackageOptionName.WithQuerySet()`\nor the `SDK_PackageOptionName.WithJSONSet()` methods.\n\n```go\nparams := FooNewParams{\n    ID:   SDK_PackageName.F("id_xxxx"),\n    Data: SDK_PackageName.F(FooNewParamsData{\n        FirstName: SDK_PackageName.F("John"),\n    }),\n}\nclient.Foo.New(context.Background(), params, SDK_PackageOptionName.WithJSONSet("data.last_name", "Doe"))\n```\n\n#### Undocumented response properties\n\nTo access undocumented response properties, you may either access the raw JSON of the response as a string\nwith `result.JSON.RawJSON()`, or get the raw JSON of a particular field on the result with\n`result.JSON.Foo.Raw()`.\n\nAny fields that are not present on the response struct will be saved and can be accessed by `result.JSON.ExtraFields()` which returns the extra fields as a `map[string]Field`.\n\n### Middleware\n\nWe provide `SDK_PackageOptionName.WithMiddleware` which applies the given\nmiddleware to requests.\n\n```go\nfunc Logger(req *http.Request, next SDK_PackageOptionName.MiddlewareNext) (res *http.Response, err error) {\n\t// Before the request\n\tstart := time.Now()\n\tLogReq(req)\n\n\t// Forward the request to the next handler\n\tres, err = next(req)\n\n\t// Handle stuff after the request\n\tend := time.Now()\n\tLogRes(res, err, start - end)\n\n    return res, err\n}\n\nclient := SDK_PackageName.SDK_ClientInitializerName(\n\tSDK_PackageOptionName.WithMiddleware(Logger),\n)\n```\n\nWhen multiple middlewares are provided as variadic arguments, the middlewares\nare applied left to right. If `SDK_PackageOptionName.WithMiddleware` is given\nmultiple times, for example first in the client then the method, the\nmiddleware in the client will run first and the middleware given in the method\nwill run next.\n\nYou may also replace the default `http.Client` with\n`SDK_PackageOptionName.WithHTTPClient(client)`. Only one http client is\naccepted (this overwrites any previous client) and receives requests after any\nmiddleware has been applied.\n\n## Semantic versioning\n\nThis package generally follows [SemVer](https://semver.org/spec/v2.0.0.html) conventions, though certain backwards-incompatible changes may be released as minor versions:\n\n1. Changes to library internals which are technically public but not intended or documented for external use. _(Please open a GitHub issue to let us know if you are relying on such internals.)_\n2. Changes that we do not expect to impact the vast majority of users in practice.\n\nWe take backwards-compatibility seriously and work hard to ensure you can rely on a smooth upgrade experience.\n\nWe are keen for your feedback; please open an [issue](https://www.github.com/DefinitelyATestOrg/sam-go/issues) with questions, bugs, or suggestions.\n\n## Contributing\n\nSee [the contributing documentation](./CONTRIBUTING.md).\n',
  },
  {
    language: 'terraform',
    content:
      '# Sam Terraform Provider\n\nThe [Sam Terraform provider](https://registry.terraform.io/providers/DefinitelyATestOrg/sam/latest/docs) provides convenient access to\nthe [Sam REST API](https://docs.sam.com) from Terraform.\n\nIt is generated with [Stainless](https://www.stainless.com/).\n\n## Requirements\n\nThis provider requires Terraform CLI 1.0 or later. You can [install it for your system](https://developer.hashicorp.com/terraform/install)\non Hashicorp\'s website.\n\n## Usage\n\nAdd the following to your `main.tf` file:\n\n<!-- x-release-please-start-version -->\n\n```hcl\n# Declare the provider and version\nterraform {\n  required_providers {\n    SDK_ProviderTypeName = {\n      source  = "DefinitelyATestOrg/sam"\n      version = "~> 0.0.1"\n    }\n  }\n}\n\n# Initialize the provider\nprovider "sam" {\n  # API key used for authenticating requests via the Authorization header\n  api_key = "My API Key" # or set API_KEY env variable\n}\n\n# Configure a resource\n\n```\n\n<!-- x-release-please-end -->\n\nInitialize your project by running `terraform init` in the directory.\n\nAdditional examples can be found in the [./examples](./examples) folder within this repository, and you can\nrefer to the full documentation on [the Terraform Registry](https://registry.terraform.io/providers/DefinitelyATestOrg/sam/latest/docs).\n\n### Provider Options\nWhen you initialize the provider, the following options are supported. It is recommended to use environment variables for sensitive values like access tokens.\nIf an environment variable is provided, then the option does not need to be set in the terraform source.\n\n| Property | Environment variable | Required | Default value |\n| -------- | -------------------- | -------- | ------------- |\n| api_key  | `API_KEY`            | true     | —             |\n\n\n## Semantic versioning\n\nThis package generally follows [SemVer](https://semver.org/spec/v2.0.0.html) conventions, though certain backwards-incompatible changes may be released as minor versions:\n\n1. Changes to library internals which are technically public but not intended or documented for external use. _(Please open a GitHub issue to let us know if you are relying on such internals.)_\n2. Changes that we do not expect to impact the vast majority of users in practice.\n\nWe take backwards-compatibility seriously and work hard to ensure you can rely on a smooth upgrade experience.\n\nWe are keen for your feedback; please open an [issue](https://www.github.com/DefinitelyATestOrg/terraform-provider-sam/issues) with questions, bugs, or suggestions.\n\n## Contributing\n\nSee [the contributing documentation](./CONTRIBUTING.md).\n',
  },
  {
    language: 'typescript',
    content:
      "# Sam TypeScript API Library\n\n[![NPM version](https://img.shields.io/npm/v/sam.svg?label=npm%20(stable))](https://npmjs.org/package/sam) ![npm bundle size](https://img.shields.io/bundlephobia/minzip/sam)\n\nThis library provides convenient access to the Sam REST API from server-side TypeScript or JavaScript.\n\n\n\nThe REST API documentation can be found on [docs.sam.com](https://docs.sam.com). The full API of this library can be found in [api.md](api.md).\n\nIt is generated with [Stainless](https://www.stainless.com/).\n\n## MCP Server\n\nUse the Sam MCP Server to enable AI assistants to interact with this API, allowing them to explore endpoints, make test requests, and use documentation to help integrate this SDK into your application.\n\n[![Add to Cursor](https://cursor.com/deeplink/mcp-install-dark.svg)](https://cursor.com/en-US/install-mcp?name=sam-mcp&config=eyJjb21tYW5kIjoibnB4IiwiYXJncyI6WyIteSIsInNhbS1tY3AiXSwiZW52Ijp7IkFQSV9LRVkiOiJNeSBBUEkgS2V5In19)\n[![Install in VS Code](https://img.shields.io/badge/_-Add_to_VS_Code-blue?style=for-the-badge&logo=data:image/svg%2bxml;base64,)](https://vscode.stainless.com/mcp/%7B%22name%22%3A%22sam-mcp%22%2C%22command%22%3A%22npx%22%2C%22args%22%3A%5B%22-y%22%2C%22sam-mcp%22%5D%2C%22env%22%3A%7B%22API_KEY%22%3A%22My%20API%20Key%22%7D%7D)\n\n> Note: You may need to set environment variables in your MCP client.\n\n## Installation\n\n```sh\nnpm install git+ssh://git@github.com:DefinitelyATestOrg/sam-node.git\n```\n> [!NOTE]\n> Once this package is [published to npm](https://www.stainless.com/docs/guides/publish), this will become: `npm install sam`\n\n\n\n## Usage\n\nThe full API of this library can be found in [api.md](api.md).\n\n<!-- prettier-ignore -->\n```js\nimport Sam from 'sam';\n\nconst client = new Sam();\n\nconst message = await client.messages.create({\n  max_tokens: 1024,\n  messages: [{ content: 'Hello, world', role: 'user' }],\n  model: 'claude-3-7-sonnet-20250219',\n});\n\nconsole.log(message.id);\n```\n\n\n\n### Request & Response types\n\nThis library includes TypeScript definitions for all request params and response fields. You may import and use them like so:\n\n<!-- prettier-ignore -->\n```ts\nimport Sam from 'sam';\n\nconst client = new Sam();\n\nconst params: Sam.MessageCreateParams = {\n  max_tokens: 1024,\n  messages: [{ content: 'Hello, world', role: 'user' }],\n  model: 'claude-3-7-sonnet-20250219',\n};\nconst message: Sam.MessageCreateResponse = await client.messages.create(params);\n```\n\nDocumentation for each method, request param, and response field are available in docstrings and will appear on hover in most modern editors.\n\n\n\n\n\n## Handling errors\n\nWhen the library is unable to connect to the API,\nor if the API returns a non-success status code (i.e., 4xx or 5xx response),\na subclass of `APIError` will be thrown:\n\n<!-- prettier-ignore -->\n```ts\nconst message = await client.messages\n  .create({\n    max_tokens: 1024,\n    messages: [{ content: 'Hello, world', role: 'user' }],\n    model: 'claude-3-7-sonnet-20250219',\n  })\n  .catch(async (err) => {\n    if (err instanceof Sam.APIError) {\n      console.log(err.status); // 400\n      console.log(err.name); // BadRequestError\n      console.log(err.headers); // {server: 'nginx', ...}\n    } else {\n      throw err;\n    }\n  });\n```\n\nError codes are as follows:\n\n| Status Code | Error Type                 |\n| ----------- | -------------------------- |\n| 400         | `BadRequestError`          |\n| 401         | `AuthenticationError`      |\n| 403         | `PermissionDeniedError`    |\n| 404         | `NotFoundError`            |\n| 422         | `UnprocessableEntityError` |\n| 429         | `RateLimitError`           |\n| >=500       | `InternalServerError`      |\n| N/A         | `APIConnectionError`       |\n\n### Retries\n\nCertain errors will be automatically retried 2 times by default, with a short exponential backoff.\nConnection errors (for example, due to a network connectivity problem), 408 Request Timeout, 409 Conflict,\n429 Rate Limit, and >=500 Internal errors will all be retried by default.\n\nYou can use the `maxRetries` option to configure or disable this:\n\n<!-- prettier-ignore -->\n```js\n// Configure the default for all requests:\nconst client = new Sam({\n  apiKey: 'My API Key',\n  maxRetries: 0, // default is 2\n});\n\n// Or, configure per-request:\nawait client.messages.create({\n  max_tokens: 1024,\n  messages: [{ content: 'Hello, world', role: 'user' }],\n  model: 'claude-3-7-sonnet-20250219',\n}, {\n  maxRetries: 5,\n});\n```\n\n### Timeouts\n\nRequests time out after 1 minute by default. You can configure this with a `timeout` option:\n\n<!-- prettier-ignore -->\n```ts\n// Configure the default for all requests:\nconst client = new Sam({\n  apiKey: 'My API Key',\n  timeout: 20 * 1000, // 20 seconds (default is 1 minute)\n});\n\n// Override per-request:\nawait client.messages.create({\n  max_tokens: 1024,\n  messages: [{ content: 'Hello, world', role: 'user' }],\n  model: 'claude-3-7-sonnet-20250219',\n}, {\n  timeout: 5 * 1000,\n});\n```\n\nOn timeout, an `APIConnectionTimeoutError` is thrown.\n\nNote that requests which time out will be [retried twice by default](#retries).\n\n\n\n\n\n## Advanced Usage\n\n### Accessing raw Response data (e.g., headers)\n\nThe \"raw\" `Response` returned by `fetch()` can be accessed through the `.asResponse()` method on the `APIPromise` type that all methods return.\nThis method returns as soon as the headers for a successful response are received and does not consume the response body, so you are free to write custom parsing or streaming logic.\n\nYou can also use the `.withResponse()` method to get the raw `Response` along with the parsed data.\nUnlike `.asResponse()` this method consumes the body, returning once it is parsed.\n\n<!-- prettier-ignore -->\n```ts\nconst client = new Sam();\n\nconst response = await client.messages\n  .create({\n    max_tokens: 1024,\n    messages: [{ content: 'Hello, world', role: 'user' }],\n    model: 'claude-3-7-sonnet-20250219',\n  })\n  .asResponse();\nconsole.log(response.headers.get('X-My-Header'));\nconsole.log(response.statusText); // access the underlying Response object\n\nconst { data: message, response: raw } = await client.messages\n  .create({\n    max_tokens: 1024,\n    messages: [{ content: 'Hello, world', role: 'user' }],\n    model: 'claude-3-7-sonnet-20250219',\n  })\n  .withResponse();\nconsole.log(raw.headers.get('X-My-Header'));\nconsole.log(message.id);\n```\n\n### Logging\n\n> [!IMPORTANT]\n> All log messages are intended for debugging only. The format and content of log messages\n> may change between releases.\n\n#### Log levels\n\nThe log level can be configured in two ways:\n\n1. Via the `SAM_LOG` environment variable\n2. Using the `logLevel` client option (overrides the environment variable if set)\n\n```ts\nimport Sam from 'sam';\n\nconst client = new Sam({\n  logLevel: 'debug', // Show all log messages\n});\n```\n\nAvailable log levels, from most to least verbose:\n\n- `'debug'` - Show debug messages, info, warnings, and errors\n- `'info'` - Show info messages, warnings, and errors\n- `'warn'` - Show warnings and errors (default)\n- `'error'` - Show only errors\n- `'off'` - Disable all logging\n\nAt the `'debug'` level, all HTTP requests and responses are logged, including headers and bodies.\nSome authentication-related headers are redacted, but sensitive data in request and response bodies\nmay still be visible.\n\n#### Custom logger\n\nBy default, this library logs to `globalThis.console`. You can also provide a custom logger.\nMost logging libraries are supported, including [pino](https://www.npmjs.com/package/pino), [winston](https://www.npmjs.com/package/winston), [bunyan](https://www.npmjs.com/package/bunyan), [consola](https://www.npmjs.com/package/consola), [signale](https://www.npmjs.com/package/signale), and [@std/log](https://jsr.io/@std/log). If your logger doesn't work, please open an issue.\n\nWhen providing a custom logger, the `logLevel` option still controls which messages are emitted, messages\nbelow the configured level will not be sent to your logger.\n\n```ts\nimport Sam from 'sam';\nimport pino from 'pino';\n\nconst logger = pino();\n\nconst client = new Sam({\n  logger: logger.child({ name: 'Sam' }),\n  logLevel: 'debug', // Send all messages to pino, allowing it to filter\n});\n```\n\n### Making custom/undocumented requests\n\nThis library is typed for convenient access to the documented API. If you need to access undocumented\nendpoints, params, or response properties, the library can still be used.\n\n#### Undocumented endpoints\n\nTo make requests to undocumented endpoints, you can use `client.get`, `client.post`, and other HTTP verbs.\nOptions on the client, such as retries, will be respected when making these requests.\n\n```ts\nawait client.post('/some/path', {\n  body: { some_prop: 'foo' },\n  query: { some_query_arg: 'bar' },\n});\n```\n\n#### Undocumented request params\n\nTo make requests using undocumented parameters, you may use `// @ts-expect-error` on the undocumented\nparameter. This library doesn't validate at runtime that the request matches the type, so any extra values you\nsend will be sent as-is.\n\n```ts\nclient.messages.create({\n  // ...\n  // @ts-expect-error baz is not yet public\n  baz: 'undocumented option',\n});\n```\n\nFor requests with the `GET` verb, any extra params will be in the query, all other requests will send the\nextra param in the body.\n\nIf you want to explicitly send an extra argument, you can do so with the `query`, `body`, and `headers` request\noptions.\n\n#### Undocumented response properties\n\nTo access undocumented response properties, you may access the response object with `// @ts-expect-error` on\nthe response object, or cast the response object to the requisite type. Like the request params, we do not\nvalidate or strip extra properties from the response from the API.\n\n### Customizing the fetch client\n\nBy default, this library expects a global `fetch` function is defined.\n\nIf you want to use a different `fetch` function, you can either polyfill the global:\n\n```ts\nimport fetch from 'my-fetch';\n\nglobalThis.fetch = fetch;\n```\n\nOr pass it to the client:\n\n```ts\nimport Sam from 'sam';\nimport fetch from 'my-fetch';\n\nconst client = new Sam({ fetch });\n```\n\n### Fetch options\n\nIf you want to set custom `fetch` options without overriding the `fetch` function, you can provide a `fetchOptions` object when instantiating the client or making a request. (Request-specific options override client options.)\n\n```ts\nimport Sam from 'sam';\n\nconst client = new Sam({\n  fetchOptions: {\n    // `RequestInit` options\n  },\n});\n```\n\n#### Configuring proxies\n\nTo modify proxy behavior, you can provide custom `fetchOptions` that add runtime-specific proxy\noptions to requests:\n\n<img src=\"https://raw.githubusercontent.com/stainless-api/sdk-assets/refs/heads/main/node.svg\" align=\"top\" width=\"18\" height=\"21\"> **Node** <sup>[[docs](https://github.com/nodejs/undici/blob/main/docs/docs/api/ProxyAgent.md#example---proxyagent-with-fetch)]</sup>\n\n```ts\nimport Sam from 'sam';\nimport * as undici from 'undici';\n\nconst proxyAgent = new undici.ProxyAgent('http://localhost:8888');\nconst client = new Sam({\n  fetchOptions: {\n    dispatcher: proxyAgent,\n  },\n});\n```\n\n<img src=\"https://raw.githubusercontent.com/stainless-api/sdk-assets/refs/heads/main/bun.svg\" align=\"top\" width=\"18\" height=\"21\"> **Bun** <sup>[[docs](https://bun.sh/guides/http/proxy)]</sup>\n\n```ts\nimport Sam from 'sam';\n\nconst client = new Sam({\n  fetchOptions: {\n    proxy: 'http://localhost:8888',\n  },\n});\n```\n\n<img src=\"https://raw.githubusercontent.com/stainless-api/sdk-assets/refs/heads/main/deno.svg\" align=\"top\" width=\"18\" height=\"21\"> **Deno** <sup>[[docs](https://docs.deno.com/api/deno/~/Deno.createHttpClient)]</sup>\n\n```ts\nimport Sam from 'npm:sam';\n\nconst httpClient = Deno.createHttpClient({ proxy: { url: 'http://localhost:8888' } });\nconst client = new Sam({\n  fetchOptions: {\n    client: httpClient,\n  },\n});\n```\n\n## Frequently Asked Questions\n\n## Semantic versioning\n\nThis package generally follows [SemVer](https://semver.org/spec/v2.0.0.html) conventions, though certain backwards-incompatible changes may be released as minor versions:\n\n1. Changes that only affect static types, without breaking runtime behavior.\n2. Changes to library internals which are technically public but not intended or documented for external use. _(Please open a GitHub issue to let us know if you are relying on such internals.)_\n3. Changes that we do not expect to impact the vast majority of users in practice.\n\nWe take backwards-compatibility seriously and work hard to ensure you can rely on a smooth upgrade experience.\n\nWe are keen for your feedback; please open an [issue](https://www.github.com/DefinitelyATestOrg/sam-node/issues) with questions, bugs, or suggestions.\n\n## Requirements\n\nTypeScript >= 4.9 is supported.\n\nThe following runtimes are supported:\n\n- Web browsers (Up-to-date Chrome, Firefox, Safari, Edge, and more)\n- Node.js 20 LTS or later ([non-EOL](https://endoflife.date/nodejs)) versions.\n- Deno v1.28.0 or higher.\n- Bun 1.0 or later.\n- Cloudflare Workers.\n- Vercel Edge Runtime.\n- Jest 28 or greater with the `\"node\"` environment (`\"jsdom\"` is not supported at this time).\n- Nitro v2.6 or greater.\n\nNote that React Native is not supported at this time.\n\nIf you are interested in other runtime environments, please open or upvote an issue on GitHub.\n\n## Contributing\n\nSee [the contributing documentation](./CONTRIBUTING.md).\n",
  },
  {
    language: 'ruby',
    content:
      '# Sam Ruby API library\n\nThe Sam Ruby library provides convenient access to the Sam REST API from any Ruby 3.2.0+ application. It ships with comprehensive types & docstrings in Yard, RBS, and RBI – [see below](https://github.com/DefinitelyATestOrg/sam-ruby#Sorbet) for usage with Sorbet. The standard library\'s `net/http` is used as the HTTP transport, with connection pooling via the `connection_pool` gem.\n\n\n\nIt is generated with [Stainless](https://www.stainless.com/).\n\n## MCP Server\n\nUse the Sam MCP Server to enable AI assistants to interact with this API, allowing them to explore endpoints, make test requests, and use documentation to help integrate this SDK into your application.\n\n[![Add to Cursor](https://cursor.com/deeplink/mcp-install-dark.svg)](https://cursor.com/en-US/install-mcp?name=sam-mcp&config=eyJjb21tYW5kIjoibnB4IiwiYXJncyI6WyIteSIsInNhbS1tY3AiXSwiZW52Ijp7IkFQSV9LRVkiOiJNeSBBUEkgS2V5In19)\n[![Install in VS Code](https://img.shields.io/badge/_-Add_to_VS_Code-blue?style=for-the-badge&logo=data:image/svg%2bxml;base64,)](https://vscode.stainless.com/mcp/%7B%22name%22%3A%22sam-mcp%22%2C%22command%22%3A%22npx%22%2C%22args%22%3A%5B%22-y%22%2C%22sam-mcp%22%5D%2C%22env%22%3A%7B%22API_KEY%22%3A%22My%20API%20Key%22%7D%7D)\n\n> Note: You may need to set environment variables in your MCP client.\n\n## Documentation\n\nDocumentation for releases of this gem can be found [on RubyDoc](https://gemdocs.org/gems/sam).\n\nThe REST API documentation can be found on [docs.sam.com](https://docs.sam.com).\n\n## Installation\n\nTo use this gem, install via Bundler by adding the following to your application\'s `Gemfile`:\n\n<!-- x-release-please-start-version -->\n\n```ruby\ngem "sam", "~> 0.0.1"\n```\n\n<!-- x-release-please-end -->\n\n## Usage\n\n```ruby\nrequire "bundler/setup"\nrequire "sam"\n\nsam = Sam::Client.new(api_key: "My API Key")\n\nmessage = sam.messages.create\n\nputs(message.id)\n```\n\n\n\n\n\n\n\n### Handling errors\n\nWhen the library is unable to connect to the API, or if the API returns a non-success status code (i.e., 4xx or 5xx response), a subclass of `Sam::Errors::APIError` will be thrown:\n\n```ruby\nbegin\n  message = sam.messages.create\nrescue Sam::Errors::APIConnectionError => e\n  puts("The server could not be reached")\n  puts(e.cause)  # an underlying Exception, likely raised within `net/http`\nrescue Sam::Errors::RateLimitError => e\n  puts("A 429 status code was received; we should back off a bit.")\nrescue Sam::Errors::APIStatusError => e\n  puts("Another non-200-range status code was received")\n  puts(e.status)\nend\n```\n\nError codes are as follows:\n\n| Cause            | Error Type                 |\n| ---------------- | -------------------------- |\n| HTTP 400         | `BadRequestError`          |\n| HTTP 401         | `AuthenticationError`      |\n| HTTP 403         | `PermissionDeniedError`    |\n| HTTP 404         | `NotFoundError`            |\n| HTTP 409         | `ConflictError`            |\n| HTTP 422         | `UnprocessableEntityError` |\n| HTTP 429         | `RateLimitError`           |\n| HTTP >= 500      | `InternalServerError`      |\n| Other HTTP error | `APIStatusError`           |\n| Timeout          | `APITimeoutError`          |\n| Network error    | `APIConnectionError`       |\n\n### Retries\n\nCertain errors will be automatically retried 2 times by default, with a short exponential backoff.\n\nConnection errors (for example, due to a network connectivity problem), 408 Request Timeout, 409 Conflict, 429 Rate Limit, >=500 Internal errors, and timeouts will all be retried by default.\n\nYou can use the `max_retries` option to configure or disable this:\n\n```ruby\n# Configure the default for all requests:\nsam = Sam::Client.new(\n  max_retries: 0, # default is 2\n  api_key: "My API Key"\n)\n\n# Or, configure per-request:\nsam.messages.create(request_options: {max_retries: 5})\n```\n\n### Timeouts\n\nBy default, requests will time out after 60 seconds. You can use the timeout option to configure or disable this:\n\n```ruby\n# Configure the default for all requests:\nsam = Sam::Client.new(\n  timeout: nil, # default is 60\n  api_key: "My API Key"\n)\n\n# Or, configure per-request:\nsam.messages.create(request_options: {timeout: 5})\n```\n\nOn timeout, `Sam::Errors::APITimeoutError` is raised.\n\nNote that requests that time out are retried by default.\n\n## Advanced concepts\n\n### BaseModel\n\nAll parameter and response objects inherit from `Sam::Internal::Type::BaseModel`, which provides several conveniences, including:\n\n1. All fields, including unknown ones, are accessible with `obj[:prop]` syntax, and can be destructured with `obj => {prop: prop}` or pattern-matching syntax.\n\n2. Structural equivalence for equality; if two API calls return the same values, comparing the responses with == will return true.\n\n3. Both instances and the classes themselves can be pretty-printed.\n\n4. Helpers such as `#to_h`, `#deep_to_h`, `#to_json`, and `#to_yaml`.\n\n### Making custom or undocumented requests\n\n#### Undocumented properties\n\nYou can send undocumented parameters to any endpoint, and read undocumented response properties, like so:\n\nNote: the `extra_` parameters of the same name overrides the documented parameters.\n\n```ruby\nmessage =\n  sam.messages.create(\n    request_options: {\n      extra_query: {my_query_parameter: value},\n      extra_body: {my_body_parameter: value},\n      extra_headers: {"my-header": value}\n    }\n  )\n\nputs(message[:my_undocumented_property])\n```\n\n#### Undocumented request params\n\nIf you want to explicitly send an extra param, you can do so with the `extra_query`, `extra_body`, and `extra_headers` under the `request_options:` parameter when making a request, as seen in the examples above.\n\n#### Undocumented endpoints\n\nTo make requests to undocumented endpoints while retaining the benefit of auth, retries, and so on, you can make requests using `client.request`, like so:\n\n```ruby\nresponse = client.request(\n  method: :post,\n  path: \'/undocumented/endpoint\',\n  query: {"dog": "woof"},\n  headers: {"useful-header": "interesting-value"},\n  body: {"hello": "world"}\n)\n```\n\n### Concurrency & connection pooling\n\nThe `Sam::Client` instances are threadsafe, but are only are fork-safe when there are no in-flight HTTP requests.\n\nEach instance of `Sam::Client` has its own HTTP connection pool with a default size of 99. As such, we recommend instantiating the client once per application in most settings.\n\nWhen all available connections from the pool are checked out, requests wait for a new connection to become available, with queue time counting towards the request timeout.\n\nUnless otherwise specified, other classes in the SDK do not have locks protecting their underlying data structure.\n\n## Sorbet\n\nThis library provides comprehensive [RBI](https://sorbet.org/docs/rbi) definitions, and has no dependency on sorbet-runtime.\n\nYou can provide typesafe request parameters like so:\n\n```ruby\nsam.messages.create \n```\n\nOr, equivalently:\n\n```ruby\n# Hashes work, but are not typesafe:\nsam.messages.create\n\n# You can also splat a full Params class:\nparams = Sam::MessageCreateParams.new\nsam.messages.create(**params)\n```\n\n\n\n## Versioning\n\nThis package follows [SemVer](https://semver.org/spec/v2.0.0.html) conventions. As the library is in initial development and has a major version of `0`, APIs may change at any time.\n\nThis package considers improvements to the (non-runtime) `*.rbi` and `*.rbs` type definitions to be non-breaking changes.\n\n## Requirements\n\nRuby 3.2.0 or higher.\n\n## Contributing\n\nSee [the contributing documentation](https://github.com/DefinitelyATestOrg/sam-ruby/tree/main/CONTRIBUTING.md).\n',
  },
  {
    language: 'java',
    content:
      '# Sam Java API Library\n\n<!-- x-release-please-start-version -->\n[![Maven Central](https://img.shields.io/maven-central/v/me.elborai.api/sam-java)](https://central.sonatype.com/artifact/me.elborai.api/sam-java/0.0.1)\n[![javadoc](https://javadoc.io/badge2/me.elborai.api/sam-java/0.0.1/javadoc.svg)](https://javadoc.io/doc/me.elborai.api/sam-java/0.0.1)\n<!-- x-release-please-end -->\n\nThe Sam Java SDK provides convenient access to the [Sam REST API](https://docs.sam.com)   from applications written in Java.\n\n\n\nIt is generated with [Stainless](https://www.stainless.com/).\n\n## MCP Server\n\nUse the Sam MCP Server to enable AI assistants to interact with this API, allowing them to explore endpoints, make test requests, and use documentation to help integrate this SDK into your application.\n\n[![Add to Cursor](https://cursor.com/deeplink/mcp-install-dark.svg)](https://cursor.com/en-US/install-mcp?name=sam-mcp&config=eyJjb21tYW5kIjoibnB4IiwiYXJncyI6WyIteSIsInNhbS1tY3AiXSwiZW52Ijp7IkFQSV9LRVkiOiJNeSBBUEkgS2V5In19)\n[![Install in VS Code](https://img.shields.io/badge/_-Add_to_VS_Code-blue?style=for-the-badge&logo=data:image/svg%2bxml;base64,)](https://vscode.stainless.com/mcp/%7B%22name%22%3A%22sam-mcp%22%2C%22command%22%3A%22npx%22%2C%22args%22%3A%5B%22-y%22%2C%22sam-mcp%22%5D%2C%22env%22%3A%7B%22API_KEY%22%3A%22My%20API%20Key%22%7D%7D)\n\n> Note: You may need to set environment variables in your MCP client.\n\n<!-- x-release-please-start-version -->\n\nThe REST API documentation can be found on [docs.sam.com](https://docs.sam.com). Javadocs are available on [javadoc.io](https://javadoc.io/doc/me.elborai.api/sam-java/0.0.1).\n\n<!-- x-release-please-end -->\n\n## Installation\n\n<!-- x-release-please-start-version -->\n\n### Gradle\n\n~~~kotlin\nimplementation("me.elborai.api:sam-java:0.0.1")\n~~~\n\n### Maven\n\n~~~xml\n<dependency>\n  <groupId>me.elborai.api</groupId>\n  <artifactId>sam-java</artifactId>\n  <version>0.0.1</version>\n</dependency>\n~~~\n\n<!-- x-release-please-end -->\n\n## Requirements\n\nThis library requires Java 8 or later.\n\n## Usage\n\n```java\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\nimport me.elborai.api.models.messages.MessageCreateParams;\nimport me.elborai.api.models.messages.MessageCreateResponse;\n\n// Configures using the `sam.apiKey` and `sam.baseUrl` system properties\n// Or configures using the `API_KEY` and `SAM_BASE_URL` environment variables\nSamClient client = SamOkHttpClient.fromEnv();\n\nMessageCreateParams params = MessageCreateParams.builder()\n    .maxTokens(1024L)\n    .addMessage(MessageCreateParams.Message.builder()\n        .content("Hello, world")\n        .role(MessageCreateParams.Message.Role.USER)\n        .build())\n    .model("claude-3-7-sonnet-20250219")\n    .build();\nMessageCreateResponse message = client.messages().create(params);\n```\n\n## Client configuration\n\nConfigure the client using system properties or environment variables:\n\n```java\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\n\n// Configures using the `sam.apiKey` and `sam.baseUrl` system properties\n// Or configures using the `API_KEY` and `SAM_BASE_URL` environment variables\nSamClient client = SamOkHttpClient.fromEnv();\n```\n\nOr manually:\n\n```java\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\n\nSamClient client = SamOkHttpClient.builder()\n    .apiKey("My API Key")\n    .build();\n```\n\nOr using a combination of the two approaches:\n\n```java\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\n\nSamClient client = SamOkHttpClient.builder()\n    // Configures using the `sam.apiKey` and `sam.baseUrl` system properties\n    // Or configures using the `API_KEY` and `SAM_BASE_URL` environment variables\n    .fromEnv()\n    .apiKey("My API Key")\n    .build();\n```\n\nSee this table for the available options:\n\n| Setter    | System property | Environment variable | Required | Default value |\n| --------- | --------------- | -------------------- | -------- | ------------- |\n| `apiKey`  | `sam.apiKey`    | `API_KEY`            | true     | -             |\n| `baseUrl` | `sam.baseUrl`   | `SAM_BASE_URL`       | true     | `"/api/v3"`   |\n\nSystem properties take precedence over environment variables.\n\n> [!TIP]\n> Don\'t create more than one client in the same application. Each client has a connection pool and\n> thread pools, which are more efficient to share between requests.\n\n### Modifying configuration\n\nTo temporarily use a modified client configuration, while reusing the same connection and thread       pools, call `withOptions()` on any client or service:\n\n```java\nimport me.elborai.api.client.SamClient;\n\nSamClient clientWithOptions = client.withOptions(optionsBuilder -> {\n    optionsBuilder.baseUrl("https://example.com");\n    optionsBuilder.maxRetries(42);\n});\n```\n\nThe `withOptions()` method does not affect the original client or service.\n\n## Requests and responses\n\nTo send a request to the Sam API, build an instance of some `Params` class and pass it to the     corresponding client method. When the response is received, it will be deserialized into an instance of     a Java class.\n\nFor example, `client.messages().create(...)` should be called with an instance of `MessageCreateParams`, and it     will return an instance of `MessageCreateResponse`.\n\n## Immutability\n\nEach class in the SDK has an associated   [builder](https://blogs.oracle.com/javamagazine/post/exploring-joshua-blochs-builder-design-pattern-in-java)   or factory method for constructing it.\n\nEach class is [immutable](https://docs.oracle.com/javase/tutorial/essential/concurrency/immutable.html)   once constructed. If the class has an associated builder, then it has a `toBuilder()` method, which can   be used to convert it back to a builder for making a modified copy.\n\nBecause each class is immutable, builder modification will _never_ affect already built class instances.\n\n## Asynchronous execution\n\nThe default client is synchronous. To switch to asynchronous execution, call the `async()` method:\n\n```java\nimport java.util.concurrent.CompletableFuture;\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\nimport me.elborai.api.models.messages.MessageCreateParams;\nimport me.elborai.api.models.messages.MessageCreateResponse;\n\n// Configures using the `sam.apiKey` and `sam.baseUrl` system properties\n// Or configures using the `API_KEY` and `SAM_BASE_URL` environment variables\nSamClient client = SamOkHttpClient.fromEnv();\n\nMessageCreateParams params = MessageCreateParams.builder()\n    .maxTokens(1024L)\n    .addMessage(MessageCreateParams.Message.builder()\n        .content("Hello, world")\n        .role(MessageCreateParams.Message.Role.USER)\n        .build())\n    .model("claude-3-7-sonnet-20250219")\n    .build();\nCompletableFuture<MessageCreateResponse> message = client.async().messages().create(params);\n```\n\nOr create an asynchronous client from the beginning:\n\n```java\nimport java.util.concurrent.CompletableFuture;\nimport me.elborai.api.client.SamClientAsync;\nimport me.elborai.api.client.okhttp.SamOkHttpClientAsync;\nimport me.elborai.api.models.messages.MessageCreateParams;\nimport me.elborai.api.models.messages.MessageCreateResponse;\n\n// Configures using the `sam.apiKey` and `sam.baseUrl` system properties\n// Or configures using the `API_KEY` and `SAM_BASE_URL` environment variables\nSamClientAsync client = SamOkHttpClientAsync.fromEnv();\n\nMessageCreateParams params = MessageCreateParams.builder()\n    .maxTokens(1024L)\n    .addMessage(MessageCreateParams.Message.builder()\n        .content("Hello, world")\n        .role(MessageCreateParams.Message.Role.USER)\n        .build())\n    .model("claude-3-7-sonnet-20250219")\n    .build();\nCompletableFuture<MessageCreateResponse> message = client.messages().create(params);\n```\n\nThe asynchronous client supports the same options as the synchronous one, except most methods return `CompletableFuture`s.\n\n\n\n\n\n\n\n## Raw responses\n\nThe SDK defines methods that deserialize responses into instances of Java classes.       However, these methods don\'t provide access to the response headers, status code, or the raw response       body.\n\nTo access this data, prefix any HTTP method call on a client or service with `withRawResponse()`:\n\n```java\nimport me.elborai.api.core.http.Headers;\nimport me.elborai.api.core.http.HttpResponseFor;\nimport me.elborai.api.models.messages.MessageCreateParams;\nimport me.elborai.api.models.messages.MessageCreateResponse;\n\nMessageCreateParams params = MessageCreateParams.builder()\n    .maxTokens(1024L)\n    .addMessage(MessageCreateParams.Message.builder()\n        .content("Hello, world")\n        .role(MessageCreateParams.Message.Role.USER)\n        .build())\n    .model("claude-3-7-sonnet-20250219")\n    .build();\nHttpResponseFor<MessageCreateResponse> message = client.messages().withRawResponse().create(params);\n\nint statusCode = message.statusCode();\nHeaders headers = message.headers();\n```\n\nYou can still deserialize the response into an instance of a Java class if needed:\n\n```java\nimport me.elborai.api.models.messages.MessageCreateResponse;\n\nMessageCreateResponse parsedMessage = message.parse();\n```\n\n## Error handling\n\nThe SDK throws custom unchecked exception types:\n\n- [`SamServiceException`](sam-java-core/src/main/kotlin/me/elborai/api/errors/SamServiceException.kt): Base class for HTTP errors. See this table for which exception       subclass is thrown for each HTTP status code:\n\n  | Status | Exception                                          |\n  | ------ | -------------------------------------------------- |\n  | 400    | [`BadRequestException`](sam-java-core/src/main/kotlin/me/elborai/api/errors/BadRequestException.kt)           |\n  | 401    | [`UnauthorizedException`](sam-java-core/src/main/kotlin/me/elborai/api/errors/UnauthorizedException.kt)         |\n  | 403    | [`PermissionDeniedException`](sam-java-core/src/main/kotlin/me/elborai/api/errors/PermissionDeniedException.kt)     |\n  | 404    | [`NotFoundException`](sam-java-core/src/main/kotlin/me/elborai/api/errors/NotFoundException.kt)             |\n  | 422    | [`UnprocessableEntityException`](sam-java-core/src/main/kotlin/me/elborai/api/errors/UnprocessableEntityException.kt)  |\n  | 429    | [`RateLimitException`](sam-java-core/src/main/kotlin/me/elborai/api/errors/RateLimitException.kt)            |\n  | 5xx    | [`InternalServerException`](sam-java-core/src/main/kotlin/me/elborai/api/errors/InternalServerException.kt)       |\n  | others | [`UnexpectedStatusCodeException`](sam-java-core/src/main/kotlin/me/elborai/api/errors/UnexpectedStatusCodeException.kt) |\n\n- [`SamIoException`](sam-java-core/src/main/kotlin/me/elborai/api/errors/SamIoException.kt): I/O networking errors.\n\n- [`SamRetryableException`](sam-java-core/src/main/kotlin/me/elborai/api/errors/SamRetryableException.kt): Generic error indicating a failure that could be retried by the client.\n\n- [`SamInvalidDataException`](sam-java-core/src/main/kotlin/me/elborai/api/errors/SamInvalidDataException.kt): Failure to interpret successfully parsed data. For example,       when accessing a property that\'s supposed to be required, but the API unexpectedly omitted it from the       response.\n\n- [`SamException`](sam-java-core/src/main/kotlin/me/elborai/api/errors/SamException.kt): Base class for all exceptions. Most errors will result in one of the       previously mentioned ones, but completely generic errors may be thrown using the base class.\n\n\n\n## Logging\n\nThe SDK uses the standard   [OkHttp logging interceptor](https://github.com/square/okhttp/tree/master/okhttp-logging-interceptor).\n\nEnable logging by setting the `SAM_LOG` environment variable to   `info`:\n\n```sh\nexport SAM_LOG=info\n```\n\nOr to `debug` for more verbose logging:\n\n```sh\nexport SAM_LOG=debug\n```\n\n## ProGuard and R8\n\nAlthough the SDK uses reflection, it is still usable with     [ProGuard](https://github.com/Guardsquare/proguard) and     [R8](https://developer.android.com/topic/performance/app-optimization/enable-app-optimization) because     `sam-java-core` is published with a     [configuration file](sam-java-core/src/main/resources/META-INF/proguard/sam-java-core.pro) containing     [keep rules](https://www.guardsquare.com/manual/configuration/usage).\n\nProGuard and R8 should automatically detect and use the published rules, but you can also manually copy     the keep rules if necessary.\n\n\n\n\n\n## Jackson\n\nThe SDK depends on [Jackson](https://github.com/FasterXML/jackson) for JSON     serialization/deserialization. It is compatible with version 2.13.4 or higher,     but depends on version 2.18.2 by default.\n\nThe SDK throws an exception if it detects an incompatible Jackson version at runtime (e.g. if the     default version was overridden in your Maven or Gradle config).\n\nIf the SDK threw an exception, but you\'re _certain_ the version is compatible, then disable the version     check using the `checkJacksonVersionCompatibility` on [`SamOkHttpClient`](sam-java-client-okhttp/src/main/kotlin/me/elborai/api/client/okhttp/SamOkHttpClient.kt) or     [`SamOkHttpClientAsync`](sam-java-client-okhttp/src/main/kotlin/me/elborai/api/client/okhttp/SamOkHttpClientAsync.kt).\n\n> [!CAUTION]\n> We make no guarantee that the SDK works correctly when the Jackson version check is disabled.\n\nAlso note that there are bugs in older Jackson versions that can affect the SDK. We don\'t work around all     Jackson bugs ([example](https://github.com/FasterXML/jackson-databind/issues/3240)) and expect users to     upgrade Jackson for those instead.\n\n## Network options\n\n### Retries\n\nThe SDK automatically retries 2 times by default, with a short exponential backoff between requests.\n\nOnly the following error types are retried:\n- Connection errors (for example, due to a network connectivity problem)\n- 408 Request Timeout\n- 409 Conflict\n- 429 Rate Limit\n- 5xx Internal\n\nThe API may also explicitly instruct the SDK to retry or not retry a request.\n\nTo set a custom number of retries, configure the client using the `maxRetries` method:\n\n```java\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\n\nSamClient client = SamOkHttpClient.builder()\n    .fromEnv()\n    .maxRetries(4)\n    .build();\n```\n\n### Timeouts\n\nRequests time out after 1 minute by default.\n\nTo set a custom timeout, configure the method call using the `timeout` method:\n\n```java\nimport me.elborai.api.models.messages.MessageCreateResponse;\n\nMessageCreateResponse message = client.messages().create(\n  params, RequestOptions.builder().timeout(Duration.ofSeconds(30)).build()\n);\n```\n\nOr configure the default for all method calls at the client level:\n\n```java\nimport java.time.Duration;\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\n\nSamClient client = SamOkHttpClient.builder()\n    .fromEnv()\n    .timeout(Duration.ofSeconds(30))\n    .build();\n```\n\n### Proxies\n\nTo route requests through a proxy, configure the client using the `proxy` method:\n\n```java\nimport java.net.InetSocketAddress;\nimport java.net.Proxy;\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\n\nSamClient client = SamOkHttpClient.builder()\n    .fromEnv()\n    .proxy(new Proxy(\n      Proxy.Type.HTTP, new InetSocketAddress(\n        "https://example.com", 8080\n      )\n    ))\n    .build();\n```\n\n### Connection pooling\n\nTo customize the underlying OkHttp connection pool, configure the client using the   `maxIdleConnections` and `keepAliveDuration` methods:\n\n```java\nimport java.time.Duration;\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\n\nSamClient client = SamOkHttpClient.builder()\n    .fromEnv()\n    // If `maxIdleConnections` is set, then `keepAliveDuration` must be set, and vice versa.\n    .maxIdleConnections(10)\n    .keepAliveDuration(Duration.ofMinutes(2))\n    .build();\n```\n\nIf both options are unset, OkHttp\'s default connection pool settings are used.\n\n### HTTPS\n\n> [!NOTE]\n> Most applications should not call these methods, and instead use the system defaults. The defaults include\n> special optimizations that can be lost if the implementations are modified.\n\nTo configure how HTTPS connections are secured, configure the client using the `sslSocketFactory`,   `trustManager`, and `hostnameVerifier` methods:\n\n```java\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\n\nSamClient client = SamOkHttpClient.builder()\n    .fromEnv()\n    // If `sslSocketFactory` is set, then `trustManager` must be set, and vice versa.\n    .sslSocketFactory(yourSSLSocketFactory)\n    .trustManager(yourTrustManager)\n    .hostnameVerifier(yourHostnameVerifier)\n    .build();\n```\n\n\n\n### Custom HTTP client\n\nThe SDK consists of three artifacts:\n- `sam-java-core`\n  - Contains core SDK logic\n  - Does not depend on [OkHttp](https://square.github.io/okhttp)\n  - Exposes [`SamClient`](sam-java-core/src/main/kotlin/me/elborai/api/client/SamClient.kt), [`SamClientAsync`](sam-java-core/src/main/kotlin/me/elborai/api/client/SamClientAsync.kt),             [`SamClientImpl`](sam-java-core/src/main/kotlin/me/elborai/api/client/SamClientImpl.kt), and [`SamClientAsyncImpl`](sam-java-core/src/main/kotlin/me/elborai/api/client/SamClientAsyncImpl.kt), all of which can             work with any HTTP client\n- `sam-java-client-okhttp`\n  - Depends on [OkHttp](https://square.github.io/okhttp)\n  - Exposes [`SamOkHttpClient`](sam-java-client-okhttp/src/main/kotlin/me/elborai/api/client/okhttp/SamOkHttpClient.kt) and [`SamOkHttpClientAsync`](sam-java-client-okhttp/src/main/kotlin/me/elborai/api/client/okhttp/SamOkHttpClientAsync.kt), which             provide a way to construct [`SamClientImpl`](sam-java-core/src/main/kotlin/me/elborai/api/client/SamClientImpl.kt) and             [`SamClientAsyncImpl`](sam-java-core/src/main/kotlin/me/elborai/api/client/SamClientAsyncImpl.kt), respectively, using OkHttp\n- `sam-java`\n  - Depends on and exposes the APIs of both `sam-java-core` and `sam-java-client-okhttp`\n  - Does not have its own logic\n\nThis structure allows replacing the SDK\'s default HTTP client without pulling in unnecessary dependencies.\n\n#### Customized [`OkHttpClient`](https://square.github.io/okhttp/3.x/okhttp/okhttp3/OkHttpClient.html)\n\n> [!TIP]\n> Try the available [network options](#network-options) before replacing the default client.\n\nTo use a customized `OkHttpClient`:\n\n1. Replace your [`sam-java` dependency](#installation) with `sam-java-core`\n2. Copy `sam-java-client-okhttp`\'s [`OkHttpClient`](sam-java-client-okhttp/src/main/kotlin/me/elborai/api/client/okhttp/OkHttpClient.kt) class into your code and        customize it\n3. Construct [`SamClientImpl`](sam-java-core/src/main/kotlin/me/elborai/api/client/SamClientImpl.kt) or [`SamClientAsyncImpl`](sam-java-core/src/main/kotlin/me/elborai/api/client/SamClientAsyncImpl.kt), similarly to        [`SamOkHttpClient`](sam-java-client-okhttp/src/main/kotlin/me/elborai/api/client/okhttp/SamOkHttpClient.kt) or [`SamOkHttpClientAsync`](sam-java-client-okhttp/src/main/kotlin/me/elborai/api/client/okhttp/SamOkHttpClientAsync.kt), using your        customized client\n\n### Completely custom HTTP client\n\nTo use a completely custom HTTP client:\n\n1. Replace your [`sam-java` dependency](#installation) with `sam-java-core`\n2. Write a class that implements the [`HttpClient`](sam-java-core/src/main/kotlin/me/elborai/api/core/http/HttpClient.kt) interface\n3. Construct [`SamClientImpl`](sam-java-core/src/main/kotlin/me/elborai/api/client/SamClientImpl.kt) or [`SamClientAsyncImpl`](sam-java-core/src/main/kotlin/me/elborai/api/client/SamClientAsyncImpl.kt), similarly to        [`SamOkHttpClient`](sam-java-client-okhttp/src/main/kotlin/me/elborai/api/client/okhttp/SamOkHttpClient.kt) or [`SamOkHttpClientAsync`](sam-java-client-okhttp/src/main/kotlin/me/elborai/api/client/okhttp/SamOkHttpClientAsync.kt), using your new        client class\n\n## Undocumented API functionality\n\nThe SDK is typed for convenient usage of the documented API. However, it also supports working with undocumented or not yet supported parts of the API.\n\n### Parameters\n\nTo set undocumented parameters, call the `putAdditionalHeader`, `putAdditionalQueryParam`, or       `putAdditionalBodyProperty` methods on any `Params` class:\n\n```java\nimport me.elborai.api.core.JsonValue;\nimport me.elborai.api.models.messages.MessageCreateParams;\n\nMessageCreateParams params = MessageCreateParams.builder()\n    .putAdditionalHeader("Secret-Header", "42")\n    .putAdditionalQueryParam("secret_query_param", "42")\n    .putAdditionalBodyProperty("secretProperty", JsonValue.from("42"))\n    .build();\n```\n\nThese can be accessed on the built object later using the `_additionalHeaders()`,       `_additionalQueryParams()`, and `_additionalBodyProperties()` methods.\n\nTo set undocumented parameters on _nested_ headers, query params, or body classes, call the         `putAdditionalProperty` method on the nested class:\n\n```java\nimport me.elborai.api.core.JsonValue;\nimport me.elborai.api.models.messages.MessageCreateParams;\n\nMessageCreateParams params = MessageCreateParams.builder()\n    .metadata(MessageCreateParams.Metadata.builder()\n        .putAdditionalProperty("secretProperty", JsonValue.from("42"))\n        .build())\n    .build();\n```\n\nThese properties can be accessed on the nested built object later using the         `_additionalProperties()` method.\n\nTo set a documented parameter or property to an undocumented or not yet supported _value_, pass a       [`JsonValue`](sam-java-core/src/main/kotlin/me/elborai/api/core/Values.kt) object to its setter:\n\n```java\nimport me.elborai.api.models.messages.MessageCreateParams;\n\nMessageCreateParams params = MessageCreateParams.builder()\n    .maxTokens(1024L)\n    .addMessage(MessageCreateParams.Message.builder()\n        .content("Hello, world")\n        .role(MessageCreateParams.Message.Role.USER)\n        .build())\n    .model("claude-3-7-sonnet-20250219")\n    .build();\n```\n\nThe most straightforward way to create a [`JsonValue`](sam-java-core/src/main/kotlin/me/elborai/api/core/Values.kt) is using its       `from(...)` method:\n\n```java\nimport java.util.List;\nimport java.util.Map;\nimport me.elborai.api.core.JsonValue;\n\n// Create primitive JSON values\nJsonValue nullValue = JsonValue.from(null);\nJsonValue booleanValue = JsonValue.from(true);\nJsonValue numberValue = JsonValue.from(42);\nJsonValue stringValue = JsonValue.from("Hello World!");\n\n// Create a JSON array value equivalent to `["Hello", "World"]`\nJsonValue arrayValue = JsonValue.from(List.of(\n  "Hello", "World"\n));\n\n// Create a JSON object value equivalent to `{ "a": 1, "b": 2 }`\nJsonValue objectValue = JsonValue.from(Map.of(\n  "a", 1,\n  "b", 2\n));\n\n// Create an arbitrarily nested JSON equivalent to:\n// {\n//   "a": [1, 2],\n//   "b": [3, 4]\n// }\nJsonValue complexValue = JsonValue.from(Map.of(\n  "a", List.of(\n    1, 2\n  ),\n  "b", List.of(\n    3, 4\n  )\n));\n```\n\nNormally a `Builder` class\'s `build` method will throw         [`IllegalStateException`](https://docs.oracle.com/javase/8/docs/api/java/lang/IllegalStateException.html)         if any required parameter or property is unset.\n\nTo forcibly omit a required parameter or property, pass [`JsonMissing`](sam-java-core/src/main/kotlin/me/elborai/api/core/Values.kt):\n\n```java\nimport me.elborai.api.core.JsonMissing;\nimport me.elborai.api.models.messages.MessageCreateParams;\n\nMessageCreateParams params = MessageCreateParams.builder()\n    .addMessage(MessageCreateParams.Message.builder()\n        .content("Hello, world")\n        .role(MessageCreateParams.Message.Role.USER)\n        .build())\n    .model("claude-3-7-sonnet-20250219")\n    .maxTokens(JsonMissing.of())\n    .build();\n```\n\n### Response properties\n\nTo access undocumented response properties, call the `_additionalProperties()` method:\n\n```java\nimport java.util.Map;\nimport me.elborai.api.core.JsonValue;\n\nMap<String, JsonValue> additionalProperties = client.messages().create(params)._additionalProperties();\nJsonValue secretPropertyValue = additionalProperties.get("secretProperty");\n\nString result = secretPropertyValue.accept(new JsonValue.Visitor<>() {\n    @Override\n    public String visitNull() {\n        return "It\'s null!";\n    }\n\n    @Override\n    public String visitBoolean(boolean value) {\n        return "It\'s a boolean!";\n    }\n\n    @Override\n    public String visitNumber(Number value) {\n        return "It\'s a number!";\n    }\n\n    // Other methods include `visitMissing`, `visitString`, `visitArray`, and `visitObject`\n    // The default implementation of each unimplemented method delegates to `visitDefault`, which throws by default, but can also be overridden\n});\n```\n\nTo access a property\'s raw JSON value, which may be undocumented, call its `_` prefixed method:\n\n```java\nimport java.util.Optional;\nimport me.elborai.api.core.JsonField;\n\nJsonField<Long> maxTokens = client.messages().create(params)._maxTokens();\n\nif (maxTokens.isMissing()) {\n  // The property is absent from the JSON response\n} else if (maxTokens.isNull()) {\n  // The property was set to literal null\n} else {\n  // Check if value was provided as a string\n  // Other methods include `asNumber()`, `asBoolean()`, etc.\n  Optional<String> jsonString = maxTokens.asString();\n\n  // Try to deserialize into a custom type\n  MyClass myObject = maxTokens.asUnknown().orElseThrow().convert(MyClass.class);\n}\n```\n\n### Response validation\n\nIn rare cases, the API may return a response that doesn\'t match the expected type. For example, the SDK     may expect a property to contain a `String`, but the API could return something else.\n\nBy default, the SDK will not throw an exception in this case. It will throw     [`SamInvalidDataException`](sam-java-core/src/main/kotlin/me/elborai/api/errors/SamInvalidDataException.kt) only if you directly access the property.\n\nIf you would prefer to check that the response is completely well-typed upfront, then either call     `validate()`:\n\n```java\nimport me.elborai.api.models.messages.MessageCreateResponse;\n\nMessageCreateResponse message = client.messages().create(params).validate();\n```\n\nOr configure the method call to validate the response using the `responseValidation` method:\n\n```java\nimport me.elborai.api.models.messages.MessageCreateResponse;\n\nMessageCreateResponse message = client.messages().create(\n  params, RequestOptions.builder().responseValidation(true).build()\n);\n```\n\nOr configure the default for all method calls at the client level:\n\n```java\nimport me.elborai.api.client.SamClient;\nimport me.elborai.api.client.okhttp.SamOkHttpClient;\n\nSamClient client = SamOkHttpClient.builder()\n    .fromEnv()\n    .responseValidation(true)\n    .build();\n```\n\n## FAQ\n\n### Why don\'t you use plain `enum` classes?\n\nJava `enum` classes are not trivially   [forwards compatible](https://www.stainless.com/blog/making-java-enums-forwards-compatible). Using them in   the SDK could cause runtime exceptions if the API is updated to respond with a new enum value.\n\n### Why do you represent fields using `JsonField<T>` instead of just plain `T`?\n\nUsing `JsonField<T>` enables a few features:\n\n- Allowing usage of [undocumented API functionality](#undocumented-api-functionality)\n- Lazily [validating the API response against the expected shape](#response-validation)\n- Representing absent vs explicitly null values\n\n### Why don\'t you use [`data` classes](https://kotlinlang.org/docs/data-classes.html)?\n\nIt is not [backwards compatible to add new fields to a data class](https://kotlinlang.org/docs/api-guidelines-backward-compatibility.html#avoid-using-data-classes-in-your-api)   and we don\'t want to introduce a breaking change every time we add a field to a class.\n\n### Why don\'t you use checked exceptions?\n\nChecked exceptions are widely considered a mistake in the Java programming language. In fact, they were   omitted from Kotlin for this reason.\n\nChecked exceptions:\n\n- Are verbose to handle\n- Encourage error handling at the wrong level of abstraction, where nothing can be done about the error\n- Are tedious to propagate due to the [function coloring problem](https://journal.stuffwithstuff.com/2015/02/01/what-color-is-your-function)\n- Don\'t play well with lambdas (also due to the function coloring problem)\n\n## Semantic versioning\n\nThis package generally follows [SemVer](https://semver.org/spec/v2.0.0.html) conventions, though certain backwards-incompatible changes may be released as minor versions:\n\n1. Changes to library internals which are technically public but not intended or documented for external use. _(Please open a GitHub issue to let us know if you are relying on such internals.)_\n2. Changes that we do not expect to impact the vast majority of users in practice.\n\nWe take backwards-compatibility seriously and work hard to ensure you can rely on a smooth upgrade experience.\n\nWe are keen for your feedback; please open an [issue](https://www.github.com/DefinitelyATestOrg/sam-java/issues) with questions, bugs, or suggestions.\n',
  },
  {
    language: 'kotlin',
    content:
      '# Sam Kotlin API Library\n\n<!-- x-release-please-start-version -->\n[![Maven Central](https://img.shields.io/maven-central/v/me.elborai.api/sam-kotlin)](https://central.sonatype.com/artifact/me.elborai.api/sam-kotlin/0.0.1)\n[![javadoc](https://javadoc.io/badge2/me.elborai.api/sam-kotlin/0.0.1/javadoc.svg)](https://javadoc.io/doc/me.elborai.api/sam-kotlin/0.0.1)\n<!-- x-release-please-end -->\n\nThe Sam Kotlin SDK provides convenient access to the [Sam REST API](https://docs.sam.com)   from applications written in Kotlin.\n\n\n\nIt is generated with [Stainless](https://www.stainless.com/).\n\n## MCP Server\n\nUse the Sam MCP Server to enable AI assistants to interact with this API, allowing them to explore endpoints, make test requests, and use documentation to help integrate this SDK into your application.\n\n[![Add to Cursor](https://cursor.com/deeplink/mcp-install-dark.svg)](https://cursor.com/en-US/install-mcp?name=sam-mcp&config=eyJjb21tYW5kIjoibnB4IiwiYXJncyI6WyIteSIsInNhbS1tY3AiXSwiZW52Ijp7IkFQSV9LRVkiOiJNeSBBUEkgS2V5In19)\n[![Install in VS Code](https://img.shields.io/badge/_-Add_to_VS_Code-blue?style=for-the-badge&logo=data:image/svg%2bxml;base64,)](https://vscode.stainless.com/mcp/%7B%22name%22%3A%22sam-mcp%22%2C%22command%22%3A%22npx%22%2C%22args%22%3A%5B%22-y%22%2C%22sam-mcp%22%5D%2C%22env%22%3A%7B%22API_KEY%22%3A%22My%20API%20Key%22%7D%7D)\n\n> Note: You may need to set environment variables in your MCP client.\n\n<!-- x-release-please-start-version -->\n\nThe REST API documentation can be found on [docs.sam.com](https://docs.sam.com). KDocs are available on [javadoc.io](https://javadoc.io/doc/me.elborai.api/sam-kotlin/0.0.1).\n\n<!-- x-release-please-end -->\n\n## Installation\n\n<!-- x-release-please-start-version -->\n\n### Gradle\n\n~~~kotlin\nimplementation("me.elborai.api:sam-kotlin:0.0.1")\n~~~\n\n### Maven\n\n~~~xml\n<dependency>\n  <groupId>me.elborai.api</groupId>\n  <artifactId>sam-kotlin</artifactId>\n  <version>0.0.1</version>\n</dependency>\n~~~\n\n<!-- x-release-please-end -->\n\n## Requirements\n\nThis library requires Java 8 or later.\n\n## Usage\n\n```kotlin\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\nimport me.elborai.api.models.messages.MessageCreateParams\nimport me.elborai.api.models.messages.MessageCreateResponse\n\n// Configures using the `sam.apiKey` and `sam.baseUrl` system properties\n// Or configures using the `API_KEY` and `SAM_BASE_URL` environment variables\nval client: SamClient = SamOkHttpClient.fromEnv()\n\nval params: MessageCreateParams = MessageCreateParams.builder()\n    .maxTokens(1024L)\n    .addMessage(MessageCreateParams.Message.builder()\n        .content("Hello, world")\n        .role(MessageCreateParams.Message.Role.USER)\n        .build())\n    .model("claude-3-7-sonnet-20250219")\n    .build()\nval message: MessageCreateResponse = client.messages().create(params)\n```\n\n## Client configuration\n\nConfigure the client using system properties or environment variables:\n\n```kotlin\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\n\n// Configures using the `sam.apiKey` and `sam.baseUrl` system properties\n// Or configures using the `API_KEY` and `SAM_BASE_URL` environment variables\nval client: SamClient = SamOkHttpClient.fromEnv()\n```\n\nOr manually:\n\n```kotlin\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\n\nval client: SamClient = SamOkHttpClient.builder()\n    .apiKey("My API Key")\n    .build()\n```\n\nOr using a combination of the two approaches:\n\n```kotlin\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\n\nval client: SamClient = SamOkHttpClient.builder()\n    // Configures using the `sam.apiKey` and `sam.baseUrl` system properties\n    // Or configures using the `API_KEY` and `SAM_BASE_URL` environment variables\n    .fromEnv()\n    .apiKey("My API Key")\n    .build()\n```\n\nSee this table for the available options:\n\n| Setter    | System property | Environment variable | Required | Default value |\n| --------- | --------------- | -------------------- | -------- | ------------- |\n| `apiKey`  | `sam.apiKey`    | `API_KEY`            | true     | -             |\n| `baseUrl` | `sam.baseUrl`   | `SAM_BASE_URL`       | true     | `"/api/v3"`   |\n\nSystem properties take precedence over environment variables.\n\n> [!TIP]\n> Don\'t create more than one client in the same application. Each client has a connection pool and\n> thread pools, which are more efficient to share between requests.\n\n### Modifying configuration\n\nTo temporarily use a modified client configuration, while reusing the same connection and thread       pools, call `withOptions()` on any client or service:\n\n```kotlin\nimport me.elborai.api.client.SamClient\n\nval clientWithOptions: SamClient = client.withOptions {\n    it.baseUrl("https://example.com")\n    it.maxRetries(42)\n}\n```\n\nThe `withOptions()` method does not affect the original client or service.\n\n## Requests and responses\n\nTo send a request to the Sam API, build an instance of some `Params` class and pass it to the     corresponding client method. When the response is received, it will be deserialized into an instance of     a Kotlin class.\n\nFor example, `client.messages().create(...)` should be called with an instance of `MessageCreateParams`, and it     will return an instance of `MessageCreateResponse`.\n\n## Immutability\n\nEach class in the SDK has an associated   [builder](https://blogs.oracle.com/javamagazine/post/exploring-joshua-blochs-builder-design-pattern-in-java)   or factory method for constructing it.\n\nEach class is [immutable](https://docs.oracle.com/javase/tutorial/essential/concurrency/immutable.html)   once constructed. If the class has an associated builder, then it has a `toBuilder()` method, which can   be used to convert it back to a builder for making a modified copy.\n\nBecause each class is immutable, builder modification will _never_ affect already built class instances.\n\n## Asynchronous execution\n\nThe default client is synchronous. To switch to asynchronous execution, call the `async()` method:\n\n```kotlin\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\nimport me.elborai.api.models.messages.MessageCreateParams\nimport me.elborai.api.models.messages.MessageCreateResponse\n\n// Configures using the `sam.apiKey` and `sam.baseUrl` system properties\n// Or configures using the `API_KEY` and `SAM_BASE_URL` environment variables\nval client: SamClient = SamOkHttpClient.fromEnv()\n\nval params: MessageCreateParams = MessageCreateParams.builder()\n    .maxTokens(1024L)\n    .addMessage(MessageCreateParams.Message.builder()\n        .content("Hello, world")\n        .role(MessageCreateParams.Message.Role.USER)\n        .build())\n    .model("claude-3-7-sonnet-20250219")\n    .build()\nval message: MessageCreateResponse = client.async().messages().create(params)\n```\n\nOr create an asynchronous client from the beginning:\n\n```kotlin\nimport me.elborai.api.client.SamClientAsync\nimport me.elborai.api.client.okhttp.SamOkHttpClientAsync\nimport me.elborai.api.models.messages.MessageCreateParams\nimport me.elborai.api.models.messages.MessageCreateResponse\n\n// Configures using the `sam.apiKey` and `sam.baseUrl` system properties\n// Or configures using the `API_KEY` and `SAM_BASE_URL` environment variables\nval client: SamClientAsync = SamOkHttpClientAsync.fromEnv()\n\nval params: MessageCreateParams = MessageCreateParams.builder()\n    .maxTokens(1024L)\n    .addMessage(MessageCreateParams.Message.builder()\n        .content("Hello, world")\n        .role(MessageCreateParams.Message.Role.USER)\n        .build())\n    .model("claude-3-7-sonnet-20250219")\n    .build()\nval message: MessageCreateResponse = client.messages().create(params)\n```\n\nThe asynchronous client supports the same options as the synchronous one, except most methods are [suspending](https://kotlinlang.org/docs/coroutines-guide.html).\n\n\n\n\n\n\n\n## Raw responses\n\nThe SDK defines methods that deserialize responses into instances of Kotlin classes.       However, these methods don\'t provide access to the response headers, status code, or the raw response       body.\n\nTo access this data, prefix any HTTP method call on a client or service with `withRawResponse()`:\n\n```kotlin\nimport me.elborai.api.core.http.Headers\nimport me.elborai.api.core.http.HttpResponseFor\nimport me.elborai.api.models.messages.MessageCreateParams\nimport me.elborai.api.models.messages.MessageCreateResponse\n\nval params: MessageCreateParams = MessageCreateParams.builder()\n    .maxTokens(1024L)\n    .addMessage(MessageCreateParams.Message.builder()\n        .content("Hello, world")\n        .role(MessageCreateParams.Message.Role.USER)\n        .build())\n    .model("claude-3-7-sonnet-20250219")\n    .build()\nval message: HttpResponseFor<MessageCreateResponse> = client.messages().withRawResponse().create(params)\n\nval statusCode: Int = message.statusCode()\nval headers: Headers = message.headers()\n```\n\nYou can still deserialize the response into an instance of a Kotlin class if needed:\n\n```kotlin\nimport me.elborai.api.models.messages.MessageCreateResponse\n\nval parsedMessage: MessageCreateResponse = message.parse()\n```\n\n## Error handling\n\nThe SDK throws custom unchecked exception types:\n\n- [`SamServiceException`](sam-kotlin-core/src/main/kotlin/me/elborai/api/errors/SamServiceException.kt): Base class for HTTP errors. See this table for which exception       subclass is thrown for each HTTP status code:\n\n  | Status | Exception                                          |\n  | ------ | -------------------------------------------------- |\n  | 400    | [`BadRequestException`](sam-kotlin-core/src/main/kotlin/me/elborai/api/errors/BadRequestException.kt)           |\n  | 401    | [`UnauthorizedException`](sam-kotlin-core/src/main/kotlin/me/elborai/api/errors/UnauthorizedException.kt)         |\n  | 403    | [`PermissionDeniedException`](sam-kotlin-core/src/main/kotlin/me/elborai/api/errors/PermissionDeniedException.kt)     |\n  | 404    | [`NotFoundException`](sam-kotlin-core/src/main/kotlin/me/elborai/api/errors/NotFoundException.kt)             |\n  | 422    | [`UnprocessableEntityException`](sam-kotlin-core/src/main/kotlin/me/elborai/api/errors/UnprocessableEntityException.kt)  |\n  | 429    | [`RateLimitException`](sam-kotlin-core/src/main/kotlin/me/elborai/api/errors/RateLimitException.kt)            |\n  | 5xx    | [`InternalServerException`](sam-kotlin-core/src/main/kotlin/me/elborai/api/errors/InternalServerException.kt)       |\n  | others | [`UnexpectedStatusCodeException`](sam-kotlin-core/src/main/kotlin/me/elborai/api/errors/UnexpectedStatusCodeException.kt) |\n\n- [`SamIoException`](sam-kotlin-core/src/main/kotlin/me/elborai/api/errors/SamIoException.kt): I/O networking errors.\n\n- [`SamRetryableException`](sam-kotlin-core/src/main/kotlin/me/elborai/api/errors/SamRetryableException.kt): Generic error indicating a failure that could be retried by the client.\n\n- [`SamInvalidDataException`](sam-kotlin-core/src/main/kotlin/me/elborai/api/errors/SamInvalidDataException.kt): Failure to interpret successfully parsed data. For example,       when accessing a property that\'s supposed to be required, but the API unexpectedly omitted it from the       response.\n\n- [`SamException`](sam-kotlin-core/src/main/kotlin/me/elborai/api/errors/SamException.kt): Base class for all exceptions. Most errors will result in one of the       previously mentioned ones, but completely generic errors may be thrown using the base class.\n\n\n\n## Logging\n\nThe SDK uses the standard   [OkHttp logging interceptor](https://github.com/square/okhttp/tree/master/okhttp-logging-interceptor).\n\nEnable logging by setting the `SAM_LOG` environment variable to   `info`:\n\n```sh\nexport SAM_LOG=info\n```\n\nOr to `debug` for more verbose logging:\n\n```sh\nexport SAM_LOG=debug\n```\n\n## ProGuard and R8\n\nAlthough the SDK uses reflection, it is still usable with     [ProGuard](https://github.com/Guardsquare/proguard) and     [R8](https://developer.android.com/topic/performance/app-optimization/enable-app-optimization) because     `sam-kotlin-core` is published with a     [configuration file](sam-kotlin-core/src/main/resources/META-INF/proguard/sam-kotlin-core.pro) containing     [keep rules](https://www.guardsquare.com/manual/configuration/usage).\n\nProGuard and R8 should automatically detect and use the published rules, but you can also manually copy     the keep rules if necessary.\n\n\n\n\n\n## Jackson\n\nThe SDK depends on [Jackson](https://github.com/FasterXML/jackson) for JSON     serialization/deserialization. It is compatible with version 2.13.4 or higher,     but depends on version 2.18.2 by default.\n\nThe SDK throws an exception if it detects an incompatible Jackson version at runtime (e.g. if the     default version was overridden in your Maven or Gradle config).\n\nIf the SDK threw an exception, but you\'re _certain_ the version is compatible, then disable the version     check using the `checkJacksonVersionCompatibility` on [`SamOkHttpClient`](sam-kotlin-client-okhttp/src/main/kotlin/me/elborai/api/client/okhttp/SamOkHttpClient.kt) or     [`SamOkHttpClientAsync`](sam-kotlin-client-okhttp/src/main/kotlin/me/elborai/api/client/okhttp/SamOkHttpClientAsync.kt).\n\n> [!CAUTION]\n> We make no guarantee that the SDK works correctly when the Jackson version check is disabled.\n\nAlso note that there are bugs in older Jackson versions that can affect the SDK. We don\'t work around all     Jackson bugs ([example](https://github.com/FasterXML/jackson-databind/issues/3240)) and expect users to     upgrade Jackson for those instead.\n\n## Network options\n\n### Retries\n\nThe SDK automatically retries 2 times by default, with a short exponential backoff between requests.\n\nOnly the following error types are retried:\n- Connection errors (for example, due to a network connectivity problem)\n- 408 Request Timeout\n- 409 Conflict\n- 429 Rate Limit\n- 5xx Internal\n\nThe API may also explicitly instruct the SDK to retry or not retry a request.\n\nTo set a custom number of retries, configure the client using the `maxRetries` method:\n\n```kotlin\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\n\nval client: SamClient = SamOkHttpClient.builder()\n    .fromEnv()\n    .maxRetries(4)\n    .build()\n```\n\n### Timeouts\n\nRequests time out after 1 minute by default.\n\nTo set a custom timeout, configure the method call using the `timeout` method:\n\n```kotlin\nimport me.elborai.api.models.messages.MessageCreateResponse\n\nval message: MessageCreateResponse = client.messages().create(\n  params, RequestOptions.builder().timeout(Duration.ofSeconds(30)).build()\n)\n```\n\nOr configure the default for all method calls at the client level:\n\n```kotlin\nimport java.time.Duration\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\n\nval client: SamClient = SamOkHttpClient.builder()\n    .fromEnv()\n    .timeout(Duration.ofSeconds(30))\n    .build()\n```\n\n### Proxies\n\nTo route requests through a proxy, configure the client using the `proxy` method:\n\n```kotlin\nimport java.net.InetSocketAddress\nimport java.net.Proxy\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\n\nval client: SamClient = SamOkHttpClient.builder()\n    .fromEnv()\n    .proxy(Proxy(\n      Proxy.Type.HTTP, InetSocketAddress(\n        "https://example.com", 8080\n      )\n    ))\n    .build()\n```\n\n### Connection pooling\n\nTo customize the underlying OkHttp connection pool, configure the client using the   `maxIdleConnections` and `keepAliveDuration` methods:\n\n```kotlin\nimport java.time.Duration\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\n\nval client: SamClient = SamOkHttpClient.builder()\n    .fromEnv()\n    // If `maxIdleConnections` is set, then `keepAliveDuration` must be set, and vice versa.\n    .maxIdleConnections(10)\n    .keepAliveDuration(Duration.ofMinutes(2))\n    .build()\n```\n\nIf both options are unset, OkHttp\'s default connection pool settings are used.\n\n### HTTPS\n\n> [!NOTE]\n> Most applications should not call these methods, and instead use the system defaults. The defaults include\n> special optimizations that can be lost if the implementations are modified.\n\nTo configure how HTTPS connections are secured, configure the client using the `sslSocketFactory`,   `trustManager`, and `hostnameVerifier` methods:\n\n```kotlin\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\n\nval client: SamClient = SamOkHttpClient.builder()\n    .fromEnv()\n    // If `sslSocketFactory` is set, then `trustManager` must be set, and vice versa.\n    .sslSocketFactory(yourSSLSocketFactory)\n    .trustManager(yourTrustManager)\n    .hostnameVerifier(yourHostnameVerifier)\n    .build()\n```\n\n\n\n### Custom HTTP client\n\nThe SDK consists of three artifacts:\n- `sam-kotlin-core`\n  - Contains core SDK logic\n  - Does not depend on [OkHttp](https://square.github.io/okhttp)\n  - Exposes [`SamClient`](sam-kotlin-core/src/main/kotlin/me/elborai/api/client/SamClient.kt), [`SamClientAsync`](sam-kotlin-core/src/main/kotlin/me/elborai/api/client/SamClientAsync.kt),             [`SamClientImpl`](sam-kotlin-core/src/main/kotlin/me/elborai/api/client/SamClientImpl.kt), and [`SamClientAsyncImpl`](sam-kotlin-core/src/main/kotlin/me/elborai/api/client/SamClientAsyncImpl.kt), all of which can             work with any HTTP client\n- `sam-kotlin-client-okhttp`\n  - Depends on [OkHttp](https://square.github.io/okhttp)\n  - Exposes [`SamOkHttpClient`](sam-kotlin-client-okhttp/src/main/kotlin/me/elborai/api/client/okhttp/SamOkHttpClient.kt) and [`SamOkHttpClientAsync`](sam-kotlin-client-okhttp/src/main/kotlin/me/elborai/api/client/okhttp/SamOkHttpClientAsync.kt), which             provide a way to construct [`SamClientImpl`](sam-kotlin-core/src/main/kotlin/me/elborai/api/client/SamClientImpl.kt) and             [`SamClientAsyncImpl`](sam-kotlin-core/src/main/kotlin/me/elborai/api/client/SamClientAsyncImpl.kt), respectively, using OkHttp\n- `sam-kotlin`\n  - Depends on and exposes the APIs of both `sam-kotlin-core` and `sam-kotlin-client-okhttp`\n  - Does not have its own logic\n\nThis structure allows replacing the SDK\'s default HTTP client without pulling in unnecessary dependencies.\n\n#### Customized [`OkHttpClient`](https://square.github.io/okhttp/3.x/okhttp/okhttp3/OkHttpClient.html)\n\n> [!TIP]\n> Try the available [network options](#network-options) before replacing the default client.\n\nTo use a customized `OkHttpClient`:\n\n1. Replace your [`sam-kotlin` dependency](#installation) with `sam-kotlin-core`\n2. Copy `sam-kotlin-client-okhttp`\'s [`OkHttpClient`](sam-kotlin-client-okhttp/src/main/kotlin/me/elborai/api/client/okhttp/OkHttpClient.kt) class into your code and        customize it\n3. Construct [`SamClientImpl`](sam-kotlin-core/src/main/kotlin/me/elborai/api/client/SamClientImpl.kt) or [`SamClientAsyncImpl`](sam-kotlin-core/src/main/kotlin/me/elborai/api/client/SamClientAsyncImpl.kt), similarly to        [`SamOkHttpClient`](sam-kotlin-client-okhttp/src/main/kotlin/me/elborai/api/client/okhttp/SamOkHttpClient.kt) or [`SamOkHttpClientAsync`](sam-kotlin-client-okhttp/src/main/kotlin/me/elborai/api/client/okhttp/SamOkHttpClientAsync.kt), using your        customized client\n\n### Completely custom HTTP client\n\nTo use a completely custom HTTP client:\n\n1. Replace your [`sam-kotlin` dependency](#installation) with `sam-kotlin-core`\n2. Write a class that implements the [`HttpClient`](sam-kotlin-core/src/main/kotlin/me/elborai/api/core/http/HttpClient.kt) interface\n3. Construct [`SamClientImpl`](sam-kotlin-core/src/main/kotlin/me/elborai/api/client/SamClientImpl.kt) or [`SamClientAsyncImpl`](sam-kotlin-core/src/main/kotlin/me/elborai/api/client/SamClientAsyncImpl.kt), similarly to        [`SamOkHttpClient`](sam-kotlin-client-okhttp/src/main/kotlin/me/elborai/api/client/okhttp/SamOkHttpClient.kt) or [`SamOkHttpClientAsync`](sam-kotlin-client-okhttp/src/main/kotlin/me/elborai/api/client/okhttp/SamOkHttpClientAsync.kt), using your new        client class\n\n## Undocumented API functionality\n\nThe SDK is typed for convenient usage of the documented API. However, it also supports working with undocumented or not yet supported parts of the API.\n\n### Parameters\n\nTo set undocumented parameters, call the `putAdditionalHeader`, `putAdditionalQueryParam`, or       `putAdditionalBodyProperty` methods on any `Params` class:\n\n```kotlin\nimport me.elborai.api.core.JsonValue\nimport me.elborai.api.models.messages.MessageCreateParams\n\nval params: MessageCreateParams = MessageCreateParams.builder()\n    .putAdditionalHeader("Secret-Header", "42")\n    .putAdditionalQueryParam("secret_query_param", "42")\n    .putAdditionalBodyProperty("secretProperty", JsonValue.from("42"))\n    .build()\n```\n\nThese can be accessed on the built object later using the `_additionalHeaders()`,       `_additionalQueryParams()`, and `_additionalBodyProperties()` methods.\n\nTo set undocumented parameters on _nested_ headers, query params, or body classes, call the         `putAdditionalProperty` method on the nested class:\n\n```kotlin\nimport me.elborai.api.core.JsonValue\nimport me.elborai.api.models.messages.MessageCreateParams\n\nval params: MessageCreateParams = MessageCreateParams.builder()\n    .metadata(MessageCreateParams.Metadata.builder()\n        .putAdditionalProperty("secretProperty", JsonValue.from("42"))\n        .build())\n    .build()\n```\n\nThese properties can be accessed on the nested built object later using the         `_additionalProperties()` method.\n\nTo set a documented parameter or property to an undocumented or not yet supported _value_, pass a       [`JsonValue`](sam-kotlin-core/src/main/kotlin/me/elborai/api/core/Values.kt) object to its setter:\n\n```kotlin\nimport me.elborai.api.models.messages.MessageCreateParams\n\nval params: MessageCreateParams = MessageCreateParams.builder()\n    .maxTokens(1024L)\n    .addMessage(MessageCreateParams.Message.builder()\n        .content("Hello, world")\n        .role(MessageCreateParams.Message.Role.USER)\n        .build())\n    .model("claude-3-7-sonnet-20250219")\n    .build()\n```\n\nThe most straightforward way to create a [`JsonValue`](sam-kotlin-core/src/main/kotlin/me/elborai/api/core/Values.kt) is using its       `from(...)` method:\n\n```kotlin\nimport me.elborai.api.core.JsonValue\n\n// Create primitive JSON values\nval nullValue: JsonValue = JsonValue.from(null)\nval booleanValue: JsonValue = JsonValue.from(true)\nval numberValue: JsonValue = JsonValue.from(42)\nval stringValue: JsonValue = JsonValue.from("Hello World!")\n\n// Create a JSON array value equivalent to `["Hello", "World"]`\nval arrayValue: JsonValue = JsonValue.from(listOf(\n  "Hello", "World"\n))\n\n// Create a JSON object value equivalent to `{ "a": 1, "b": 2 }`\nval objectValue: JsonValue = JsonValue.from(mapOf(\n  "a" to 1, "b" to 2\n))\n\n// Create an arbitrarily nested JSON equivalent to:\n// {\n//   "a": [1, 2],\n//   "b": [3, 4]\n// }\nval complexValue: JsonValue = JsonValue.from(mapOf(\n  "a" to listOf(\n    1, 2\n  ), "b" to listOf(\n    3, 4\n  )\n))\n```\n\nNormally a `Builder` class\'s `build` method will throw         [`IllegalStateException`](https://docs.oracle.com/javase/8/docs/api/java/lang/IllegalStateException.html)         if any required parameter or property is unset.\n\nTo forcibly omit a required parameter or property, pass [`JsonMissing`](sam-kotlin-core/src/main/kotlin/me/elborai/api/core/Values.kt):\n\n```kotlin\nimport me.elborai.api.core.JsonMissing\nimport me.elborai.api.models.messages.MessageCreateParams\n\nval params: MessageCreateParams = MessageCreateParams.builder()\n    .addMessage(MessageCreateParams.Message.builder()\n        .content("Hello, world")\n        .role(MessageCreateParams.Message.Role.USER)\n        .build())\n    .model("claude-3-7-sonnet-20250219")\n    .maxTokens(JsonMissing.of())\n    .build()\n```\n\n### Response properties\n\nTo access undocumented response properties, call the `_additionalProperties()` method:\n\n```kotlin\nimport me.elborai.api.core.JsonBoolean\nimport me.elborai.api.core.JsonNull\nimport me.elborai.api.core.JsonNumber\nimport me.elborai.api.core.JsonValue\n\nval additionalProperties: Map<String, JsonValue> = client.messages().create(params)._additionalProperties()\nval secretPropertyValue: JsonValue = additionalProperties.get("secretProperty")\n\nval result = when (secretPropertyValue) {\n    is JsonNull -> "It\'s null!"\n    is JsonBoolean -> "It\'s a boolean!"\n    is JsonNumber -> "It\'s a number!"\n    // Other types include `JsonMissing`, `JsonString`, `JsonArray`, and `JsonObject`\n    else -> "It\'s something else!"\n}\n```\n\nTo access a property\'s raw JSON value, which may be undocumented, call its `_` prefixed method:\n\n```kotlin\nimport me.elborai.api.core.JsonField\n\nval maxTokens: JsonField<Long> = client.messages().create(params)._maxTokens()\n\nif (maxTokens.isMissing()) {\n  // The property is absent from the JSON response\n} else if (maxTokens.isNull()) {\n  // The property was set to literal null\n} else {\n  // Check if value was provided as a string\n  // Other methods include `asNumber()`, `asBoolean()`, etc.\n  val jsonString: String? = maxTokens.asString();\n\n  // Try to deserialize into a custom type\n  val myObject: MyClass = maxTokens.asUnknown()!!.convert(MyClass::class.java)\n}\n```\n\n### Response validation\n\nIn rare cases, the API may return a response that doesn\'t match the expected type. For example, the SDK     may expect a property to contain a `String`, but the API could return something else.\n\nBy default, the SDK will not throw an exception in this case. It will throw     [`SamInvalidDataException`](sam-kotlin-core/src/main/kotlin/me/elborai/api/errors/SamInvalidDataException.kt) only if you directly access the property.\n\nIf you would prefer to check that the response is completely well-typed upfront, then either call     `validate()`:\n\n```kotlin\nimport me.elborai.api.models.messages.MessageCreateResponse\n\nval message: MessageCreateResponse = client.messages().create(params).validate()\n```\n\nOr configure the method call to validate the response using the `responseValidation` method:\n\n```kotlin\nimport me.elborai.api.models.messages.MessageCreateResponse\n\nval message: MessageCreateResponse = client.messages().create(\n  params, RequestOptions.builder().responseValidation(true).build()\n)\n```\n\nOr configure the default for all method calls at the client level:\n\n```kotlin\nimport me.elborai.api.client.SamClient\nimport me.elborai.api.client.okhttp.SamOkHttpClient\n\nval client: SamClient = SamOkHttpClient.builder()\n    .fromEnv()\n    .responseValidation(true)\n    .build()\n```\n\n## FAQ\n\n### Why don\'t you use plain `enum` classes?\n\nKotlin `enum` classes are not trivially   [forwards compatible](https://www.stainless.com/blog/making-java-enums-forwards-compatible). Using them in   the SDK could cause runtime exceptions if the API is updated to respond with a new enum value.\n\n### Why do you represent fields using `JsonField<T>` instead of just plain `T`?\n\nUsing `JsonField<T>` enables a few features:\n\n- Allowing usage of [undocumented API functionality](#undocumented-api-functionality)\n- Lazily [validating the API response against the expected shape](#response-validation)\n- Representing absent vs explicitly null values\n\n### Why don\'t you use [`data` classes](https://kotlinlang.org/docs/data-classes.html)?\n\nIt is not [backwards compatible to add new fields to a data class](https://kotlinlang.org/docs/api-guidelines-backward-compatibility.html#avoid-using-data-classes-in-your-api)   and we don\'t want to introduce a breaking change every time we add a field to a class.\n\n### Why don\'t you use checked exceptions?\n\nChecked exceptions are widely considered a mistake in the Java programming language. In fact, they were   omitted from Kotlin for this reason.\n\nChecked exceptions:\n\n- Are verbose to handle\n- Encourage error handling at the wrong level of abstraction, where nothing can be done about the error\n- Are tedious to propagate due to the [function coloring problem](https://journal.stuffwithstuff.com/2015/02/01/what-color-is-your-function)\n- Don\'t play well with lambdas (also due to the function coloring problem)\n\n## Semantic versioning\n\nThis package generally follows [SemVer](https://semver.org/spec/v2.0.0.html) conventions, though certain backwards-incompatible changes may be released as minor versions:\n\n1. Changes to library internals which are technically public but not intended or documented for external use. _(Please open a GitHub issue to let us know if you are relying on such internals.)_\n2. Changes that we do not expect to impact the vast majority of users in practice.\n\nWe take backwards-compatibility seriously and work hard to ensure you can rely on a smooth upgrade experience.\n\nWe are keen for your feedback; please open an [issue](https://www.github.com/DefinitelyATestOrg/sam-kotlin/issues) with questions, bugs, or suggestions.\n',
  },
];

const INDEX_OPTIONS = {
  fields: [
    'name',
    'endpoint',
    'summary',
    'description',
    'qualified',
    'stainlessPath',
    'content',
    'sectionContext',
  ],
  storeFields: ['kind', '_original'],
  searchOptions: {
    prefix: true,
    fuzzy: 0.1,
    boost: {
      name: 5,
      stainlessPath: 3,
      endpoint: 3,
      qualified: 3,
      summary: 2,
      content: 1,
      description: 1,
    } as Record<string, number>,
  },
};

/**
 * Self-contained local search engine backed by MiniSearch.
 * Method data is embedded at SDK build time; prose documents
 * can be loaded from an optional docs directory at runtime.
 */
export class LocalDocsSearch {
  private methodIndex: MiniSearch<MiniSearchDocument>;
  private proseIndex: MiniSearch<MiniSearchDocument>;

  private constructor() {
    this.methodIndex = new MiniSearch<MiniSearchDocument>(INDEX_OPTIONS);
    this.proseIndex = new MiniSearch<MiniSearchDocument>(INDEX_OPTIONS);
  }

  static async create(opts?: { docsDir?: string }): Promise<LocalDocsSearch> {
    const instance = new LocalDocsSearch();
    instance.indexMethods(EMBEDDED_METHODS);
    for (const readme of EMBEDDED_READMES) {
      instance.indexProse(readme.content, `readme:${readme.language}`);
    }
    if (opts?.docsDir) {
      await instance.loadDocsDirectory(opts.docsDir);
    }
    return instance;
  }

  search(props: {
    query: string;
    language?: string;
    detail?: string;
    maxResults?: number;
    maxLength?: number;
  }): SearchResult {
    const { query, language = 'typescript', detail = 'default', maxResults = 5, maxLength = 100_000 } = props;

    const useMarkdown = detail === 'verbose' || detail === 'high';

    // Search both indices and merge results by score.
    // Filter prose hits so language-tagged content (READMEs and docs with
    // frontmatter) only matches the requested language.
    const methodHits = this.methodIndex
      .search(query)
      .map((hit) => ({ ...hit, _kind: 'http_method' as const }));
    const proseHits = this.proseIndex
      .search(query)
      .filter((hit) => {
        const source = ((hit as Record<string, unknown>)['_original'] as ProseChunk | undefined)?.source;
        if (!source) return true;
        // Check for language-tagged sources: "readme:<lang>" or "lang:<lang>:<filename>"
        let taggedLang: string | undefined;
        if (source.startsWith('readme:')) taggedLang = source.slice('readme:'.length);
        else if (source.startsWith('lang:')) taggedLang = source.split(':')[1];
        if (!taggedLang) return true;
        return taggedLang === language || (language === 'javascript' && taggedLang === 'typescript');
      })
      .map((hit) => ({ ...hit, _kind: 'prose' as const }));
    const merged = [...methodHits, ...proseHits].sort((a, b) => b.score - a.score);
    const top = merged.slice(0, maxResults);

    const fullResults: (string | Record<string, unknown>)[] = [];

    for (const hit of top) {
      const original = (hit as Record<string, unknown>)['_original'];
      if (hit._kind === 'http_method') {
        const m = original as MethodEntry;
        if (useMarkdown && m.markdown) {
          fullResults.push(m.markdown);
        } else {
          // Use per-language data when available, falling back to the
          // top-level fields (which are TypeScript-specific in the
          // legacy codepath).
          const langData = m.perLanguage?.[language];
          fullResults.push({
            method: langData?.method ?? m.qualified,
            summary: m.summary,
            description: m.description,
            endpoint: `${m.httpMethod.toUpperCase()} ${m.endpoint}`,
            ...(langData?.example ? { example: langData.example } : {}),
            ...(m.params ? { params: m.params } : {}),
            ...(m.response ? { response: m.response } : {}),
          });
        }
      } else {
        const c = original as ProseChunk;
        fullResults.push({
          content: c.content,
          ...(c.source ? { source: c.source } : {}),
        });
      }
    }

    let totalLength = 0;
    const results: (string | Record<string, unknown>)[] = [];
    for (const result of fullResults) {
      const len = typeof result === 'string' ? result.length : JSON.stringify(result).length;
      totalLength += len;
      if (totalLength > maxLength) break;
      results.push(result);
    }

    if (results.length < fullResults.length) {
      results.unshift(`Truncated; showing ${results.length} of ${fullResults.length} results.`);
    }

    return { results };
  }

  private indexMethods(methods: MethodEntry[]): void {
    const docs: MiniSearchDocument[] = methods.map((m, i) => ({
      id: `method-${i}`,
      kind: 'http_method' as const,
      name: m.name,
      endpoint: m.endpoint,
      summary: m.summary,
      description: m.description,
      qualified: m.qualified,
      stainlessPath: m.stainlessPath,
      _original: m as unknown as Record<string, unknown>,
    }));
    if (docs.length > 0) {
      this.methodIndex.addAll(docs);
    }
  }

  private async loadDocsDirectory(docsDir: string): Promise<void> {
    let entries;
    try {
      entries = await fs.readdir(docsDir, { withFileTypes: true });
    } catch (err) {
      getLogger().warn({ err, docsDir }, 'Could not read docs directory');
      return;
    }

    const files = entries
      .filter((e) => e.isFile())
      .filter((e) => e.name.endsWith('.md') || e.name.endsWith('.markdown') || e.name.endsWith('.json'));

    for (const file of files) {
      try {
        const filePath = path.join(docsDir, file.name);
        const content = await fs.readFile(filePath, 'utf-8');

        if (file.name.endsWith('.json')) {
          const texts = extractTexts(JSON.parse(content));
          if (texts.length > 0) {
            this.indexProse(texts.join('\n\n'), file.name);
          }
        } else {
          // Parse optional YAML frontmatter for language tagging.
          // Files with a "language" field in frontmatter will only
          // surface in searches for that language.
          //
          // Example:
          //   ---
          //   language: python
          //   ---
          //   # Error handling in Python
          //   ...
          const frontmatter = parseFrontmatter(content);
          const source = frontmatter.language ? `lang:${frontmatter.language}:${file.name}` : file.name;
          this.indexProse(content, source);
        }
      } catch (err) {
        getLogger().warn({ err, file: file.name }, 'Failed to index docs file');
      }
    }
  }

  private indexProse(markdown: string, source: string): void {
    const chunks = chunkMarkdown(markdown);
    const baseId = this.proseIndex.documentCount;

    const docs: MiniSearchDocument[] = chunks.map((chunk, i) => ({
      id: `prose-${baseId + i}`,
      kind: 'prose' as const,
      content: chunk.content,
      ...(chunk.sectionContext != null ? { sectionContext: chunk.sectionContext } : {}),
      _original: { ...chunk, source } as unknown as Record<string, unknown>,
    }));

    if (docs.length > 0) {
      this.proseIndex.addAll(docs);
    }
  }
}

/** Lightweight markdown chunker — splits on headers, chunks by word count. */
function chunkMarkdown(markdown: string): { content: string; tag: string; sectionContext?: string }[] {
  // Strip YAML frontmatter
  const stripped = markdown.replace(/^---\n[\s\S]*?\n---\n?/, '');
  const lines = stripped.split('\n');

  const chunks: { content: string; tag: string; sectionContext?: string }[] = [];
  const headers: string[] = [];
  let current: string[] = [];

  const flush = () => {
    const text = current.join('\n').trim();
    if (!text) return;
    const sectionContext = headers.length > 0 ? headers.join(' > ') : undefined;
    // Split into ~200-word chunks
    const words = text.split(/\s+/);
    for (let i = 0; i < words.length; i += 200) {
      const slice = words.slice(i, i + 200).join(' ');
      if (slice) {
        chunks.push({ content: slice, tag: 'p', ...(sectionContext != null ? { sectionContext } : {}) });
      }
    }
    current = [];
  };

  for (const line of lines) {
    const headerMatch = line.match(/^(#{1,6})\s+(.+)/);
    if (headerMatch) {
      flush();
      const level = headerMatch[1]!.length;
      const text = headerMatch[2]!.trim();
      while (headers.length >= level) headers.pop();
      headers.push(text);
    } else {
      current.push(line);
    }
  }
  flush();

  return chunks;
}

/** Recursively extracts string values from a JSON structure. */
function extractTexts(data: unknown, depth = 0): string[] {
  if (depth > 10) return [];
  if (typeof data === 'string') return data.trim() ? [data] : [];
  if (Array.isArray(data)) return data.flatMap((item) => extractTexts(item, depth + 1));
  if (typeof data === 'object' && data !== null) {
    return Object.values(data).flatMap((v) => extractTexts(v, depth + 1));
  }
  return [];
}

/** Parses YAML frontmatter from a markdown string, extracting the language field if present. */
function parseFrontmatter(markdown: string): { language?: string } {
  const match = markdown.match(/^---\n([\s\S]*?)\n---/);
  if (!match) return {};
  const body = match[1] ?? '';
  const langMatch = body.match(/^language:\s*(.+)$/m);
  return langMatch ? { language: langMatch[1]!.trim() } : {};
}
